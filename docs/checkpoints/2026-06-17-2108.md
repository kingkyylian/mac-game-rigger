# Project Checkpoint - 2026-06-17 21:08

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, latest commit before this checkpoint: `3488c4c data: add H-001 import smoke evidence`
- Context: Mac Game Rigger is progressing from planning into real production-trial evidence. The current milestone is a truthful 10-asset evidence pack, not a pass-looking manifest.

## Done This Session

- Added `tools/blender_asset_workflow.py`, a reusable headless Blender workflow runner for humanoid assets.
- Added `tests/test_blender_asset_workflow.py` for bbox-derived humanoid landmark generation.
- Ran the full H-001 Blender workflow on:
  - `local_assets/H-001/H-001-quaternius-animated-human.fbx`
- The workflow strips the source rig before testing Mac Game Rigger output:
  - removed 1 imported armature;
  - removed 1 imported armature modifier;
  - removed 34 imported vertex groups.
- Generated MGR armature and capsule weights:
  - 17 generated MGR bones;
  - 791 vertices capsule-weighted;
  - 0 unweighted vertices;
  - 0 over-limit vertices;
  - QA warnings/errors: none.
- Generated H-001 evidence artifacts:
  - `evidence/H-001/qa-report.json`
  - `evidence/H-001/preview-neutral.png`
  - `evidence/H-001/preview-pose.png`
  - `evidence/H-001/export-unity.fbx`
  - `evidence/H-001/export-unity.qa.json`
  - `evidence/H-001/workflow-summary.json`
  - `evidence/H-001/unity-import.json`
  - `evidence/H-001/unreal-import.json`
- Registered H-001 in `samples/manifest.json` with:
  - deformation score: `2`
  - Unity status: `blocked`
  - Unreal status: `blocked`
  - failure type: `deformation quality issue`
- Generated `docs/asset-evidence-progress.md`.

## Current State

- H-001 is now a complete evidence entry, but it is not a quality success.
- Production trial remains blocked.
- Current gate numbers:
  - `realAssetCount`: 1
  - `completeEvidenceCount`: 1
  - `score3PlusCount`: 0
  - `unityImportPassesAtLeast3`: false
  - `unrealPassOrExplicitBlocker`: true
- H-001 preview evidence is visible and complete, but it is side-view/weak pose evidence. It proves the pipeline runs, not that the rigging quality is acceptable for production.

## Important Files / Artifacts

- `tools/blender_asset_workflow.py`: full Blender workflow runner for imported humanoid assets.
- `tests/test_blender_asset_workflow.py`: unit coverage for generated landmark layout.
- `samples/manifest.json`: H-001 is now registered as real evidence with score 2.
- `docs/asset-evidence-progress.md`: current evidence dashboard.
- `evidence/H-001/notes.md`: source, workflow, QA, score, and blocker notes.
- `evidence/H-001/preview-neutral.png`: visual evidence for neutral pose.
- `evidence/H-001/preview-pose.png`: visual evidence for pose test.
- `evidence/H-001/export-unity.fbx`: generated Unity FBX export.

## Verification

- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-001/H-001-quaternius-animated-human.fbx --evidence-dir evidence/H-001 --summary evidence/H-001/workflow-summary.json`
  - Result: passed; workflow status `pass`.
- Command: `python3 -m pytest tests/test_asset_evidence_validator.py tests/test_generate_asset_evidence_report.py tests/test_register_asset_evidence.py tests/test_blender_asset_workflow.py -q`
  - Result: passed, `16 passed`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `56 passed`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed; schema pass, H-001 complete.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --require-production-trial`
  - Result: expected blocked; production gate still missing 10-asset coverage, score>=3 ratio, and Unity passes.
- Command: visual inspection via `view_image`
  - Result: H-001 preview is readable but weak; score 2 is intentional.

## Open Questions / Risks

- H-001 is a weak-quality result. The next product work should improve landmark placement/camera/pose evidence before claiming a quality baseline.
- Unity Editor is not installed/discoverable locally, so real Unity import remains blocked.
- Unreal Editor is not installed/discoverable locally, so real Unreal import remains blocked.
- The production trial still needs at least 9 more complete assets, including quadrupeds, tail creature, prop, low-poly humanoid, thin-limb humanoid, and wide-shoulder/bulky humanoid.

## Next Steps

1. Improve humanoid landmark generation enough to get an H-001 score >= 3, or keep H-001 as a failed baseline and move to H-002/H-003 for broader evidence.
2. Select and download the next batch: H-003 armored, H-006 low-poly, Q-001 dog-like quadruped.
3. Install/configure Unity Editor if Unity import pass evidence is required on this machine; otherwise keep Unity status blocked and do not claim production readiness.

## Resume Prompt

Continue from this checkpoint. First read `docs/checkpoints/LATEST.md` and inspect `git status --short`. H-001 is registered but score 2; production gate is still blocked. Next useful work is either improving H-001 quality to score >= 3 or adding the next real asset evidence slot.
