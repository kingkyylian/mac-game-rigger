# Project Checkpoint - 2026-06-19 00:08

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, latest commit before this checkpoint:
  `d7f66a0 data: add H-006 low-poly failure evidence`
- Context: Mac Game Rigger is still in truthful production-trial evidence work.
  The current target is to make evidence and deformation review reliable before
  adding more slots.

## Done This Session

- Investigated why H-006 previews were blank/dark despite successful workflow
  execution.
- Confirmed root cause in Blender diagnostics:
  - imported H-006 bbox height was about `5.2`;
  - after capsule weighting, evaluated mesh bbox expanded to about `528`
    height and `481` depth;
  - workflow camera was created before weighting;
  - camera `clip_end` was fixed at `1000`, while the updated camera distance
    for the deformed bbox needed to see past about `1495`.
- Added preview reframe behavior to `tools/blender_asset_workflow.py`:
  - each preview frame now recomputes current mesh bbox before rendering;
  - each frame recreates the workflow camera/light from the current bbox;
  - camera plan now includes dynamic `clipEnd`;
  - workflow summary now records `previewFrames.neutral` and
    `previewFrames.pose` with bbox and camera plan.
- Added regression coverage:
  - preview render helper reframes from current mesh bbox before render;
  - camera plan extends `clipEnd` past large deformed bboxes.
- Re-ran H-006 workflow with `--camera-axis x`.
  - `preview-neutral.png` and `preview-pose.png` are now visually readable.
  - H-006 remains low-quality, not production success.
- Re-ran H-001 workflow with `--camera-axis y`.
  - H-001 stays readable side evidence.
  - H-001 still needs orientation normalization for better front-facing review.
- Updated H-006 score from `1` to `2`.
  - Reason: preview is no longer structurally/visually unusable, but
    deformation remains unacceptable without manual rig work.
- Added PNG nonblank validation to `scripts/validate_asset_evidence.py`.
  - The validator now decodes PNG files with stdlib and flags blank previews.
  - This prevents "file exists but render is empty" from passing evidence
    validation again.
- Updated test fixtures to write real small PNG files where file validation is
  expected.

## Current State

- H-001: complete evidence, deformation score `2`, Unity/Unreal blocked.
- H-006: complete evidence, deformation score `2`, Unity/Unreal blocked.
- Production trial remains blocked.
- Current gate numbers:
  - `realAssetCount`: 2
  - `completeEvidenceCount`: 2
  - `score3PlusCount`: 0
  - `lowPolyHumanoidIncluded`: true
  - `unityImportPassesAtLeast3`: false
- The evidence pipeline is stronger now because blank preview PNGs fail
  validation, but rigging quality is still below the production threshold.

## Important Files / Artifacts

- `tools/blender_asset_workflow.py`: dynamic preview reframe and dynamic camera
  clip-end handling.
- `scripts/validate_asset_evidence.py`: PNG nonblank validation.
- `tests/test_blender_asset_workflow.py`: camera/reframe regression tests.
- `tests/test_asset_evidence_validator.py`: blank preview rejection test.
- `tests/test_generate_asset_evidence_report.py`: PNG-aware report fixtures.
- `evidence/H-006/preview-neutral.png`: now readable.
- `evidence/H-006/preview-pose.png`: now readable, still weak deformation.
- `evidence/H-006/workflow-summary.json`: records preview frame bboxes/cameras.
- `docs/asset-evidence-progress.md`: 2 complete assets, score >= 3 count is 0.

## Verification

- Command: `python3 -m pytest tests/test_blender_asset_workflow.py -q`
  - Result: passed, `7 passed`.
- Command: `python3 -m pytest tests/test_asset_evidence_validator.py -q`
  - Result: passed, `7 passed`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `63 passed`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed; blank-preview validation accepts current H-001/H-006 PNGs.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --require-production-trial`
  - Result: expected blocked; production trial is still not ready.
- Command: `git diff --check`
  - Result: passed.

## Open Questions / Risks

- Orientation normalization is now the next evidence-quality blocker. H-001 is
  readable from `y` but side-on; `x` is more front-like but appears rotated.
- Deformation quality remains unacceptable: H-001 and H-006 are both score `2`.
- Unity and Unreal import evidence remains blocked because local editors are
  not installed/discoverable.
- Adding more asset slots before fixing orientation/deformation quality would
  increase evidence count but not product readiness.

## Next Steps

1. Add automatic preview orientation selection/normalization so humanoids render
   upright and front-facing consistently.
2. Add a stronger humanoid stress pose set beyond weak arm raise, then re-score
   H-001/H-006.
3. After at least one humanoid reaches score >= 3, continue category spread:
   Q-001, Q-002, C-001, and P-001.

## Resume Prompt

Continue from this checkpoint. First run `git status --short`, then inspect
`tools/blender_asset_workflow.py` and `evidence/H-001/preview-neutral.png`.
The blank render bug is fixed; do not chase clipping again unless regression
evidence says otherwise. The next real blocker is orientation normalization and
deformation quality.
