# Project Checkpoint - 2026-06-19 00:26

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, latest commit before this checkpoint:
  `8fd8974 fix: validate readable preview evidence`
- Context: Mac Game Rigger evidence work is now focused on moving from
  readable humanoid evidence to stronger deformation quality.

## Done This Session

- Investigated the remaining orientation problem after the blank-render fix.
- Measured H-001/H-006 axis candidates with temporary `x`/`y` renders.
  - H-001 and H-006 both need `x` camera axis for front-like review.
  - H-001 source mesh was effectively Y-up before source rig stripping.
  - H-006 was already Z-up.
- Probed H-001 in Blender and confirmed that a `+90 deg` X rotation before
  rig stripping produces upright/front-facing evidence.
- Added orientation normalization to `tools/blender_asset_workflow.py`.
  - Y-up mesh dimensions now produce a normalization plan.
  - The workflow applies normalization immediately after import and before
    source rig stripping.
  - The workflow summary records `orientationNormalization`.
- Changed the workflow default preview camera axis from `y` to `x`, while
  keeping explicit `--camera-axis y` available.
- Added regression tests in `tests/test_blender_asset_workflow.py`:
  - Y-up dimensions produce a Z-up normalization plan.
  - Z-up dimensions are left unchanged.
  - default camera axis is `x`.
- Re-ran H-001 and H-006 workflows with the new default.
  - H-001 preview is now upright/front-facing.
  - H-006 preview remains upright/front-facing.
- Updated H-001/H-006 notes and the production-trial source plan.

## Current State

- H-001: complete real evidence, front/upright preview, deformation score `2`.
- H-006: complete real evidence, front/upright preview, deformation score `2`.
- Production trial remains blocked.
- Current gate numbers are unchanged:
  - `realAssetCount`: 2
  - `completeEvidenceCount`: 2
  - `score3PlusCount`: 0
  - `lowPolyHumanoidIncluded`: true
  - `unityImportPassesAtLeast3`: false
- The evidence view is now usable enough to judge humanoid deformation, but
  the actual deformation quality is still below production-trial threshold.

## Important Files / Artifacts

- `tools/blender_asset_workflow.py`: orientation normalization and `x` default
  preview axis.
- `tests/test_blender_asset_workflow.py`: orientation/default-axis regression
  tests.
- `evidence/H-001/preview-neutral.png`: H-001 front/upright neutral evidence.
- `evidence/H-001/preview-pose.png`: H-001 front/upright pose evidence.
- `evidence/H-001/workflow-summary.json`: includes Y-up normalization record.
- `evidence/H-006/preview-neutral.png`: H-006 front/upright neutral evidence.
- `evidence/H-006/workflow-summary.json`: confirms no normalization needed.
- `docs/production-trial-asset-source-plan.md`: next route now points to
  stronger stress poses and deformation quality.

## Verification

- Command: `python3 -m pytest tests/test_blender_asset_workflow.py -q`
  - Result: passed, `10 passed`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --require-production-trial`
  - Result: expected blocked; production trial is still not ready.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `66 passed`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed.

## Open Questions / Risks

- The remaining core blocker is deformation quality, not preview readability.
- The current pose test still does not stress shoulders/elbows/hips/knees
  enough to support a score >= 3 claim.
- Unity and Unreal import evidence is still blocked by missing local editors.
- Production-trial coverage still needs more real assets and categories.

## Next Steps

1. Add stronger humanoid stress poses for shoulder, elbow, hip, and knee review.
2. Re-run H-001/H-006 and only raise a score if visual evidence supports it.
3. After one humanoid reaches score >= 3, continue category spread with Q-001,
   Q-002, C-001, and P-001.

## Resume Prompt

Continue from this checkpoint. First inspect `git status --short`, then open
`evidence/H-001/preview-neutral.png` and `evidence/H-001/preview-pose.png`.
Orientation is fixed for current humanoid evidence. The next useful task is
stronger pose/deformation evaluation, not more camera work.
