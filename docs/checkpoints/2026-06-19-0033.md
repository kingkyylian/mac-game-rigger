# Project Checkpoint - 2026-06-19 00:33

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, latest commit before this checkpoint:
  `b3cf7b9 fix: normalize humanoid preview orientation`
- Context: H-001/H-006 evidence is now readable and front/upright; the active
  gap is deformation quality.

## Done This Session

- Inspected existing pose operators and found workflow evidence only used
  `pose_arm_raise`.
- Added a stronger humanoid stress pose in
  `addon/mac_game_rigger/core/pose_tests.py`.
  - It combines arm raise, lower-arm bend, upper/lower-leg bend, neck turn,
    and head turn.
  - It changes 10 bones instead of the previous 4.
- Added UI/operator support:
  - `mgr.pose_humanoid_stress`
  - panel button: `Pose Humanoid Stress`
- Updated `tools/blender_asset_workflow.py` so workflow pose previews use
  `pose_humanoid_stress`.
- Added tests:
  - `tests/test_pose_tests.py`
  - updated workflow operator-name test in `tests/test_blender_asset_workflow.py`.
- Re-ran H-001 and H-006 workflows.
  - Both summaries now record `pose_humanoid_stress`.
  - Both Blender runs reported `Humanoid stress: changed=10`.
- Updated H-001/H-006 evidence notes and production-trial route docs.

## Current State

- H-001: complete evidence, front/upright preview, stress pose evidence,
  deformation score `2`.
- H-006: complete evidence, front/upright preview, stress pose evidence,
  deformation score `2`.
- Production trial remains blocked.
- Current gate facts:
  - `realAssetCount`: 2
  - `completeEvidenceCount`: 2
  - `score3PlusCount`: 0
  - `lowPolyHumanoidIncluded`: true
  - `unityImportPassesAtLeast3`: false
- We now have better evidence to review deformation, but not better deformation.

## Important Files / Artifacts

- `addon/mac_game_rigger/core/pose_tests.py`: humanoid stress pose.
- `addon/mac_game_rigger/ui/operators.py`: `mgr.pose_humanoid_stress`.
- `addon/mac_game_rigger/ui/panels.py`: stress pose panel button.
- `tools/blender_asset_workflow.py`: uses stress pose for preview evidence.
- `tests/test_pose_tests.py`: core pose behavior test.
- `evidence/H-001/preview-pose.png`: H-001 stress-pose evidence.
- `evidence/H-006/preview-pose.png`: H-006 stress-pose evidence.
- `docs/production-trial-asset-source-plan.md`: next route now points to
  deformation quality, not pose coverage.

## Verification

- Command: `python3 -m pytest tests/test_pose_tests.py tests/test_blender_asset_workflow.py -q`
  - Result: passed, `11 passed`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --require-production-trial`
  - Result: expected blocked.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `67 passed`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed.

## Open Questions / Risks

- Deformation quality remains score `2`; no score was raised.
- Current stress pose reveals more issues but does not fix weighting.
- Engine import evidence still needs local Unity/Unreal setup.
- Production trial still needs category coverage and score >= 3 assets.

## Next Steps

1. Improve humanoid weighting/landmark placement enough to reach score >= 3 on
   H-001 or H-006.
2. Add more visual evidence views only if they directly support scoring.
3. Continue category spread after one humanoid quality baseline is defensible.

## Resume Prompt

Continue from this checkpoint. First inspect `git status --short`, then open
`evidence/H-001/preview-pose.png` and `evidence/H-006/preview-pose.png`. Stress
pose coverage is now present; the next useful task is improving deformation
quality, not adding another pose.
