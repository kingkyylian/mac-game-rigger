# Project Checkpoint - 2026-06-19 00:40

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, latest commit before this checkpoint:
  `dd5f340 feat: add humanoid stress pose evidence`
- Context: We made a narrow deformation-weighting pass after readable front
  previews and humanoid stress-pose evidence were already in place.

## Done This Session

- Added bone-specific capsule radius scaling in
  `addon/mac_game_rigger/core/weight_binding.py`.
  - Torso/default bones keep the requested capsule radius scale.
  - Arm/leg capsules are narrowed with `LIMB_CAPSULE_RADIUS_SCALE`.
  - Hand/foot capsules are narrowed further with
    `DISTAL_CAPSULE_RADIUS_SCALE`.
- Added focused coverage in `tests/test_capsule_weights.py` for the new radius
  policy.
- Re-ran H-001 and H-006 Blender asset workflows after the weighting change.
  - H-001 evidence artifacts were regenerated.
  - H-006 evidence artifacts were regenerated.
  - Both workflows still report QA clean: 0 unweighted vertices, 0 over-limit
    vertices, no warnings, no errors.
- Updated H-001/H-006 evidence notes to document the narrower limb/distal
  weighting policy.
- Regenerated `docs/asset-evidence-progress.md`; production-trial gate remains
  blocked.

## Current State

- H-001: complete evidence, front/upright preview, stress pose evidence,
  deformation score `2`.
- H-006: complete evidence, front/upright preview, stress pose evidence,
  deformation score `2`.
- The radius policy is an incremental improvement for limb/body bleed, but it
  is not enough to raise either asset to score `3`.
- Production trial remains blocked.
- Current gate facts:
  - `realAssetCount`: 2
  - `completeEvidenceCount`: 2
  - `score3PlusCount`: 0
  - `score3PlusRatio`: 0.0
  - `lowPolyHumanoidIncluded`: true
  - `unityImportPassesAtLeast3`: false

## Important Files / Artifacts

- `addon/mac_game_rigger/core/weight_binding.py`: limb/distal capsule radius
  scale policy.
- `tests/test_capsule_weights.py`: focused regression coverage for capsule
  scaling.
- `evidence/H-001/preview-pose.png`: regenerated stress-pose preview.
- `evidence/H-006/preview-pose.png`: regenerated stress-pose preview.
- `evidence/H-001/export-unity.fbx`: regenerated Unity FBX artifact.
- `evidence/H-006/export-unity.fbx`: regenerated Unity FBX artifact.
- `evidence/H-001/notes.md`: records the weighting policy.
- `evidence/H-006/notes.md`: records the weighting policy.
- `docs/asset-evidence-progress.md`: current gate report.

## Verification

- Command: `python3 -m pytest tests/test_capsule_weights.py -q`
  - Result: passed, `8 passed`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `68 passed`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --require-production-trial`
  - Result: expected blocked, exit `1`.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; alpha smoke completed, package built at
    `dist/MacGameRigger-0.1.0.zip`.

## Open Questions / Risks

- Deformation quality remains score `2`; no score was raised.
- The current approach still lacks actual landmark-aware weighting around
  shoulders, hips, knees, elbows, wrists, and ankles.
- H-006 has very large transformed preview-space dimensions; dynamic camera
  clipping now prevents blank renders, but scale normalization still deserves
  a separate pass.
- Engine import evidence is still blocked until local Unity/Unreal verification
  is available.
- Project-local `AGENTS.md` is absent; continue with `/Users/kyylian` Codex
  workflow instructions.

## Next Steps

1. Implement a real deformation-quality pass, not just capsule shrinkage:
   shoulder/hip influence separation, joint falloff tuning, and side-aware
   landmark constraints.
2. Re-run H-001/H-006 workflows and visually compare `preview-pose.png` before
   considering any score increase.
3. Once one humanoid reaches defensible score `3`, continue category spread:
   thin-limb humanoid, wide/bulky humanoid, quadrupeds, tail creatures, and
   prop/accessory slots.

## Resume Prompt

Continue from this checkpoint. First inspect `git status --short`, then open
`addon/mac_game_rigger/core/weight_binding.py`,
`tests/test_capsule_weights.py`, `evidence/H-001/preview-pose.png`, and
`evidence/H-006/preview-pose.png`. The next useful task is improving actual
deformation quality enough to reach a defensible score `3`; do not add more
evidence-only work unless it directly supports scoring.
