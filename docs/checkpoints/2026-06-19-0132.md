# Project Checkpoint - 2026-06-19 01:32

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, latest commit before this checkpoint:
  `dd5f340 feat: add humanoid stress pose evidence`
- Context: We continued the deformation-quality route with a test-first
  humanoid landmark correction after the previous limb/distal capsule radius
  pass.

## Done This Session

- Kept the existing open radius-policy change:
  - `addon/mac_game_rigger/core/weight_binding.py`
  - arm/leg capsules use a narrower radius than torso capsules;
  - hand/foot capsules are narrower than arm/leg capsules.
- Added a failing TDD test first in `tests/test_blender_asset_workflow.py`:
  `test_humanoid_landmarks_keep_arm_chain_near_shoulder_band`.
  - The test failed because bbox-generated `upper_arm`, `lower_arm`, and
    `hand` landmarks were below the chest band.
- Updated `tools/blender_asset_workflow.py` so generated humanoid arm-chain
  landmarks stay near the chest/shoulder band:
  - `upper_arm.*`: height factor `0.73`
  - `lower_arm.*`: height factor `0.72`
  - `hand.*`: height factor `0.71`
- Re-ran H-001 and H-006 Blender workflows after the landmark change.
  - H-001 and H-006 preview PNGs, workflow summaries, QA reports, and Unity
    FBX artifacts were regenerated.
  - Both workflows still report QA clean: 0 unweighted vertices, 0 over-limit
    vertices, no warnings, no errors.
- Updated H-001/H-006 notes with both policies:
  - narrower limb/distal capsules;
  - arm landmarks held near the chest/shoulder band for T-pose game assets.
- Regenerated `docs/asset-evidence-progress.md`.

## Current State

- H-001: complete evidence, front/upright preview, stress pose evidence,
  deformation score `2`.
  - Visual status: leg stress is now more readable than before; shoulder/elbow
    separation is still too weak for score `3`.
- H-006: complete evidence, front/upright preview, stress pose evidence,
  deformation score `2`.
  - Visual status: foot/lower-leg motion is more readable; shoulders/elbows
    still do not clear the production-trial quality bar.
- No deformation score was raised.
- Production trial remains blocked.
- Current gate facts:
  - `realAssetCount`: 2
  - `completeEvidenceCount`: 2
  - `score3PlusCount`: 0
  - `score3PlusRatio`: 0.0
  - `lowPolyHumanoidIncluded`: true
  - `unityImportPassesAtLeast3`: false

## Important Files / Artifacts

- `addon/mac_game_rigger/core/weight_binding.py`: limb/distal capsule radius
  scale policy.
- `tools/blender_asset_workflow.py`: bbox-generated humanoid arm landmarks now
  stay near shoulder height.
- `tests/test_capsule_weights.py`: radius policy coverage.
- `tests/test_blender_asset_workflow.py`: arm landmark height coverage.
- `evidence/H-001/preview-pose.png`: regenerated; leg stress now more visible.
- `evidence/H-006/preview-pose.png`: regenerated; lower-leg/foot motion now
  more visible.
- `evidence/H-001/workflow-summary.json`: regenerated workflow evidence.
- `evidence/H-006/workflow-summary.json`: regenerated workflow evidence.
- `docs/asset-evidence-progress.md`: current production-gate report.

## Verification

- Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_humanoid_landmarks_keep_arm_chain_near_shoulder_band -q`
  - Result: failed first as expected, then passed after implementation.
- Command: `python3 -m pytest tests/test_blender_asset_workflow.py tests/test_capsule_weights.py -q`
  - Result: passed, `19 passed`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `69 passed`.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-001/H-001-quaternius-animated-human.fbx --evidence-dir evidence/H-001 --summary evidence/H-001/workflow-summary.json`
  - Result: passed; workflow summary written.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-006/H-006-quaternius-animated-woman.fbx --evidence-dir evidence/H-006 --summary evidence/H-006/workflow-summary.json`
  - Result: passed; workflow summary written.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --require-production-trial`
  - Result: expected blocked, exit `1`.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; alpha smoke completed, package built at
    `dist/MacGameRigger-0.1.0.zip`.

## Open Questions / Risks

- Deformation quality remains score `2`; production trial is not achieved.
- The next real quality barrier is shoulder/elbow weighting and pose response,
  not evidence coverage.
- Upper-leg heads still originate from the shared `hips` landmark in the
  humanoid template; adding side-specific hip landmarks may be needed for a
  cleaner pelvis/thigh split.
- Engine import evidence is still blocked until local Unity/Unreal verification
  is available.
- Project-local `AGENTS.md` is absent; continue with `/Users/kyylian` Codex
  workflow instructions.

## Next Steps

1. Add a test-first side-specific hip/shoulder landmark route or weighting
   constraint so limbs separate from torso at the root joints.
2. Re-run H-001/H-006 workflows and visually compare `preview-pose.png`; only
   raise score if shoulder/elbow and hip/knee behavior clears the rubric.
3. After one humanoid reaches defensible score `3`, continue category spread:
   thin-limb humanoid, wide/bulky humanoid, quadrupeds, tail creatures, and
   prop/accessory slots.

## Resume Prompt

Continue from this checkpoint. First inspect `git status --short`, then open
`tools/blender_asset_workflow.py`, `addon/mac_game_rigger/core/weight_binding.py`,
`evidence/H-001/preview-pose.png`, and `evidence/H-006/preview-pose.png`.
The next useful task is side-specific limb-root separation, probably starting
with hips/shoulders; keep using TDD and do not raise deformation scores without
visual evidence.
