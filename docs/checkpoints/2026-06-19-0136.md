# Project Checkpoint - 2026-06-19 01:36

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, latest commit before this checkpoint:
  `dd5f340 feat: add humanoid stress pose evidence`
- Context: We made two deformation-quality passes after stress-pose evidence:
  narrower limb capsules, shoulder-band arm landmarks, and side-specific hip
  landmarks for upper-leg roots.

## Done This Session

- Kept and verified the limb/distal capsule radius policy in
  `addon/mac_game_rigger/core/weight_binding.py`.
  - arm/leg capsules are narrower than torso capsules;
  - hand/foot capsules are narrower than arm/leg capsules.
- Added test-first arm landmark coverage in
  `tests/test_blender_asset_workflow.py`.
  - `test_humanoid_landmarks_keep_arm_chain_near_shoulder_band` failed first,
    then passed after implementation.
  - `tools/blender_asset_workflow.py` now keeps generated `upper_arm`,
    `lower_arm`, and `hand` landmarks near the chest/shoulder band.
- Added test-first side-hip coverage.
  - `test_humanoid_landmarks_place_side_hips_near_center_pelvis_height`
    failed first because `hip.L/R` were missing.
  - `test_loads_builtin_humanoid_template_with_core_game_bones` failed first
    because the humanoid template still used shared `hips` for upper-leg roots.
- Updated `addon/mac_game_rigger/templates/humanoid.json`.
  - required landmarks now include `hip.L` and `hip.R`;
  - `UpperLeg.L` starts from `hip.L`;
  - `UpperLeg.R` starts from `hip.R`.
- Updated `tools/blender_asset_workflow.py` to generate `hip.L/R` from bbox
  width at pelvis height.
- Re-ran H-001 and H-006 Blender workflows after both landmark changes.
  - H-001/H-006 preview PNGs, workflow summaries, QA reports, and Unity FBX
    artifacts were regenerated.
  - Both workflows still report QA clean: 0 unweighted vertices, 0 over-limit
    vertices, no warnings, no errors.
- Updated H-001/H-006 notes with:
  - narrower limb/distal capsule policy;
  - shoulder-band arm landmark policy;
  - side-specific hip root policy.
- Regenerated `docs/asset-evidence-progress.md`.

## Current State

- H-001: complete evidence, front/upright preview, stress pose evidence,
  deformation score `2`.
  - Visual status: leg stress is more readable and pelvis separation is less
    crude, but the leg stress artifact and shoulder/elbow response still block
    score `3`.
- H-006: complete evidence, front/upright preview, stress pose evidence,
  deformation score `2`.
  - Visual status: foot/lower-leg motion is readable, but shoulders/elbows
    still do not clear the production-trial quality bar.
- No deformation score was raised.
- Production trial remains blocked.
- Current gate facts:
  - `realAssetCount`: 2
  - `completeEvidenceCount`: 2
  - `score3PlusCount`: 0
  - `score3PlusRatio`: 0.0
  - `lowPolyHumanoidIncluded`: true
  - `unityImportPassesAtLeast3`: false

## Important Files / Artifacts

- `addon/mac_game_rigger/core/weight_binding.py`: limb/distal capsule radius
  policy.
- `addon/mac_game_rigger/templates/humanoid.json`: side-specific hip landmarks
  and upper-leg roots.
- `tools/blender_asset_workflow.py`: bbox-generated arm landmarks now stay near
  shoulder height; bbox-generated side hips feed the template.
- `tests/test_capsule_weights.py`: radius policy coverage.
- `tests/test_blender_asset_workflow.py`: shoulder-band arm landmarks and
  side-hip landmark coverage.
- `tests/test_templates.py`: humanoid template side-hip root coverage.
- `evidence/H-001/preview-pose.png`: regenerated visual evidence.
- `evidence/H-006/preview-pose.png`: regenerated visual evidence.
- `evidence/H-001/workflow-summary.json`: regenerated workflow evidence.
- `evidence/H-006/workflow-summary.json`: regenerated workflow evidence.
- `docs/asset-evidence-progress.md`: current production-gate report.

## Verification

- Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_humanoid_landmarks_keep_arm_chain_near_shoulder_band -q`
  - Result: failed first, then passed after implementation.
- Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_humanoid_landmarks_place_side_hips_near_center_pelvis_height -q`
  - Result: failed first, then passed after implementation.
- Command: `python3 -m pytest tests/test_templates.py::test_loads_builtin_humanoid_template_with_core_game_bones -q`
  - Result: failed first, then passed after implementation.
- Command: `python3 -m pytest tests/test_blender_asset_workflow.py tests/test_templates.py tests/test_capsule_weights.py -q`
  - Result: passed, `23 passed`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `70 passed`.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-001/H-001-quaternius-animated-human.fbx --evidence-dir evidence/H-001 --summary evidence/H-001/workflow-summary.json`
  - Result: passed.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-006/H-006-quaternius-animated-woman.fbx --evidence-dir evidence/H-006 --summary evidence/H-006/workflow-summary.json`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --require-production-trial`
  - Result: expected blocked, exit `1`.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; alpha smoke completed, package built at
    `dist/MacGameRigger-0.1.0.zip`.

## Open Questions / Risks

- Deformation quality remains score `2`; production trial is not achieved.
- Shoulder/elbow response is now the most obvious remaining humanoid blocker.
- H-001 still shows a large leg stress artifact; lower-leg/foot weighting or
  stress-pose axis tuning likely needs the next pass.
- Engine import evidence is still blocked until local Unity/Unreal verification
  is available.
- Project-local `AGENTS.md` is absent; continue with `/Users/kyylian` Codex
  workflow instructions.

## Next Steps

1. TDD next: add a shoulder/elbow separation rule or metric. Practical targets:
   keep arm-root weights out of torso vertices, or add shoulder-side landmarks
   that make upper-arm roots less body-centered.
2. Re-run H-001/H-006 workflows and visually compare `preview-pose.png`; only
   raise score if shoulder/elbow and hip/knee behavior clears the rubric.
3. After one humanoid reaches defensible score `3`, continue category spread:
   thin-limb humanoid, wide/bulky humanoid, quadrupeds, tail creatures, and
   prop/accessory slots.

## Resume Prompt

Continue from this checkpoint. First inspect `git status --short`, then open
`tools/blender_asset_workflow.py`, `addon/mac_game_rigger/templates/humanoid.json`,
`addon/mac_game_rigger/core/weight_binding.py`, `evidence/H-001/preview-pose.png`,
and `evidence/H-006/preview-pose.png`. The next useful task is shoulder/elbow
separation; keep using TDD and do not raise deformation scores without visual
evidence.
