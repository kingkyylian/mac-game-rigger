# Project Checkpoint - 2026-06-19 01:42

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, latest commit before this checkpoint:
  `dd5f340 feat: add humanoid stress pose evidence`
- Context: We continued the deformation-quality route with an axis-aware
  humanoid landmark correction for depth-dominant arm spans.

## Done This Session

- Kept the open deformation-quality changes:
  - narrower limb/distal capsule radius policy in
    `addon/mac_game_rigger/core/weight_binding.py`;
  - side-specific `hip.L/R` landmarks and upper-leg roots in
    `addon/mac_game_rigger/templates/humanoid.json`;
  - shoulder-band arm landmark heights in `tools/blender_asset_workflow.py`.
- Added a failing TDD test in `tests/test_blender_asset_workflow.py`:
  `test_humanoid_landmarks_use_depth_as_lateral_axis_for_wide_arm_span`.
  - It failed first because depth-dominant assets still spread shoulders and
    hands on X.
- Updated `tools/blender_asset_workflow.py`.
  - Arm landmarks now use Y/depth as the lateral axis when depth is much larger
    than width.
  - Hip and leg landmarks still use X separation so leg/foot pose behavior does
    not drift sideways with arm span.
  - Foot/toe forward offsets remain depth-based.
- Re-ran H-001 and H-006 Blender workflows.
  - Preview PNGs, workflow summaries, QA reports, and Unity FBX artifacts were
    regenerated.
  - Both workflows report 0 unweighted vertices and 0 over-limit vertices.
- Updated H-001/H-006 notes with the axis policy.
- Regenerated `docs/asset-evidence-progress.md`.

## Current State

- H-001: complete evidence, front/upright preview, stress pose evidence,
  deformation score `2`.
  - Visual status: shoulder/arm alignment is more plausible; leg stress and
    elbow response still block score `3`.
- H-006: complete evidence, front/upright preview, stress pose evidence,
  deformation score `2`.
  - Visual status: shoulder/hand alignment is improved but still visibly crude;
    not production-trial quality.
- No deformation score was raised.
- Production trial remains blocked.
- Current gate facts:
  - `realAssetCount`: 2
  - `completeEvidenceCount`: 2
  - `score3PlusCount`: 0
  - `score3PlusRatio`: 0.0
  - `lowPolyHumanoidIncluded`: true
  - `unityImportPassesAtLeast3`: false

## Important Files / Artifacts

- `tools/blender_asset_workflow.py`: depth-aware arm lateral axis with X-based
  hip/leg separation.
- `tests/test_blender_asset_workflow.py`: new axis-selection coverage plus
  prior shoulder-band and side-hip coverage.
- `addon/mac_game_rigger/templates/humanoid.json`: side-specific hip landmarks.
- `addon/mac_game_rigger/core/weight_binding.py`: limb/distal capsule policy.
- `evidence/H-001/preview-pose.png`: regenerated visual evidence.
- `evidence/H-006/preview-pose.png`: regenerated visual evidence.
- `docs/asset-evidence-progress.md`: current production-gate report.

## Verification

- Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_humanoid_landmarks_use_depth_as_lateral_axis_for_wide_arm_span -q`
  - Result: failed first, then passed after implementation.
- Command: `python3 -m pytest tests/test_blender_asset_workflow.py tests/test_templates.py tests/test_capsule_weights.py -q`
  - Result: passed, `24 passed`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `71 passed`.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-001/H-001-quaternius-animated-human.fbx --evidence-dir evidence/H-001 --summary evidence/H-001/workflow-summary.json`
  - Result: passed.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-006/H-006-quaternius-animated-woman.fbx --evidence-dir evidence/H-006 --summary evidence/H-006/workflow-summary.json`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --require-production-trial`
  - Result: expected blocked, exit `1`.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; alpha smoke completed, package built at
    `dist/MacGameRigger-0.1.0.zip`.

## Open Questions / Risks

- Deformation quality remains score `2`; production trial is not achieved.
- Empty vertex group removal increased after axis-aware landmarks
  (`H-001`: 13, `H-006`: 14), so some generated bones still receive no useful
  mesh influence.
- The next real blocker is elbow/lower-arm and leg stress behavior, not more
  evidence coverage.
- Engine import evidence remains blocked until local Unity/Unreal verification
  is available.
- Project-local `AGENTS.md` is absent; continue with `/Users/kyylian` Codex
  workflow instructions.

## Next Steps

1. TDD next: add a metric or rule for empty/unused deform groups so workflow
   regressions like arm/leg bones receiving no useful influence are caught.
2. Then tune elbow/lower-arm and lower-leg weighting/pose response against
   H-001/H-006 previews.
3. Only raise a deformation score after visual evidence clears the rubric; then
   continue category spread toward the production-trial gate.

## Resume Prompt

Continue from this checkpoint. First inspect `git status --short`, then open
`tools/blender_asset_workflow.py`, `tests/test_blender_asset_workflow.py`,
`evidence/H-001/preview-pose.png`, and `evidence/H-006/preview-pose.png`.
The next useful task is catching/reducing unused deform groups and improving
elbow/lower-arm or leg stress behavior; keep using TDD and do not raise scores
without visual evidence.
