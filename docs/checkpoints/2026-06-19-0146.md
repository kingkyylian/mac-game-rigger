# Project Checkpoint - 2026-06-19 01:46

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, latest commit before this checkpoint:
  `dd5f340 feat: add humanoid stress pose evidence`
- Context: We added structured cleanup metrics so unused/empty deform groups
  are visible in workflow evidence instead of being hidden in operator logs.

## Done This Session

- Kept the open deformation-quality changes:
  - narrower limb/distal capsule radius policy;
  - side-specific `hip.L/R` humanoid landmarks and upper-leg roots;
  - shoulder-band arm landmark heights;
  - depth-aware arm lateral axis for depth-dominant humanoid assets.
- Added a failing TDD test in `tests/test_blender_asset_workflow.py`:
  `test_cleanup_summary_from_scene_parses_removed_empty_groups`.
  - It failed first because `cleanup_summary_from_scene` did not exist.
- Added `cleanup_summary_from_scene(scene)` in `tools/blender_asset_workflow.py`.
  - Parses the existing `mgr_weight_cleanup_message`.
  - Produces structured fields:
    - `unweightedVertices`
    - `overLimitVertices`
    - `removedEmptyGroups`
    - `prunedWeights`
    - `normalizedVertices`
- Added `cleanup` to each workflow summary JSON.
- Re-ran H-001 and H-006 Blender workflows.
  - H-001 summary now records `cleanup.removedEmptyGroups: 13`.
  - H-006 summary now records `cleanup.removedEmptyGroups: 14`.
  - Both still report QA clean: 0 unweighted vertices, 0 over-limit vertices.
- Updated H-001/H-006 notes to call out the empty deform group counts as a
  quality risk.
- Regenerated `docs/asset-evidence-progress.md`.

## Current State

- H-001: complete evidence, front/upright preview, stress pose evidence,
  deformation score `2`.
  - Visual status: shoulder/arm alignment is more plausible than earlier, but
    leg stress and elbow response still block score `3`.
  - Cleanup metric: `13` empty deform groups removed.
- H-006: complete evidence, front/upright preview, stress pose evidence,
  deformation score `2`.
  - Visual status: shoulder/hand alignment improved but remains visibly crude.
  - Cleanup metric: `14` empty deform groups removed.
- No deformation score was raised.
- Production trial remains blocked.
- Current gate facts:
  - `realAssetCount`: 2
  - `completeEvidenceCount`: 2
  - `score3PlusCount`: 0
  - `score3PlusRatio`: 0.0
  - `lowPolyHumanoidIncluded`: true
  - `unityImportPassesAtLeast3`: false

## Important Files / Artifacts

- `tools/blender_asset_workflow.py`: structured cleanup metrics in workflow
  summary.
- `tests/test_blender_asset_workflow.py`: cleanup summary parsing coverage,
  depth-aware arm lateral axis coverage, shoulder-band arm coverage, side-hip
  coverage.
- `evidence/H-001/workflow-summary.json`: now includes `cleanup`.
- `evidence/H-006/workflow-summary.json`: now includes `cleanup`.
- `evidence/H-001/notes.md`: documents the empty deform group risk.
- `evidence/H-006/notes.md`: documents the empty deform group risk.
- `docs/asset-evidence-progress.md`: current production-gate report.

## Verification

- Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_cleanup_summary_from_scene_parses_removed_empty_groups -q`
  - Result: failed first, then passed after implementation.
- Command: `python3 -m pytest tests/test_blender_asset_workflow.py tests/test_templates.py tests/test_capsule_weights.py -q`
  - Result: passed, `25 passed`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `72 passed`.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-001/H-001-quaternius-animated-human.fbx --evidence-dir evidence/H-001 --summary evidence/H-001/workflow-summary.json`
  - Result: passed.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-006/H-006-quaternius-animated-woman.fbx --evidence-dir evidence/H-006 --summary evidence/H-006/workflow-summary.json`
  - Result: passed.
- Command: `rg -n '"cleanup"|"removedEmptyGroups"|"qa"' evidence/H-001/workflow-summary.json evidence/H-006/workflow-summary.json`
  - Result: confirmed H-001 `13`, H-006 `14`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --require-production-trial`
  - Result: expected blocked, exit `1`.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; alpha smoke completed, package built at
    `dist/MacGameRigger-0.1.0.zip`.

## Open Questions / Risks

- Deformation quality remains score `2`; production trial is not achieved.
- Empty deform group counts are now visible, but still high:
  - H-001: `13`
  - H-006: `14`
- This likely means several generated bones are too far from the mesh or
  capsule radii are too narrow after the recent landmark changes.
- Engine import evidence remains blocked until local Unity/Unreal verification
  is available.
- Project-local `AGENTS.md` is absent; continue with `/Users/kyylian` Codex
  workflow instructions.

## Next Steps

1. TDD next: expose or preserve the names of removed empty deform groups so we
   know exactly which bones are unused on H-001/H-006.
2. Tune landmark placement or capsule fallback for those named bones, then
   rerun H-001/H-006 workflows.
3. Only raise deformation scores after visual evidence and cleanup metrics both
   support the quality claim.

## Resume Prompt

Continue from this checkpoint. First inspect `git status --short`, then open
`tools/blender_asset_workflow.py`, `addon/mac_game_rigger/core/weight_cleanup.py`,
`evidence/H-001/workflow-summary.json`, and `evidence/H-006/workflow-summary.json`.
The next useful task is recording names of removed empty deform groups, then
using those names to reduce unused limb/hand/foot bones; keep using TDD.
