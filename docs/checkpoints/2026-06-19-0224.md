# Project Checkpoint - 2026-06-19 02:24

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, latest commit before this checkpoint:
  `dd5f340 feat: add humanoid stress pose evidence`
- Context: Empty deform group counts now include exact removed group names,
  turning a vague quality risk into a concrete tuning target.

## Done This Session

- Added TDD coverage in `tests/test_weight_cleanup.py`.
  - `find_empty_vertex_group_names` failed first because it did not exist.
  - `cleanup_mesh_weights` failed first because the result did not preserve
    removed empty group names.
- Added `find_empty_vertex_group_names(mesh)` to
  `addon/mac_game_rigger/core/weight_cleanup.py`.
- Extended `WeightCleanupResult` with `removed_empty_group_names`.
- Updated `remove_empty_vertex_groups` and `cleanup_mesh_weights` so cleanup
  removes empty groups and returns their names.
- Updated `addon/mac_game_rigger/ui/operators.py`.
  - `mgr.cleanup_weights` now stores removed empty group names on the scene.
  - Added `mgr_removed_empty_group_names` scene string property.
- Updated `tools/blender_asset_workflow.py`.
  - `cleanup_summary_from_scene` now emits `removedEmptyGroupNames`.
  - Workflow summaries now include both count and names.
- Re-ran H-001 and H-006 Blender workflows.
  - H-001 cleanup now records 13 removed empty groups by name.
  - H-006 cleanup now records 14 removed empty groups by name.
- Updated H-001/H-006 notes with the removed group lists.
- Regenerated `docs/asset-evidence-progress.md`.

## Current State

- H-001: complete evidence, deformation score `2`.
  - Removed empty groups:
    `Hips`, `Spine`, `Chest`, `Head`, `UpperArm.R`, `LowerArm.R`, `Hand.R`,
    `UpperLeg.L`, `LowerLeg.L`, `Foot.L`, `UpperLeg.R`, `LowerLeg.R`,
    `Foot.R`.
- H-006: complete evidence, deformation score `2`.
  - Removed empty groups:
    `Hips`, `Spine`, `Chest`, `Head`, `UpperArm.L`, `LowerArm.L`,
    `UpperArm.R`, `LowerArm.R`, `UpperLeg.L`, `LowerLeg.L`, `Foot.L`,
    `UpperLeg.R`, `LowerLeg.R`, `Foot.R`.
- No deformation score was raised.
- Production trial remains blocked.
- Current gate facts:
  - `realAssetCount`: 2
  - `completeEvidenceCount`: 2
  - `score3PlusCount`: 0
  - `score3PlusRatio`: 0.0
  - `lowPolyHumanoidIncluded`: true
  - `unityImportPassesAtLeast3`: false

## Important Files / Artifacts

- `addon/mac_game_rigger/core/weight_cleanup.py`: finds and preserves empty
  group names.
- `addon/mac_game_rigger/ui/operators.py`: stores empty group names on scene.
- `tools/blender_asset_workflow.py`: writes `removedEmptyGroupNames` to
  workflow summary.
- `tests/test_weight_cleanup.py`: core cleanup name coverage.
- `tests/test_blender_asset_workflow.py`: cleanup summary name coverage.
- `evidence/H-001/workflow-summary.json`: exact H-001 removed groups.
- `evidence/H-006/workflow-summary.json`: exact H-006 removed groups.
- `evidence/H-001/notes.md`: exact group names documented.
- `evidence/H-006/notes.md`: exact group names documented.

## Verification

- Command: `python3 -m pytest tests/test_weight_cleanup.py -q`
  - Result: failed first, then passed after implementation.
- Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_cleanup_summary_from_scene_parses_removed_empty_groups -q`
  - Result: failed first, then passed after implementation.
- Command: `python3 -m pytest tests/test_weight_cleanup.py tests/test_blender_asset_workflow.py -q`
  - Result: passed, `16 passed`.
- Command: `python3 -m pytest tests/test_weight_cleanup.py tests/test_blender_asset_workflow.py tests/test_templates.py tests/test_capsule_weights.py -q`
  - Result: passed, `27 passed`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `74 passed`.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-001/H-001-quaternius-animated-human.fbx --evidence-dir evidence/H-001 --summary evidence/H-001/workflow-summary.json`
  - Result: passed.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-006/H-006-quaternius-animated-woman.fbx --evidence-dir evidence/H-006 --summary evidence/H-006/workflow-summary.json`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --require-production-trial`
  - Result: expected blocked, exit `1`.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; alpha smoke completed, package built at
    `dist/MacGameRigger-0.1.0.zip`.

## Open Questions / Risks

- Deformation quality remains score `2`; production trial is not achieved.
- Core torso groups (`Hips`, `Spine`, `Chest`, `Head`) being empty is a serious
  signal: current capsule placement/radius policy is not feeding central bones.
- Many limb groups are empty too, so the next fix should not be cosmetic; it
  needs to change binding behavior enough to keep expected deform bones active.
- Engine import evidence remains blocked until local Unity/Unreal verification
  is available.
- Project-local `AGENTS.md` is absent; continue with `/Users/kyylian` Codex
  workflow instructions.

## Next Steps

1. TDD next: require torso/core groups to keep positive influence after cleanup
   on a representative synthetic mesh or workflow summary.
2. Tune capsule radius/weight fallback so `Hips`, `Spine`, `Chest`, and limb
   groups are not dropped on H-001/H-006.
3. Re-run workflows and only consider score changes after both visual evidence
   and cleanup names improve.

## Resume Prompt

Continue from this checkpoint. First inspect `git status --short`, then open
`addon/mac_game_rigger/core/weight_cleanup.py`,
`addon/mac_game_rigger/core/weight_binding.py`,
`evidence/H-001/workflow-summary.json`, and
`evidence/H-006/workflow-summary.json`. The next useful task is reducing the
named removed empty groups, starting with core torso groups; keep using TDD.
