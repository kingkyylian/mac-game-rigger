# Project Checkpoint - 2026-06-19 02:33

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, latest commit before this checkpoint:
  `dd5f340 feat: add humanoid stress pose evidence`
- Context: Mac-first automatic game rigger prototype has measurable evidence
  output, but deformation quality is still below production-trial threshold.

## Done This Session

- Continued the no-CUDA Mac rigger execution path instead of only planning.
- Added side-specific humanoid hip landmarks:
  - `hip.L`
  - `hip.R`
  - `UpperLeg.L` now starts from `hip.L`
  - `UpperLeg.R` now starts from `hip.R`
- Tuned generated humanoid landmarks in `tools/blender_asset_workflow.py`.
  - Arm chain is kept nearer the shoulder/chest band.
  - Very deep meshes can use depth as the arm lateral axis.
  - Leg landmarks keep normal left/right separation.
- Added narrower capsule behavior for limbs/distal bones and wider/higher-priority
  behavior for core body capsules.
  - Important result: this is test-covered, but current H-001/H-006 evidence
    shows it did not reduce the empty deform group list.
- Added cleanup diagnostics for removed empty vertex groups.
  - Cleanup now reports exact removed group names, not only a count.
  - Workflow summaries now include `removedEmptyGroupNames`.
  - The Blender UI operator stores removed empty group names on the scene.
- Re-ran H-001 and H-006 evidence workflows.
  - Evidence files, pose previews, FBX exports, and workflow summaries were
    regenerated.
- Regenerated `docs/asset-evidence-progress.md`.

## Current State

- H-001 evidence is complete, deformation score remains `2`.
  - Cleanup still removes 13 empty groups:
    `Hips`, `Spine`, `Chest`, `Head`, `UpperArm.R`, `LowerArm.R`, `Hand.R`,
    `UpperLeg.L`, `LowerLeg.L`, `Foot.L`, `UpperLeg.R`, `LowerLeg.R`,
    `Foot.R`.
- H-006 evidence is complete, deformation score remains `2`.
  - Cleanup still removes 14 empty groups:
    `Hips`, `Spine`, `Chest`, `Head`, `UpperArm.L`, `LowerArm.L`,
    `UpperArm.R`, `LowerArm.R`, `UpperLeg.L`, `LowerLeg.L`, `Foot.L`,
    `UpperLeg.R`, `LowerLeg.R`, `Foot.R`.
- Production trial gate remains blocked.
  - `realAssetCount`: 2
  - `completeEvidenceCount`: 2
  - `score3PlusCount`: 0
  - `score3PlusRatio`: 0.0
  - `lowPolyHumanoidIncluded`: true
  - `unityImportPassesAtLeast3`: false
- This is not yet a standalone production tool. It is now a measurable alpha
  harness with clear failure signals.

## Important Files / Artifacts

- `addon/mac_game_rigger/core/weight_binding.py`: capsule radius scaling and
  core weight bias experiment.
- `addon/mac_game_rigger/core/weight_cleanup.py`: reports exact removed empty
  vertex group names.
- `addon/mac_game_rigger/templates/humanoid.json`: side hip landmarks and
  upper-leg root changes.
- `addon/mac_game_rigger/ui/operators.py`: exposes cleanup name diagnostics to
  scene state.
- `tools/blender_asset_workflow.py`: generated landmark policy and workflow
  cleanup summary.
- `tests/test_capsule_weights.py`: weight-binding policy coverage.
- `tests/test_weight_cleanup.py`: cleanup name reporting coverage.
- `tests/test_blender_asset_workflow.py`: landmark and cleanup summary coverage.
- `tests/test_templates.py`: humanoid template landmark coverage.
- `evidence/H-001/workflow-summary.json`: current H-001 evidence and empty
  group names.
- `evidence/H-006/workflow-summary.json`: current H-006 evidence and empty
  group names.
- `docs/asset-evidence-progress.md`: regenerated evidence progress report.
- `dist/MacGameRigger-0.1.0.zip`: current alpha package from smoke run.

## Verification

- Command: `python3 -m pytest tests -q`
  - Result: passed, `77 passed in 9.99s`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed. Schema and evidence file checks are clean.
- Command: `scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --output docs/asset-evidence-progress.md`
  - Result: passed and report regenerated.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --require-production-trial`
  - Result: expected blocked, exit `1`.
  - Missing: 10 complete real assets, 3 humanoids, 2 quadrupeds, thin-limb
    humanoid, bulky/wide-shoulder asset, tail creature, prop/accessory,
    score >=3 on at least 70%, and at least 3 Unity import passes.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed. Unit tests passed, compileall passed, add-on package built.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk git status --short`
  - Result: worktree has uncommitted implementation, evidence, test, report, and
    checkpoint changes.

## Open Questions / Risks

- The current core capsule widening/priority experiment is not enough. It is
  useful as a documented negative result, but not a quality fix yet.
- Empty core groups (`Hips`, `Spine`, `Chest`, `Head`) are the strongest current
  blocker. A usable game rig cannot ship with those groups removed.
- Limb groups are also being removed, especially legs and one or both arm sides.
  The next fix needs deeper binding behavior, not cosmetic seeding.
- Unity/Unreal import evidence is still blocked locally. Scores should not be
  raised until engine import and visual evidence improve.
- No commit was created for this checkpoint.

## Next Steps

1. TDD a binding-level regression that requires representative torso vertices
   to keep positive `Hips`, `Spine`, and `Chest` influence after cleanup.
2. Replace or augment nearest-capsule weighting with a torso/limb band fallback
   that assigns expected bones by vertical body region and side, then re-run
   H-001/H-006 workflows.
3. Use the exact `removedEmptyGroupNames` lists as the quality metric. The next
   meaningful milestone is reducing core empty groups before touching score.

## Resume Prompt

Continue from this checkpoint. First read this file, inspect
`rtk git status --short`, then open `addon/mac_game_rigger/core/weight_binding.py`,
`addon/mac_game_rigger/core/weight_cleanup.py`,
`tools/blender_asset_workflow.py`, `evidence/H-001/workflow-summary.json`, and
`evidence/H-006/workflow-summary.json`. Start with a TDD fix for empty core
deform groups; do not raise deformation scores until evidence actually improves.
