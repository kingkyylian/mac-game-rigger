# Project Checkpoint - 2026-06-19 04:04

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, latest commit before this checkpoint:
  `dd5f340 feat: add humanoid stress pose evidence`
- Context: Empty deform group blocker was reduced from a hard failure signal to
  zero removed groups on both current humanoid evidence assets.

## Done This Session

- Continued from the 02:33 checkpoint, focusing on the real quality blocker:
  empty deform groups after capsule weighting.
- Added TDD coverage in `tests/test_capsule_weights.py`.
  - Core torso candidates should not be pushed out of the max-4 influence set
    when they are positive and stronger than non-core candidates.
  - Minimum bone coverage should add nearest real mesh vertices for an empty
    bone.
  - Crowded minimum coverage should spread fallback weights across vertices
    instead of repeatedly overwriting earlier fallback influences.
- Updated `addon/mac_game_rigger/core/weight_binding.py`.
  - Added selected core candidate preservation.
  - Added minimum per-bone coverage fallback.
  - Coverage fallback uses nearest actual mesh vertices, respects max influence
    count, and normalizes weights.
  - Added crowded fallback protection so later bones do not evict earlier
    fallback bones from the same vertex.
- Re-ran Blender workflows for:
  - `local_assets/H-001/H-001-quaternius-animated-human.fbx`
  - `local_assets/H-006/H-006-quaternius-animated-woman.fbx`
- Updated H-001/H-006 notes to reflect the new cleanup metric.
- Regenerated `docs/asset-evidence-progress.md`.

## Current State

- H-001 evidence is complete, deformation score remains `2`.
  - Previous empty groups: 13.
  - Current empty groups: 0.
  - `removedEmptyGroupNames`: `[]`.
- H-006 evidence is complete, deformation score remains `2`.
  - Previous empty groups: 14, then 6 after the first fallback attempt.
  - Current empty groups: 0.
  - `removedEmptyGroupNames`: `[]`.
- QA is clean for both current evidence assets:
  - `unweightedVertices`: 0
  - `overLimitVertices`: 0
  - `warnings`: none
  - `errors`: none
- Production trial remains blocked because coverage and engine evidence are not
  sufficient yet.
  - `realAssetCount`: 2
  - `completeEvidenceCount`: 2
  - `score3PlusCount`: 0
  - `score3PlusRatio`: 0.0
  - `unityImportPassesAtLeast3`: false

## Important Files / Artifacts

- `addon/mac_game_rigger/core/weight_binding.py`: core preservation and minimum
  coverage fallback.
- `tests/test_capsule_weights.py`: new regression coverage for core candidate
  preservation and fallback distribution.
- `evidence/H-001/workflow-summary.json`: now reports zero removed empty groups.
- `evidence/H-006/workflow-summary.json`: now reports zero removed empty groups.
- `evidence/H-001/notes.md`: updated cleanup/fallback notes.
- `evidence/H-006/notes.md`: updated cleanup/fallback notes.
- `docs/asset-evidence-progress.md`: regenerated evidence progress report.
- `dist/MacGameRigger-0.1.0.zip`: current alpha package from smoke run.

## Verification

- Command: `python3 -m pytest tests/test_capsule_weights.py::test_top_capsule_weights_preserves_core_body_candidates_for_torso_point -q`
  - Result: failed first, then passed after candidate selection fix.
- Command: `python3 -m pytest tests/test_capsule_weights.py::test_minimum_bone_coverage_adds_nearest_vertices_for_empty_bone -q`
  - Result: failed first, then passed after coverage fallback implementation.
- Command: `python3 -m pytest tests/test_capsule_weights.py::test_minimum_bone_coverage_spreads_crowded_empty_bones_across_vertices -q`
  - Result: failed first, then passed after fallback distribution/protection fix.
- Command: `python3 -m pytest tests/test_capsule_weights.py -q`
  - Result: passed, `14 passed`.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-001/H-001-quaternius-animated-human.fbx --evidence-dir evidence/H-001 --summary evidence/H-001/workflow-summary.json`
  - Result: passed, `removed_empty=0`.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-006/H-006-quaternius-animated-woman.fbx --evidence-dir evidence/H-006 --summary evidence/H-006/workflow-summary.json`
  - Result: passed, `removed_empty=0`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `80 passed in 10.09s`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed.
- Command: `scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --output docs/asset-evidence-progress.md`
  - Result: passed and report regenerated.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --require-production-trial`
  - Result: expected blocked, exit `1`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; alpha package rebuilt.
- Command: `rtk git diff --check`
  - Result: passed.

## Open Questions / Risks

- Deformation scores are intentionally still `2`; empty groups are fixed, but
  pose deformation quality has not been proven score-3+.
- Unity and Unreal import verification are still blocked locally.
- Production trial still requires broader asset coverage:
  - at least 10 complete real assets;
  - at least 3 humanoids;
  - at least 2 quadrupeds;
  - thin-limb, bulky/wide-shoulder, tail creature, and prop/accessory coverage;
  - score >=3 on at least 70%;
  - at least 3 Unity import passes.
- No commit was created for this checkpoint.

## Next Steps

1. Add a quantitative deformation/pose QA signal beyond empty group cleanup:
   joint movement bounds, mesh explosion detection, and per-limb stress pose
   sanity checks.
2. Add the next real asset slot, preferably H-002 or a thin-limb humanoid, and
   run the same workflow to see whether the fallback generalizes.
3. Set up or locate Unity verification so at least one exported FBX can move
   from `unityImportStatus=blocked` to a real pass/fail result.

## Resume Prompt

Continue from this checkpoint. First read this file, inspect
`rtk git status --short`, then open `addon/mac_game_rigger/core/weight_binding.py`,
`tests/test_capsule_weights.py`, `evidence/H-001/workflow-summary.json`, and
`evidence/H-006/workflow-summary.json`. The empty deform group blocker is now
cleared on both evidence assets; next useful work is deformation-quality QA or
adding the next real asset slot.
