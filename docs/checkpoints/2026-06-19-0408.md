# Project Checkpoint - 2026-06-19 04:08

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, latest commit before this checkpoint:
  `dd5f340 feat: add humanoid stress pose evidence`
- Context: Empty deform groups are now clean, and pose deformation quality has a
  quantitative fail/pass signal in evidence summaries.

## Done This Session

- Added TDD coverage in `tests/test_blender_asset_workflow.py`.
  - Extreme stress-pose bbox expansion is flagged as `fail`.
  - Moderate stress-pose expansion is accepted as `pass`.
- Added `pose_deformation_summary(neutral_bbox, pose_bbox)` to
  `tools/blender_asset_workflow.py`.
  - Computes per-axis expansion ratios.
  - Emits `maxAxisExpansionRatio`.
  - Emits `expandedAxes` for >4x expansion.
  - Emits `warningAxes` for >2.5x and <=4x expansion.
  - Emits `status`: `pass`, `warn`, or `fail`.
- Integrated the metric into workflow summaries as `poseDeformation`.
- Re-ran H-001 and H-006 Blender evidence workflows.
- Updated H-001/H-006 notes with the new pose deformation metric.
- Regenerated `docs/asset-evidence-progress.md`.

## Current State

- H-001 evidence:
  - `removedEmptyGroups`: 0
  - `poseDeformation.status`: `fail`
  - `maxAxisExpansionRatio`: 4.6069
  - `expandedAxes`: `["x"]`
  - deformation score remains `2`
- H-006 evidence:
  - `removedEmptyGroups`: 0
  - `poseDeformation.status`: `fail`
  - `maxAxisExpansionRatio`: 4.8767
  - `expandedAxes`: `["x"]`
  - deformation score remains `2`
- This is useful progress but not a quality pass. The product now has a clearer
  reason not to raise the scores: stress-pose expansion is still too high.
- Production trial remains blocked.
  - `realAssetCount`: 2
  - `completeEvidenceCount`: 2
  - `score3PlusCount`: 0
  - `score3PlusRatio`: 0.0
  - `unityImportPassesAtLeast3`: false

## Important Files / Artifacts

- `tools/blender_asset_workflow.py`: new `poseDeformation` summary metric.
- `tests/test_blender_asset_workflow.py`: TDD coverage for pose deformation
  fail/pass classification.
- `evidence/H-001/workflow-summary.json`: now includes `poseDeformation`.
- `evidence/H-006/workflow-summary.json`: now includes `poseDeformation`.
- `evidence/H-001/notes.md`: documents H-001 pose deformation failure.
- `evidence/H-006/notes.md`: documents H-006 pose deformation failure.
- `docs/asset-evidence-progress.md`: regenerated evidence progress report.
- `dist/MacGameRigger-0.1.0.zip`: current alpha package from smoke run.

## Verification

- Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_pose_deformation_summary_flags_extreme_bbox_expansion tests/test_blender_asset_workflow.py::test_pose_deformation_summary_passes_moderate_stress_pose_expansion -q`
  - Result: failed first because `pose_deformation_summary` did not exist, then
    passed after implementation.
- Command: `python3 -m pytest tests/test_blender_asset_workflow.py -q`
  - Result: passed, `16 passed`.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-001/H-001-quaternius-animated-human.fbx --evidence-dir evidence/H-001 --summary evidence/H-001/workflow-summary.json`
  - Result: passed. H-001 summary now reports `poseDeformation.status=fail`.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-006/H-006-quaternius-animated-woman.fbx --evidence-dir evidence/H-006 --summary evidence/H-006/workflow-summary.json`
  - Result: passed. H-006 summary now reports `poseDeformation.status=fail`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `82 passed in 9.78s`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed.
- Command: `scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --output docs/asset-evidence-progress.md`
  - Result: passed and report regenerated.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --require-production-trial`
  - Result: expected blocked, exit `1`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; alpha package rebuilt.
- Command: `rtk git diff --check`
  - Result: passed.

## Open Questions / Risks

- Stress-pose X expansion is still above the fail threshold on both current
  humanoid assets. This likely reflects exaggerated arm/shoulder pose spread or
  deformation that still needs tuning.
- Deformation score stays at `2`; do not raise it until `poseDeformation` is at
  least `pass` or a manually reviewed exception is justified.
- Unity and Unreal import verification are still blocked locally.
- Broader production trial coverage remains missing: more real assets,
  quadrupeds, tail creature, prop/accessory, score >=3 ratio, and Unity passes.
- No commit was created for this checkpoint.

## Next Steps

1. Tune the humanoid stress pose or arm/shoulder binding so H-001/H-006
   `poseDeformation.status` moves from `fail` toward `pass` without hiding real
   deformation problems.
2. Add pose QA to the asset evidence progress report so fail/warn/pass is
   visible across slots, not only inside workflow summaries.
3. Add another real asset slot after the metric is visible in the report, then
   compare whether the same pose expansion failure generalizes.

## Resume Prompt

Continue from this checkpoint. First read this file, inspect
`rtk git status --short`, then open `tools/blender_asset_workflow.py`,
`tests/test_blender_asset_workflow.py`, `evidence/H-001/workflow-summary.json`,
and `evidence/H-006/workflow-summary.json`. The next useful task is either
making `poseDeformation` visible in `docs/asset-evidence-progress.md` or tuning
pose/binding so the current `fail` metric improves.
