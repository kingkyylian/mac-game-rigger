# Project Checkpoint - 2026-06-19 04:12

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, latest commit before this checkpoint:
  `dd5f340 feat: add humanoid stress pose evidence`
- Context: Pose deformation QA is now visible in the main evidence progress
  report, not only inside individual workflow summary JSON files.

## Done This Session

- Added TDD coverage in `tests/test_generate_asset_evidence_report.py`.
  - The report must include a `Pose QA` column.
  - A slot with `evidence/<slot>/workflow-summary.json` and
    `poseDeformation` must render status, max expansion ratio, and axes.
- Updated `scripts/generate_asset_evidence_report.py`.
  - Loads optional `evidence/<slot>/workflow-summary.json` per slot.
  - Extracts `poseDeformation.status`.
  - Extracts `poseDeformation.maxAxisExpansionRatio`.
  - Shows `expandedAxes` or `warningAxes`.
  - Renders the slot table with a `Pose QA` column.
- Regenerated `docs/asset-evidence-progress.md`.

## Current State

- Main report now shows pose deformation quality directly:
  - H-001: `fail 4.6069x x`
  - H-006: `fail 4.8767x x`
- H-001/H-006 still have clean weight cleanup:
  - `removedEmptyGroups`: 0
  - `removedEmptyGroupNames`: `[]`
- Deformation scores remain `2`.
- Production trial remains blocked.
  - `realAssetCount`: 2
  - `completeEvidenceCount`: 2
  - `score3PlusCount`: 0
  - `score3PlusRatio`: 0.0
  - `unityImportPassesAtLeast3`: false

## Important Files / Artifacts

- `scripts/generate_asset_evidence_report.py`: loads pose QA data from workflow
  summaries and renders it in the slot table.
- `tests/test_generate_asset_evidence_report.py`: regression coverage for the
  new `Pose QA` report column.
- `docs/asset-evidence-progress.md`: regenerated report showing H-001/H-006
  pose deformation failures.
- `evidence/H-001/workflow-summary.json`: source pose QA for H-001.
- `evidence/H-006/workflow-summary.json`: source pose QA for H-006.

## Verification

- Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py::test_generate_asset_evidence_report_shows_pose_deformation_summary -q`
  - Result: failed first because the report did not have `Pose QA`, then passed
    after implementation.
- Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py -q`
  - Result: passed, `4 passed`.
- Command: `scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --output docs/asset-evidence-progress.md`
  - Result: passed and regenerated the report.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `83 passed in 10.24s`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --require-production-trial`
  - Result: expected blocked, exit `1`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; alpha package rebuilt.
- Command: `rtk git diff --check`
  - Result: passed.

## Open Questions / Risks

- Pose deformation still fails on both current humanoid assets. The failure is
  now visible in the top-level evidence report.
- This report is still informational; production gate logic does not yet enforce
  `poseDeformation.status`.
- Unity and Unreal import verification are still blocked locally.
- No commit was created for this checkpoint.

## Next Steps

1. Decide whether `poseDeformation.status=fail` should become a formal
   validator issue or production gate requirement for score >=3 assets.
2. Tune stress pose or arm/shoulder binding so H-001/H-006 pose expansion moves
   below the fail threshold.
3. Add the next real asset slot only after keeping this Pose QA column visible,
   so new assets can be compared against H-001/H-006 immediately.

## Resume Prompt

Continue from this checkpoint. First read this file, inspect
`rtk git status --short`, then open `scripts/generate_asset_evidence_report.py`,
`docs/asset-evidence-progress.md`, `evidence/H-001/workflow-summary.json`, and
`evidence/H-006/workflow-summary.json`. The next useful task is either making
pose QA enforceable in the validator/gate or reducing the current pose
deformation failures.
