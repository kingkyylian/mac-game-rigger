# Project Checkpoint - 2026-06-19 04:16

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, latest commit before this checkpoint:
  `dd5f340 feat: add humanoid stress pose evidence`
- Context: Pose deformation QA is now enforceable for score >=3 evidence, not
  only visible in reports.

## Done This Session

- Added TDD coverage in `tests/test_asset_evidence_validator.py`.
  - Score >=3 evidence with `poseDeformation.status=fail` must become
    incomplete.
  - Score 2 evidence may keep a failed pose QA summary as low-quality evidence.
- Updated `scripts/validate_asset_evidence.py`.
  - For score >=3, validator requires
    `evidence/<slot>/workflow-summary.json`.
  - For score >=3, workflow summary must include `poseDeformation`.
  - For score >=3, `poseDeformation.status=fail` is rejected.
  - `pass` and `warn` remain acceptable for score >=3.
- Updated report test fixtures to respect the stricter validator rule.
- Regenerated `docs/asset-evidence-progress.md`.

## Current State

- H-001/H-006 remain complete evidence because their deformation score is `2`.
- Their pose QA failures remain visible in the main report:
  - H-001: `fail 4.6069x x`
  - H-006: `fail 4.8767x x`
- A future asset cannot claim score >=3 while pose QA is missing or failed.
- Production trial remains blocked by coverage, score ratio, and Unity pass
  requirements.

## Important Files / Artifacts

- `scripts/validate_asset_evidence.py`: score >=3 pose QA enforcement.
- `tests/test_asset_evidence_validator.py`: regression coverage for score >=3
  pose QA rejection and score 2 allowance.
- `tests/test_generate_asset_evidence_report.py`: report fixtures updated for
  the stricter validator.
- `docs/asset-evidence-progress.md`: regenerated report with `Pose QA` column.
- `evidence/H-001/workflow-summary.json`: current H-001 failed pose QA.
- `evidence/H-006/workflow-summary.json`: current H-006 failed pose QA.

## Verification

- Command: `python3 -m pytest tests/test_asset_evidence_validator.py::test_asset_evidence_validator_blocks_score_three_plus_with_failed_pose_qa -q`
  - Result: failed first because validator accepted score 4 + pose fail, then
    passed after implementation.
- Command: `python3 -m pytest tests/test_asset_evidence_validator.py::test_asset_evidence_validator_blocks_score_three_plus_with_failed_pose_qa tests/test_asset_evidence_validator.py::test_asset_evidence_validator_allows_failed_pose_qa_for_low_score_evidence -q`
  - Result: passed, `2 passed`.
- Command: `python3 -m pytest tests/test_asset_evidence_validator.py -q`
  - Result: passed, `9 passed`.
- Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py -q`
  - Result: passed, `4 passed`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `85 passed in 9.84s`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed; current score 2 assets remain complete evidence.
- Command: `scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --output docs/asset-evidence-progress.md`
  - Result: passed and report regenerated.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --require-production-trial`
  - Result: expected blocked, exit `1`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; alpha package rebuilt.
- Command: `rtk git diff --check`
  - Result: passed.

## Open Questions / Risks

- Pose deformation still fails on H-001/H-006, so they correctly remain score 2.
- Validator enforcement only applies to score >=3. This avoids blocking
  low-quality evidence while preventing inflated quality claims.
- Unity and Unreal import verification are still blocked locally.
- No commit was created for this checkpoint.

## Next Steps

1. Reduce H-001/H-006 `poseDeformation.maxAxisExpansionRatio` below the fail
   threshold by tuning stress pose or shoulder/arm binding.
2. Add a score >=3 positive fixture in real evidence once a pose QA pass/warn
   asset exists.
3. Continue expanding real asset coverage after the pose QA rule is stable.

## Resume Prompt

Continue from this checkpoint. First read this file, inspect
`rtk git status --short`, then open `scripts/validate_asset_evidence.py`,
`tests/test_asset_evidence_validator.py`, `docs/asset-evidence-progress.md`,
`evidence/H-001/workflow-summary.json`, and
`evidence/H-006/workflow-summary.json`. The next useful task is reducing the
pose deformation fail metric or adding a new real asset with enforceable pose QA.
