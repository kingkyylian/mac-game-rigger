# Project Checkpoint - 2026-06-19 04:44

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`
- Latest commit before this checkpoint:
  `dd5f340 feat: add humanoid stress pose evidence`
- Context: H-001/H-006 now have front and side visual evidence for neutral and stress-pose review.

## Done This Session

- Continued the multi-view evidence work that was partially implemented before the previous status answer.
- Added workflow support for side preview artifacts:
  - `preview-neutral-side.png`
  - `preview-pose-side.png`
- Added stable workflow helpers:
  - `preview_artifact_paths`
  - `preview_artifact_summary`
  - `side_preview_axis`
- Added register CLI support for:
  - `--preview-neutral-side`
  - `--preview-pose-side`
- Extended validator preview evidence checks so side preview paths are checked when present.
- Re-ran Blender workflow on real assets:
  - H-001
  - H-006
- Updated manifest and notes to record side preview evidence.
- Updated `docs/asset-evaluation-protocol.md` to document side preview fields and register flags.
- Visually inspected side pose previews:
  - H-001 side view confirms leg/foot and torso/arm quality problems.
  - H-006 side view confirms torso/arm and foot quality problems.
- Kept both assets at score `2` and `visualReview.status=fail`.

## Current State

- H-001:
  - evidence complete: yes
  - front neutral/pose previews: present
  - side neutral/pose previews: present
  - score: `2`
  - visual review: `fail`
  - Unity/Unreal: `blocked`
- H-006:
  - evidence complete: yes
  - front neutral/pose previews: present
  - side neutral/pose previews: present
  - score: `2`
  - visual review: `fail`
  - Unity/Unreal: `blocked`
- Production trial remains blocked.
  - `completeEvidenceCount`: 2
  - `score3PlusCount`: 0
  - `score3PlusRatio`: 0.0

## Important Files / Artifacts

- `tools/blender_asset_workflow.py`: generates front and side preview evidence.
- `scripts/register_asset_evidence.py`: accepts side preview paths.
- `scripts/validate_asset_evidence.py`: validates side preview files when present.
- `samples/manifest.json`: H-001/H-006 include side preview paths.
- `evidence/H-001/preview-neutral-side.png`: new H-001 side neutral evidence.
- `evidence/H-001/preview-pose-side.png`: new H-001 side pose evidence.
- `evidence/H-006/preview-neutral-side.png`: new H-006 side neutral evidence.
- `evidence/H-006/preview-pose-side.png`: new H-006 side pose evidence.
- `evidence/H-001/workflow-summary.json`: includes `previewFrames.neutralSide` and `previewFrames.poseSide`.
- `evidence/H-006/workflow-summary.json`: includes `previewFrames.neutralSide` and `previewFrames.poseSide`.
- `docs/asset-evaluation-protocol.md`: documents multi-view preview evidence.
- `docs/asset-evidence-progress.md`: regenerated report.

## Verification

- Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_preview_artifact_paths_include_front_and_side_views -q`
  - Result: failed first because helper did not exist, then passed after implementation.
- Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_preview_artifact_summary_uses_stable_manifest_keys -q`
  - Result: failed first because helper did not exist, then passed after implementation.
- Command: `python3 -m pytest tests/test_register_asset_evidence.py::test_register_asset_evidence_updates_manifest_and_validates_files -q`
  - Result: failed first because side preview CLI flags did not exist, then passed after implementation.
- Command: `python3 -m pytest tests/test_asset_evidence_validator.py::test_asset_evidence_validator_checks_side_preview_files_when_present -q`
  - Result: failed first because side preview paths were ignored, then passed after implementation.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-001/H-001-quaternius-animated-human.fbx --evidence-dir evidence/H-001 --summary evidence/H-001/workflow-summary.json`
  - Result: passed; generated H-001 front and side previews.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-006/H-006-quaternius-animated-woman.fbx --evidence-dir evidence/H-006 --summary evidence/H-006/workflow-summary.json`
  - Result: passed; generated H-006 front and side previews.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed; side preview files found and readable.
- Command: `scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --output docs/asset-evidence-progress.md`
  - Result: passed and report regenerated.
- Command: `python3 -m pytest tests/test_blender_asset_workflow.py tests/test_register_asset_evidence.py tests/test_asset_evidence_validator.py tests/test_generate_asset_evidence_report.py -q`
  - Result: passed, `42 passed`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `93 passed in 10.68s`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial`
  - Result: expected blocked, exit `1`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; alpha package rebuilt at `dist/MacGameRigger-0.1.0.zip`.
- Command: `rtk git diff --check`
  - Result: passed.

## Open Questions / Risks

- Multi-view evidence improved review quality but did not improve deformation quality.
- H-001/H-006 remain score `2`.
- No score >=3 asset exists.
- No new real binary asset was added.
- Unity/Unreal import validation remains blocked locally.
- No commit was created for this checkpoint.

## Next Steps

1. Use the side views to target deformation fixes, starting with H-006 torso/arm and foot artifacts.
2. Add automated visual artifact metrics or region-level deformation diagnostics so score 2 vs score 3 is less manual.
3. Download/process the next real asset slot, preferably Q-001 or H-010, after the current evidence changes remain stable.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status --short`, then open
`tools/blender_asset_workflow.py`, `scripts/validate_asset_evidence.py`,
`samples/manifest.json`, and the new side preview images for H-001/H-006. The
next useful task is either a focused H-006 deformation-quality fix using side
view evidence, or adding a new real asset such as Q-001/H-010.
