# Project Checkpoint - 2026-06-19 04:49

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`
- Latest commit before this checkpoint:
  `dd5f340 feat: add humanoid stress pose evidence`
- Context: Score >=3 visual pass now requires side preview evidence, not just notes.

## Done This Session

- Restored context from `docs/checkpoints/2026-06-19-0444.md`.
- Added a stricter score >=3 validator rule:
  - If Unity/Unreal import is not `pass`, then `visualReview.status=pass` must have:
    - non-empty `visualReview.notes`;
    - `previewNeutralSide`;
    - `previewPoseSide`.
- Updated tests so production-trial fixtures include side preview evidence for visual-pass score 4 assets.
- Updated missing-side-preview tests to verify both missing path checks and missing visual-pass support checks.
- Updated `docs/asset-evaluation-protocol.md` to state that visual pass for score >=3 requires side preview artifacts.
- Regenerated `docs/asset-evidence-progress.md`.

## Current State

- H-001:
  - evidence complete: yes
  - score: `2`
  - visual review: `fail`
  - front/side previews: present
  - Unity/Unreal: `blocked`
- H-006:
  - evidence complete: yes
  - score: `2`
  - visual review: `fail`
  - front/side previews: present
  - Unity/Unreal: `blocked`
- Production trial remains blocked.
  - `completeEvidenceCount`: 2
  - `score3PlusCount`: 0
  - `score3PlusRatio`: 0.0

## Important Files / Artifacts

- `scripts/validate_asset_evidence.py`: now requires side previews for visual-pass score >=3 support.
- `tests/test_asset_evidence_validator.py`: covers missing side previews for score >=3 visual pass.
- `docs/asset-evaluation-protocol.md`: documents the stricter visual evidence requirement.
- `docs/asset-evidence-progress.md`: regenerated report.

## Verification

- Command: `python3 -m pytest tests/test_asset_evidence_validator.py::test_asset_evidence_validator_requires_side_previews_for_score_three_plus_visual_pass -q`
  - Result: failed first because visual pass with notes was accepted without side previews, then passed after implementation.
- Command: `python3 -m pytest tests/test_asset_evidence_validator.py -q`
  - Result: passed, `14 passed`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `94 passed in 10.72s`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed.
- Command: `scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --output docs/asset-evidence-progress.md`
  - Result: passed and report regenerated.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial`
  - Result: expected blocked, exit `1`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; alpha package rebuilt at `dist/MacGameRigger-0.1.0.zip`.
- Command: `rtk git diff --check`
  - Result: passed.

## Open Questions / Risks

- This improves evidence quality gates, not deformation quality.
- H-001/H-006 remain score `2`.
- No score >=3 asset exists.
- No new real binary asset was added.
- Unity/Unreal import validation remains blocked locally.
- No commit was created for this checkpoint.

## Next Steps

1. Start a focused deformation fix for H-006 using front and side pose evidence.
2. Add region-level deformation diagnostics so wrist/foot/torso artifacts can be tracked numerically.
3. Download/process a new real asset slot, preferably Q-001 or H-010, after current evidence gates remain stable.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status --short`, then open
`scripts/validate_asset_evidence.py`, `tests/test_asset_evidence_validator.py`,
`docs/asset-evaluation-protocol.md`, and the H-006 front/side pose previews. The
next useful task is a focused deformation-quality fix or region-level artifact metric.
