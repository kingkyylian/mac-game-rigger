# Project Checkpoint - 2026-06-19 04:57

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, HEAD `dd5f340 feat: add humanoid stress pose evidence`
- Project-local `AGENTS.md`: not present; use `/Users/kyylian` Codex workflow/preferences.
- Context: CUDA-free Mac rigging MVP still produces score-2 humanoid evidence; this checkpoint adds numeric preview silhouette diagnostics so visual deformation failures are easier to track.

## Done This Session

- Added preview silhouette diagnostics to `scripts/validate_asset_evidence.py`.
  - Reused the PNG luma decoder through `_png_luma_values`.
  - Added `png_foreground_stats` with image size, foreground pixel ratio, foreground bounding box, and bounding-box fill ratio.
  - Added `previewDiagnostics` to slot output when `--check-evidence-files` is enabled.
- Added report-visible preview summary to `scripts/generate_asset_evidence_report.py`.
  - If side previews exist, report shows side silhouette width expansion.
  - Otherwise report shows neutral foreground/fill percentages.
- Updated `docs/asset-evaluation-protocol.md` to explain preview silhouette diagnostics.
- Regenerated `docs/asset-evidence-progress.md`.
- Followed TDD:
  - First validator test failed with missing `previewDiagnostics`.
  - Then implementation made it pass.
  - First report test failed with missing `Preview` column.
  - Then report implementation made it pass.

## Current State

- H-001:
  - evidence complete: yes
  - score: `2`
  - visual review: `fail`
  - Unity/Unreal: `blocked`
  - side preview metric: `side 54->190px 3.52x`
- H-006:
  - evidence complete: yes
  - score: `2`
  - visual review: `fail`
  - Unity/Unreal: `blocked`
  - side preview metric: `side 54->220px 4.07x`
- Production trial remains blocked:
  - `completeEvidenceCount`: 2
  - `score3PlusCount`: 0
  - `score3PlusRatio`: 0.0
- The new metric does not raise scores or open the production gate; it exposes deformation/cropping/silhouette signals for review and regression tracking.

## Important Files / Artifacts

- `scripts/validate_asset_evidence.py`: emits `previewDiagnostics` during evidence-file checks.
- `scripts/generate_asset_evidence_report.py`: renders `Preview` column from diagnostics.
- `tests/test_asset_evidence_validator.py`: covers preview silhouette diagnostics.
- `tests/test_generate_asset_evidence_report.py`: covers report preview summary.
- `docs/asset-evaluation-protocol.md`: documents diagnostic meaning and limits.
- `docs/asset-evidence-progress.md`: regenerated with H-001/H-006 side expansion metrics.
- `evidence/H-001/preview-*-side.png` and `evidence/H-006/preview-*-side.png`: side-view evidence used by the new metrics.

## Verification

- Command: `python3 -m pytest tests/test_asset_evidence_validator.py::test_asset_evidence_validator_reports_preview_silhouette_diagnostics -q`
  - Result: failed first with `KeyError: 'previewDiagnostics'`, then passed after implementation.
- Command: `python3 -m pytest tests/test_asset_evidence_validator.py -q`
  - Result: passed, `15 passed`.
- Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py::test_generate_asset_evidence_report_shows_preview_silhouette_summary -q`
  - Result: failed first because the `Preview` column was missing, then passed after implementation.
- Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py -q`
  - Result: passed, `5 passed`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `96 passed in 10.81s`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files`
  - Result: passed; H-001/H-006 include preview diagnostics in JSON output.
- Command: `scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --output docs/asset-evidence-progress.md`
  - Result: passed and report regenerated.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial`
  - Result: expected blocked, exit `1`; gate remains blocked.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; package rebuilt at `dist/MacGameRigger-0.1.0.zip`.
- Command: `rtk git diff --check`
  - Result: passed.

## Open Questions / Risks

- The metric is silhouette-level, not body-region semantic analysis.
- H-001/H-006 are still score `2`; no score >=3 asset exists.
- No Unity/Unreal import pass exists locally.
- Blender headless tests were skipped in smoke by request flag; existing evidence came from earlier Blender workflow runs.
- Worktree remains intentionally uncommitted with broad prior changes plus this checkpoint.

## Next Steps

1. Use the side expansion metric to tune H-006 weighting first; it currently has the worse side-view expansion (`4.07x`).
2. Add semantic region metrics next: wrist/ankle/torso/foot masks or bone/vertex-group derived deformation summaries.
3. After H-006 improves, rerun Blender workflow and compare `side neutral->pose` width ratio, pose QA, manual visual review, and FBX export.

## Resume Prompt

Continue from this checkpoint. First read `docs/checkpoints/2026-06-19-0457.md`, then inspect `scripts/validate_asset_evidence.py`, `scripts/generate_asset_evidence_report.py`, `docs/asset-evidence-progress.md`, and H-006 front/side previews. The next useful task is a focused H-006 deformation fix using the new preview silhouette metric as a regression signal.
