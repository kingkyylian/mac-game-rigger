# Project Checkpoint - 2026-06-19 05:07

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, HEAD `dd5f340 feat: add humanoid stress pose evidence`
- Project-local `AGENTS.md`: not present; use `/Users/kyylian` Codex workflow/preferences.
- Context: This pass added a stronger side-view diagnostic and made a conservative weighting/pose policy adjustment, but H-006 remains score 2.

## Done This Session

- Restored context from `docs/checkpoints/2026-06-19-0457.md`.
- Added `verticalCenterShiftRatio` to preview diagnostics in `scripts/validate_asset_evidence.py`.
  - It compares top and bottom foreground-band center positions in a PNG silhouette.
  - It gives a numeric signal for side-view body lean / centerline drift.
- Updated `scripts/generate_asset_evidence_report.py` so the `Preview` column includes `lean` when side previews are present.
- Updated `docs/asset-evaluation-protocol.md` to document vertical center-shift / lean diagnostics.
- Added TDD coverage:
  - validator reports `verticalCenterShiftRatio`;
  - report renders side preview lean summary.
- Added a conservative weight binding policy:
  - distal bones (`Hand`, `Foot`) now cap minimum fallback coverage at 2 vertices;
  - limb bones (`Arm`, `Leg`) cap minimum fallback coverage at 4 vertices;
  - core bones keep the requested default.
- Added TDD coverage for smaller distal minimum coverage.
- Reduced stress-pose upper-leg rotation from `-0.25` to `-0.1` while keeping lower-leg bend at `0.75`.
  - Added TDD coverage that stress pose keeps upper-leg motion side-review friendly.
- Re-ran Blender workflows for both H-006 and H-001 so evidence matches current code.
- Regenerated `docs/asset-evidence-progress.md`.

## Current State

- H-001:
  - evidence complete: yes
  - score: `2`
  - visual review: `fail`
  - Unity/Unreal: `blocked`
  - pose QA: `warn 3.3283x x`
  - preview metric: `side 54->192px 3.56x lean 0.15`
- H-006:
  - evidence complete: yes
  - score: `2`
  - visual review: `fail`
  - Unity/Unreal: `blocked`
  - pose QA: `warn 3.8172x x`
  - preview metric: `side 54->220px 4.07x lean 0.27`
- Production trial remains blocked:
  - `completeEvidenceCount`: 2
  - `score3PlusCount`: 0
  - `score3PlusRatio`: 0.0
- Important finding: distal coverage and upper-leg pose tuning did not materially improve H-006 metrics. The next quality issue is likely landmark/weight distribution around torso-arm/leg influence, not just distal fallback count.

## Important Files / Artifacts

- `scripts/validate_asset_evidence.py`: emits `verticalCenterShiftRatio`.
- `scripts/generate_asset_evidence_report.py`: renders side preview `lean`.
- `addon/mac_game_rigger/core/weight_binding.py`: distal/limb minimum coverage caps.
- `addon/mac_game_rigger/core/pose_tests.py`: reduced stress upper-leg rotation.
- `tests/test_asset_evidence_validator.py`: validator lean diagnostic coverage.
- `tests/test_generate_asset_evidence_report.py`: report lean summary coverage.
- `tests/test_capsule_weights.py`: distal coverage policy coverage.
- `tests/test_pose_tests.py`: side-review-friendly upper-leg stress coverage.
- `evidence/H-001/*` and `evidence/H-006/*`: regenerated Blender evidence.
- `docs/asset-evidence-progress.md`: regenerated with `lean` metrics.

## Verification

- Command: `python3 -m pytest tests/test_asset_evidence_validator.py::test_asset_evidence_validator_reports_preview_vertical_center_shift -q`
  - Result: failed first with missing `verticalCenterShiftRatio`, then passed after implementation.
- Command: `python3 -m pytest tests/test_asset_evidence_validator.py -q`
  - Result: passed, `16 passed`.
- Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py::test_generate_asset_evidence_report_shows_side_preview_lean_summary -q`
  - Result: failed first because `lean` was not rendered, then passed after implementation.
- Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py -q`
  - Result: passed, `6 passed`.
- Command: `python3 -m pytest tests/test_capsule_weights.py::test_minimum_bone_coverage_uses_smaller_target_for_distal_bones -q`
  - Result: failed first because `Hand.L` received 8 fallback vertices, then passed after implementation.
- Command: `python3 -m pytest tests/test_capsule_weights.py -q`
  - Result: passed, `15 passed`.
- Command: `python3 -m pytest tests/test_pose_tests.py::test_humanoid_stress_pose_keeps_upper_leg_motion_side_review_friendly -q`
  - Result: failed first because upper-leg rotation was `0.25`, then passed after implementation.
- Command: `python3 -m pytest tests/test_pose_tests.py -q`
  - Result: passed, `3 passed`.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-006/H-006-quaternius-animated-woman.fbx --evidence-dir evidence/H-006 --summary evidence/H-006/workflow-summary.json --camera-axis x`
  - Result: passed; H-006 evidence regenerated.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-001/H-001-quaternius-animated-human.fbx --evidence-dir evidence/H-001 --summary evidence/H-001/workflow-summary.json --camera-axis x`
  - Result: passed; H-001 evidence regenerated.
- Command: `scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --output docs/asset-evidence-progress.md`
  - Result: passed and report regenerated.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `100 passed in 10.73s`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --quiet --output /tmp/mac-game-rigger-evidence-report.json`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial`
  - Result: expected blocked, exit `1`; gate remains blocked.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; package rebuilt at `dist/MacGameRigger-0.1.0.zip`.
- Command: `rtk git diff --check`
  - Result: passed.

## Open Questions / Risks

- H-006 remains score `2`; this pass did not create a game-ready rig.
- `lean` is still silhouette-level, not semantic body-region segmentation.
- No score >=3 asset exists.
- No Unity/Unreal import pass exists locally.
- Blender workflows passed locally, but alpha smoke still skipped Blender headless tests by flag.
- Worktree remains intentionally uncommitted and includes broad previous changes.

## Next Steps

1. Add semantic region diagnostics from actual rig data: per-bone or vertex-group influence distribution for torso, upper arms, lower arms, upper legs, lower legs, hands, feet.
2. Use those diagnostics on H-006 to identify whether torso vertices are being pulled by limb bones or limb vertices are under-supported.
3. Then implement the next weighting fix against a numeric H-006 target; do not rely on global capsule/pose tweaks alone.

## Resume Prompt

Continue from this checkpoint. First read `docs/checkpoints/2026-06-19-0507.md`, then inspect `addon/mac_game_rigger/core/weight_binding.py`, `scripts/validate_asset_evidence.py`, `docs/asset-evidence-progress.md`, and H-006 previews. The next useful task is semantic region-level weight diagnostics for H-006, because distal fallback and pose tuning did not improve the visible score-2 deformation.
