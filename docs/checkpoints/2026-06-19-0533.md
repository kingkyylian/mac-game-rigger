# Project Checkpoint - 2026-06-19 05:33

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, HEAD `dd5f340 feat: add humanoid stress pose evidence`
- Project-local `AGENTS.md`: not present; use `/Users/kyylian` Codex workflow/preferences.
- Context: This pass found and fixed the main real-asset weight-binding coordinate-space bug; rig output is materially better but still not production-ready.

## Done This Session

- Continued from `docs/checkpoints/2026-06-19-0507.md`.
- Added semantic weight diagnostics to the Blender asset workflow:
  - region totals/dominance for `core`, `neckHead`, arms, legs, hands, and feet;
  - height-band dominance diagnostics;
  - capsule diagnostics for generated armature bone head/tail/radius bounds;
  - bind-space diagnostics for raw vs normalized mesh vertex ranges.
- Added report support for the `Weight` column in `docs/asset-evidence-progress.md`.
- Found the real H-006/H-001 weight failure:
  - previous evidence showed almost all dominant weights on `Neck`;
  - capsule diagnostics showed armature/bbox coordinates were around normal asset scale;
  - raw mesh vertex coordinates were in a different coordinate space for binding;
  - weighting compared mesh vertices against armature bones without explicit bind-space normalization.
- Added `mesh_bind_vertex_points(..., target_bounds=...)` in `addon/mac_game_rigger/core/weight_binding.py`.
  - `apply_capsule_weights_to_mesh` now maps mesh vertices into capsule/armature bounds before capsule weighting.
  - This fixed the dominant region distribution on both real humanoid assets.
- Added TDD coverage for:
  - neck/head separated from torso core diagnostics;
  - height-band weight diagnostics;
  - capsule diagnostics;
  - non-unit scale transform planning;
  - bind-space normalization and explicit target bounds.
- Re-ran Blender workflows for both real humanoid assets and regenerated evidence/report artifacts.

## Current State

- H-001:
  - evidence complete: yes
  - score: `2`
  - visual review: `fail`
  - Unity/Unreal: `blocked`
  - pose QA: `warn 3.4877x x`
  - preview metric: `side 54->216px 4.00x lean 0.17`
  - weight distribution now: `core 409 neck 28 arm 158 leg 47 foot 149`
  - previous critical defect was `core 0`, now fixed.
- H-006:
  - evidence complete: yes
  - score: `2`
  - visual review: `fail`
  - Unity/Unreal: `blocked`
  - pose QA: `warn 3.9983x x`
  - preview metric: `side 54->201px 3.72x lean 0.13`
  - weight distribution now: `core 526 neck 43 arm 158 leg 53 foot 174`
  - previous critical defect was `neck 736 / core 0`, now fixed.
- Production trial remains blocked:
  - complete real assets: `2`
  - score >=3 count: `0`
  - score >=3 ratio: `0.0`
  - missing categories/assets/import passes remain unchanged.
- Important: rig quality improved structurally, but scoring did not cross score 3. The remaining blocker is now pose/evaluation quality rather than the previous catastrophic neck-dominant bind.

## Important Files / Artifacts

- `addon/mac_game_rigger/core/weight_binding.py`: bind-space vertex normalization and capsule target bounds.
- `tools/blender_asset_workflow.py`: weight diagnostics, capsule diagnostics, bind-space diagnostics, scale transform planning.
- `scripts/generate_asset_evidence_report.py`: renders weight diagnostics in progress report.
- `docs/asset-evidence-progress.md`: regenerated status report with current weight distributions.
- `evidence/H-001/workflow-summary.json`: regenerated with improved core-dominant weights.
- `evidence/H-006/workflow-summary.json`: regenerated with improved core-dominant weights.
- `tests/test_capsule_weights.py`: coverage for capsule selection, diagnostics, and bind-space normalization.
- `tests/test_blender_asset_workflow.py`: coverage for workflow diagnostics and scale planning.

## Verification

- Command: `python3 -m pytest tests/test_capsule_weights.py tests/test_blender_asset_workflow.py -q`
  - Result: passed, `44 passed`.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-006/H-006-quaternius-animated-woman.fbx --evidence-dir evidence/H-006 --summary evidence/H-006/workflow-summary.json --camera-axis x`
  - Result: passed; H-006 evidence regenerated.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-001/H-001-quaternius-animated-human.fbx --evidence-dir evidence/H-001 --summary evidence/H-001/workflow-summary.json --camera-axis x`
  - Result: passed; H-001 evidence regenerated.
- Command: `scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --output docs/asset-evidence-progress.md`
  - Result: passed; report regenerated.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `112 passed in 11.15s`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --quiet --output /tmp/mac-game-rigger-evidence-report.json`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial`
  - Result: expected blocked, exit `1`; production gate remains blocked.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; package rebuilt at `dist/MacGameRigger-0.1.0.zip`.
- Command: `rtk git diff --check`
  - Result: passed.

## Open Questions / Risks

- No asset is score >=3 yet; production trial is still blocked.
- Unity import pass remains missing; Unreal remains explicitly blocked.
- Evidence set is still too small: 2 real assets only, both humanoid.
- H-001/H-006 side pose width remains high; next work should focus on scoring/pose realism and whether the stress pose is too harsh or the visual scoring threshold is wrong for T-pose arm extension.
- The worktree remains intentionally uncommitted and includes broad prior changes outside this exact pass.

## Next Steps

1. Inspect H-001/H-006 side pose previews after the core-weight fix and decide whether the remaining fail is actual deformation or over-strict side-width scoring.
2. Add a numeric pose-quality target for post-fix assets: likely reduce side expansion below the score-3 threshold without hiding real deformation.
3. Add at least one more real humanoid asset after the pose/scoring issue is addressed, then begin quadruped/tail creature coverage.

## Resume Prompt

Continue from this checkpoint. First read `docs/checkpoints/2026-06-19-0533.md`, then inspect `docs/asset-evidence-progress.md`, `evidence/H-006/workflow-summary.json`, `evidence/H-006/preview-pose-side.png`, and `addon/mac_game_rigger/core/weight_binding.py`. The next useful task is no longer fixing neck-dominant weights; that is fixed. Focus on why score remains 2 despite core-dominant weights: pose stress severity, side-width scoring, or remaining real deformation.
