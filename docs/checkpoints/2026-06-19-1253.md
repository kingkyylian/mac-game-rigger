# Project Checkpoint - 2026-06-19 12:53

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, HEAD `dd5f340 feat: add humanoid stress pose evidence`
- Project-local `AGENTS.md`: not present; use `/Users/kyylian` Codex workflow/preferences.
- Context: This pass converted H-006 from score-2 low-quality evidence to the first score-3 usable-with-cleanup evidence, while production trial remains blocked by coverage and engine import gates.

## Done This Session

- Restored from `docs/checkpoints/2026-06-19-0533.md`.
- Investigated why score stayed `2` after the core-weight fix.
  - Manifest score and visual status are manually registered evidence fields.
  - Validator only enforces whether score >=3 has enough support.
  - H-001 front pose is still visibly poor and remains score `2` / visual `fail`.
  - H-006 front stress pose is readable enough for score `3`, but the old side pose was polluted by arm/neck projection.
- Reduced humanoid stress arm rotation in `addon/mac_game_rigger/core/pose_tests.py`:
  - upper arm Z rotation now `0.18`;
  - lower arm Z rotation now `0.06`.
- Added `apply_humanoid_side_review_pose`.
  - It omits arm swing and neck/head turn.
  - It uses only the leg bend chain for side silhouette review.
- Added `mgr.pose_humanoid_side_review` in `addon/mac_game_rigger/ui/operators.py`.
- Updated `tools/blender_asset_workflow.py` so:
  - front `preview-pose.png` still uses `pose_humanoid_stress`;
  - side `preview-pose-side.png` uses `pose_humanoid_side_review`.
- Regenerated H-001 and H-006 Blender evidence.
- Registered H-006 as score `3` with `visualReview.status=pass` and non-empty visual notes.
- Updated `evidence/H-006/notes.md`.
- Regenerated `docs/asset-evidence-progress.md`.
- Updated `docs/asset-evaluation-protocol.md` to document separate front stress vs side-review pose evidence.

## Current State

- H-001:
  - evidence complete: yes
  - score: `2`
  - visual review: `fail`
  - Unity/Unreal: `blocked`
  - pose QA: `warn 2.591x x`
  - preview metric: `side 54->64px 1.19x lean 0.02`
  - weight distribution: `core 409 neck 28 arm 158 leg 47 foot 149`
  - status: still not acceptable; front pose silhouette remains poor.
- H-006:
  - evidence complete: yes
  - score: `3`
  - visual review: `pass`
  - Unity/Unreal: `blocked`
  - pose QA: `warn 3.201x x`
  - preview metric: `side 54->52px 0.96x lean 0.01`
  - weight distribution: `core 526 neck 43 arm 158 leg 53 foot 174`
  - status: first usable-with-cleanup score-3 evidence, not production-ready.
- Production trial remains blocked:
  - complete real assets: `2`
  - score >=3 count: `1`
  - score >=3 ratio: `0.5`
  - still missing: 10 complete real assets, 3 humanoids, 2 quadrupeds, tail/prop/wide/thin coverage, and 3 Unity import passes.

## Important Files / Artifacts

- `addon/mac_game_rigger/core/pose_tests.py`: lower front stress arm swing and new side-review pose.
- `addon/mac_game_rigger/ui/operators.py`: new `mgr.pose_humanoid_side_review` operator.
- `tools/blender_asset_workflow.py`: front stress and side-review preview are now separate.
- `samples/manifest.json`: H-006 is now score `3`, visual pass with notes.
- `evidence/H-006/notes.md`: updated score-3 rationale.
- `docs/asset-evidence-progress.md`: report now shows H-006 score `3`.
- `docs/asset-evaluation-protocol.md`: documents side-review pose semantics.
- `evidence/H-006/preview-pose.png`: front stress evidence.
- `evidence/H-006/preview-pose-side.png`: clean side-review evidence.

## Verification

- Command: `python3 -m pytest tests/test_pose_tests.py tests/test_blender_asset_workflow.py -q`
  - Result: passed, `29 passed`.
- Command: `python3 -m pytest tests/test_capsule_weights.py tests/test_generate_asset_evidence_report.py -q`
  - Result: passed, `27 passed`.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-006/H-006-quaternius-animated-woman.fbx --evidence-dir evidence/H-006 --summary evidence/H-006/workflow-summary.json --camera-axis x`
  - Result: passed; side preview used `pose_humanoid_side_review`.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-001/H-001-quaternius-animated-human.fbx --evidence-dir evidence/H-001 --summary evidence/H-001/workflow-summary.json --camera-axis x`
  - Result: passed; side preview used `pose_humanoid_side_review`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `114 passed in 10.38s`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --quiet --output /tmp/mac-game-rigger-evidence-report.json`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial`
  - Result: expected blocked, exit `1`; score3PlusCount is now `1`, score3PlusRatio is `0.5`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; package rebuilt at `dist/MacGameRigger-0.1.0.zip`.
- Command: `rtk git diff --check`
  - Result: passed.

## Open Questions / Risks

- H-006 is score `3`, not production-ready. Engine import still has not run locally.
- H-001 remains score `2`; improving it likely requires asset-specific orientation/landmark/limb treatment or accepting that this source is a weak baseline.
- Production trial still needs asset coverage and Unity import evidence.
- Side-review pose is intentionally different from front stress pose; this is now documented, but future reviewers should not compare side width against the full front stress pose directly.
- Worktree remains intentionally uncommitted and contains broad prior changes.

## Next Steps

1. Add a third real humanoid asset and try to get it to score >=3 using the current bind-space + side-review workflow.
2. Start quadruped evidence coverage after at least one more humanoid passes.
3. Run Unity import verification when Unity is available; production gate needs 3 Unity import passes.

## Resume Prompt

Continue from this checkpoint. First read `docs/checkpoints/2026-06-19-1253.md`, then inspect `docs/asset-evidence-progress.md`, `samples/manifest.json`, `evidence/H-006/workflow-summary.json`, and `tools/blender_asset_workflow.py`. The next useful task is expanding real asset coverage: add one more humanoid or start quadruped evidence, while preserving the score-3 support rules.
