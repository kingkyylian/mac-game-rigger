# Project Checkpoint - 2026-06-19 13:32

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, HEAD `dd5f340 feat: add humanoid stress pose evidence`
- Project-local `AGENTS.md`: not present; use `/Users/kyylian` Codex workflow/preferences.
- Context: This pass added real quadruped motion proof to the add-on and Q-001 workflow, while keeping Q-001 score 2 because visual evidence is still not production-readable.

## Done This Session

- Resumed from `docs/checkpoints/2026-06-19-1324.md`.
- Used TDD for quadruped pose support:
  - Added failing tests for `apply_quadruped_gait_pose`.
  - Added failing tests for `apply_quadruped_side_review_pose`.
  - Added failing workflow tests for quadruped operator dispatch.
- Implemented quadruped pose functions in `addon/mac_game_rigger/core/pose_tests.py`:
  - `apply_quadruped_gait_pose`
  - `apply_quadruped_side_review_pose`
- Added Blender UI operators in `addon/mac_game_rigger/ui/operators.py`:
  - `mgr.pose_quadruped_gait`
  - `mgr.pose_quadruped_side_review`
- Added manual panel buttons in `addon/mac_game_rigger/ui/panels.py`.
- Updated `tools/blender_asset_workflow.py` so:
  - humanoid still uses `pose_humanoid_stress` and `pose_humanoid_side_review`;
  - quadruped now uses `pose_quadruped_gait` and `pose_quadruped_side_review`;
  - quadruped no longer uses static placeholder pose previews;
  - pose deformation summary is generated for quadruped assets.
- Re-ran Q-001 workflow with the new quadruped pose operator:
  - Blender log: `Quadruped gait: changed=16`
  - Blender log: `Quadruped side review: changed=12`
  - workflow summary now reports `posePreview.operator = pose_quadruped_gait`
  - pose side preview now reports `pose_quadruped_side_review`
  - `poseDeformation.status = pass`
  - `maxAxisExpansionRatio = 1.2729`
- Visually reviewed Q-001 again:
  - side preview now shows visible motion;
  - front preview and overall orientation/readability are still too weak for production-quality claim;
  - kept Q-001 at score `2` and visual review `fail`.
- Updated:
  - `evidence/Q-001/notes.md`
  - `samples/manifest.json`
  - `samples/asset-source-candidates.json`
  - `docs/asset-evidence-progress.md`

## Current State

- Production trial gate remains blocked.
- Current report:
  - real assets registered: `5`
  - complete evidence entries: `5`
  - score >= 3 count: `3`
  - score >= 3 ratio: `0.6`
  - humanoids complete: `4`
  - quadrupeds complete: `1`
  - tail creatures complete: `0`
- Q-001 now has real quadruped motion proof, but not a score-3 visual outcome.
- Requirements passing:
  - `humanoidsAtLeast3`
  - `lowPolyHumanoidIncluded`
  - `thinLimbHumanoidIncluded`
  - `wideShoulderOrBulkyIncluded`
  - `propOrAccessoryIncluded`
  - `unrealPassOrExplicitBlocker`
- Requirements still missing:
  - `completeRealAssetsAtLeast10`
  - `quadrupedsAtLeast2`
  - `tailCreatureIncluded`
  - `score3PlusAtLeast70Percent`
  - `unityImportPassesAtLeast3`

## Important Files / Artifacts

- `addon/mac_game_rigger/core/pose_tests.py`: new quadruped gait and side-review pose functions.
- `addon/mac_game_rigger/ui/operators.py`: new Blender operators for quadruped pose tests.
- `addon/mac_game_rigger/ui/panels.py`: manual UI buttons for quadruped pose tests.
- `tools/blender_asset_workflow.py`: quadruped workflow now uses real pose operators and records pose deformation.
- `tests/test_pose_tests.py`: quadruped pose unit tests.
- `tests/test_blender_asset_workflow.py`: quadruped operator dispatch tests.
- `evidence/Q-001/workflow-summary.json`: Q-001 now records `pose_quadruped_gait`, `pose_quadruped_side_review`, and pose deformation pass.
- `evidence/Q-001/preview-pose-side.png`: updated side pose preview with quadruped motion.
- `evidence/Q-001/notes.md`: updated Q-001 score rationale.
- `samples/manifest.json`: Q-001 visual note updated; score remains 2.
- `docs/asset-evidence-progress.md`: current gate report with Q-001 pose QA pass but score 2.

## Verification

- Command: `python3 -m pytest tests/test_pose_tests.py::test_quadruped_gait_pose_moves_diagonal_leg_pairs_and_tail tests/test_pose_tests.py::test_quadruped_side_review_pose_keeps_tail_and_head_neutral -q`
  - Result before implementation: failed as expected; missing quadruped functions.
- Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_pose_preview_uses_quadruped_gait_pose_for_quadruped_template tests/test_blender_asset_workflow.py::test_side_pose_preview_uses_quadruped_side_review_pose_for_quadruped_template -q`
  - Result before implementation: failed as expected; operator name functions did not accept template arg.
- Command: `python3 -m pytest tests/test_pose_tests.py tests/test_blender_asset_workflow.py -q`
  - Result: passed, `35 passed in 0.02s`.
- Command: `python3 -m pytest tests/test_templates.py tests/test_asset_analysis.py -q`
  - Result: passed, `7 passed in 0.01s`.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/Q-001/Q-001-quaternius-husky.fbx --evidence-dir evidence/Q-001 --summary evidence/Q-001/workflow-summary.json --camera-axis y --template quadruped`
  - Result: passed; gait changed 16 bones, side review changed 12 bones.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `121 passed in 10.70s`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --quiet --output /tmp/mac-game-rigger-evidence-report-quadruped-pose.json`
  - Result: passed.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial`
  - Result: expected blocked, exit `1`; complete real assets `5`, quadrupeds `1`, score ratio `0.6`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; package rebuilt at `dist/MacGameRigger-0.1.0.zip`.
- Command: `rtk git diff --check`
  - Result: passed.

## Open Questions / Risks

- Q-001 is closer to useful now, but still not score 3. The remaining issue is visual proof/readability, not lack of motion.
- Quadruped camera/orientation evidence is weak: front view still reads too narrow/top-down and side silhouette is not clean enough.
- Unity/Unreal import remains blocked for all real assets; production gate needs 3 Unity import passes.
- Worktree remains intentionally uncommitted and contains broad prior changes from earlier passes.

## Next Steps

1. Improve quadruped orientation/camera evidence for Q-001 so front and side previews read like an inspectable four-leg asset.
2. Re-run Q-001 after preview/orientation improvement and decide whether score 3 is defensible.
3. After Q-001 reaches score 3 or the blocker is documented, add Q-002 and C-001 coverage, then start Unity import passes.

## Resume Prompt

Continue from this checkpoint. First read `docs/checkpoints/2026-06-19-1332.md`, then inspect `docs/asset-evidence-progress.md`, `evidence/Q-001/workflow-summary.json`, `evidence/Q-001/preview-neutral-side.png`, and `evidence/Q-001/preview-pose-side.png`. The next useful task is improving quadruped preview orientation/camera evidence so Q-001 can be judged for score 3 without exaggerating quality.
