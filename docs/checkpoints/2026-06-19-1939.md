# Project Checkpoint - 2026-06-19 19:39

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, HEAD `dd5f340 feat: add humanoid stress pose evidence`
- Project-local `AGENTS.md`: not present; use `/Users/kyylian` Codex workflow/preferences.
- Context: Continued from the 19:31 prop classification/diagnostics checkpoint. Production-trial gate still passes, and P-001 now has CLI-level hinge pivot/base controls recorded in workflow evidence.

## Done This Session

- Followed the checkpoint next step: hinge pivot/origin controls for prop assets.
- Used TDD for the behavior changes:
  - Added failing tests for `prop_hinge_landmarks_from_bbox(..., hinge_pivot_x, base_origin_x)`.
  - Added a failing test for `landmarks_from_bbox("prop_hinge", ..., prop_hinge_pivot_x, prop_hinge_base_x)`.
  - Added failing CLI parse coverage for `--prop-hinge-pivot-x` and `--prop-hinge-base-x`.
  - Added failing range validation coverage for out-of-range prop hinge controls.
  - Added failing summary coverage for `workflow_controls_summary(...)`.
- Implemented artist-facing CLI controls in `tools/blender_asset_workflow.py`:
  - `--prop-hinge-pivot-x`
  - `--prop-hinge-base-x`
  - both values are normalized `0.0..1.0` bbox factors.
- Updated prop hinge landmark generation:
  - default behavior remains compatible with the previous template.
  - explicit controls can move hinge pivot and base/origin along bbox X.
- Added `workflowControls` to workflow summary JSON for prop hinge runs:
  - `workflowControls.propHinge.pivotX`
  - `workflowControls.propHinge.baseX`
- Reran P-001 Blender workflow with explicit controls:
  - `--prop-hinge-pivot-x 0.08`
  - `--prop-hinge-base-x 0.18`
- P-001 current workflow summary:
  - `workflowControls`: `{"propHinge": {"pivotX": 0.08, "baseX": 0.18}}`
  - weight regions:
    - `propBase`: 138 dominant vertices
    - `propHinge`: 12 dominant vertices
    - `propMovingPart`: 210 dominant vertices
  - pose deformation: pass
  - allowed expanded axis: `y`
- Reran Unity batchmode import for the refreshed P-001 export:
  - `evidence/P-001/export-unity.fbx` -> pass.
- Updated `evidence/P-001/notes.md`:
  - records pivot X `0.08`, base/origin X `0.18`.
  - records updated prop weight diagnostics.
  - remaining risk is now UI/gizmo feedback and diagnostic thresholds, not absence of CLI controls.
- Regenerated `docs/asset-evidence-progress.md`.
  - P-001 row now shows: `prop base 138 hinge 12 moving 210`.
- Updated `samples/asset-source-candidates.json`:
  - P-001 selection notes mention CLI-level hinge pivot/base controls.
  - next action focuses on UI/gizmo controls, prop diagnostic thresholds, broader mechanical classification, and shaded/scale review.

## Current State

- Production-trial gate: `pass`
- Missing production-trial requirements: none
- Real assets registered: `11`
- Complete evidence entries: `11`
- Score >= 3 count: `10`
- Score >= 3 ratio: `0.909`
- Category counts:
  - humanoid: `7`
  - quadruped: `2`
  - tail creature: `1`
  - prop: `1`
- Unity import pass slots:
  - `H-003`
  - `H-004`
  - `Q-001`
  - `C-001`
  - `P-001`
- P-001 current evidence:
  - suggested category: `prop`
  - template: `prop_hinge`
  - CLI controls: pivot X `0.08`, base/origin X `0.18`
  - deformation score: `3`
  - visual review: `pass`
  - Unity import: `pass`
  - Unreal import: `blocked`

## Important Files / Artifacts

- `tools/blender_asset_workflow.py`: prop hinge CLI controls, landmark control routing, and `workflowControls` summary.
- `tests/test_blender_asset_workflow.py`: RED/GREEN coverage for pivot/base controls and range validation.
- `evidence/P-001/workflow-summary.json`: contains current `workflowControls` and refreshed prop diagnostics.
- `evidence/P-001/notes.md`: updated P-001 review notes.
- `docs/asset-evidence-progress.md`: regenerated production evidence report.
- `samples/asset-source-candidates.json`: route notes updated.

## Verification

- Command: focused RED pivot/origin tests before implementation
  - Result: failed as expected for missing keyword args, missing CLI args, and missing `workflow_controls_summary`.
- Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_prop_hinge_landmarks_accept_artist_pivot_and_origin_controls tests/test_blender_asset_workflow.py::test_landmarks_from_bbox_passes_prop_hinge_pivot_controls tests/test_blender_asset_workflow.py::test_parse_args_accepts_prop_hinge_pivot_and_origin_controls tests/test_blender_asset_workflow.py::test_parse_args_rejects_out_of_range_prop_hinge_controls tests/test_blender_asset_workflow.py::test_workflow_controls_summary_records_prop_hinge_pivot_values -q`
  - Result: passed, `5 passed in 0.01s`.
- Command: `python3 -m pytest tests/test_blender_asset_workflow.py -q`
  - Result: passed, `44 passed in 0.02s`.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/P-001/P-001-local-wooden-door.fbx --evidence-dir evidence/P-001 --summary evidence/P-001/workflow-summary.json --camera-axis x --template prop_hinge --prop-hinge-pivot-x 0.08 --prop-hinge-base-x 0.18`
  - Result: passed.
- Command: `scripts/verify_unity_fbx_import.sh --fbx evidence/P-001/export-unity.fbx --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240`
  - Result: passed, `{"status":"pass","assetPath":"Assets/MacGameRiggerImportCandidate/export-unity.fbx"}`.
- Command: `scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --output docs/asset-evidence-progress.md --check-evidence-files --evidence-root .`
  - Result: passed.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `145 passed in 10.83s`.
- Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --check-evidence-files --require-production-trial --quiet --output /tmp/mac-game-rigger-pivot-controls-pass.json`
  - Result: passed; production gate status `pass`, missing `[]`.
- Command: `python3 -m json.tool samples/asset-source-candidates.json >/tmp/asset-source-candidates.checked.json && python3 -m json.tool samples/manifest.json >/tmp/manifest.checked.json && python3 -m json.tool evidence/P-001/workflow-summary.json >/tmp/p001-workflow.checked.json`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.

## Open Questions / Risks

- P-001 remains internal-local evidence, not public CC0 coverage.
- Pivot/base placement now has CLI controls, but not Blender UI sliders or a visual gizmo.
- Prop diagnostics have semantic regions, but still do not enforce thresholds for weak base/hinge/moving coverage.
- The prop classifier still needs broader accessory/mechanical heuristics beyond thin door-like slabs.
- Unreal import remains blocked until an Unreal verifier exists.
- H-001 remains score 2 / visual fail and is the clearest registered quality gap.
- Worktree remains intentionally dirty with broad prior implementation/evidence changes; do not revert unrelated files.

## Next Steps

1. Add prop diagnostic thresholds/warnings for weak base/hinge/moving coverage.
2. Add Blender UI controls for prop hinge pivot/base values.
3. Add visual pivot/gizmo feedback for prop hinge placement.
4. Broaden accessory/mechanical classification beyond thin door-like slabs.
5. Improve or replace H-001 to reduce the main score-2 quality gap.

## Resume Prompt

Continue from this checkpoint. First read this file, then inspect `tools/blender_asset_workflow.py`, `evidence/P-001/workflow-summary.json`, `evidence/P-001/notes.md`, and `docs/asset-evidence-progress.md`. The next serious work is prop diagnostic thresholds/warnings for weak base/hinge/moving coverage, followed by Blender UI controls for pivot/base placement.
