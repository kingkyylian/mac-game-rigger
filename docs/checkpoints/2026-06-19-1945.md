# Project Checkpoint - 2026-06-19 19:45

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, HEAD `dd5f340 feat: add humanoid stress pose evidence`
- Project-local `AGENTS.md`: not present; use `/Users/kyylian` Codex workflow/preferences.
- Context: Continued from the 19:39 prop hinge pivot/base checkpoint. Production-trial gate still passes, and P-001 now has prop coverage threshold QA in workflow evidence and the generated progress report.

## Done This Session

- Followed the checkpoint next step: prop diagnostic thresholds/warnings for weak base/hinge/moving coverage.
- Used TDD for the behavior changes:
  - Added failing tests for `prop_diagnostics_summary(...)`.
  - Added pass coverage for balanced P-001-like `propBase` / `propHinge` / `propMovingPart` distribution.
  - Added warning coverage for weak hinge coverage.
  - Added failure coverage for missing moving-part coverage.
  - Added report coverage so `prop qa <status>` and warning codes appear in `docs/asset-evidence-progress.md`.
- Implemented `prop_diagnostics_summary(...)` in `tools/blender_asset_workflow.py`.
  - Non-prop templates return `{}`.
  - Prop hinge summaries include:
    - `status`
    - `coverageRatios`
    - `warnings`
  - Current warning/failure codes include:
    - `missingPropBaseCoverage`
    - `missingPropHingeCoverage`
    - `missingPropMovingPartCoverage`
    - `weakPropBaseCoverage`
    - `weakPropHingeCoverage`
    - `weakPropMovingPartCoverage`
- Workflow summary now writes `propDiagnostics` alongside `weightDiagnostics`.
- Updated `scripts/generate_asset_evidence_report.py` so the Weight cell appends prop QA state:
  - current P-001 report: `prop base 138 hinge 12 moving 210 prop qa pass`
  - warnings render as `prop qa warn <warningCode>`.
- Reran P-001 Blender workflow with explicit controls:
  - `--prop-hinge-pivot-x 0.08`
  - `--prop-hinge-base-x 0.18`
- P-001 current `propDiagnostics`:
  - status: `pass`
  - coverage ratios:
    - `propBase`: `0.3833`
    - `propHinge`: `0.0333`
    - `propMovingPart`: `0.5833`
  - warnings: none
- Reran Unity batchmode import for the refreshed P-001 export:
  - `evidence/P-001/export-unity.fbx` -> pass.
- Updated `evidence/P-001/notes.md`:
  - records prop threshold QA pass and ratios.
  - remaining risk is threshold tuning against more prop shapes.
- Regenerated `docs/asset-evidence-progress.md`.
- Updated `samples/asset-source-candidates.json`:
  - P-001 selection notes mention prop coverage threshold QA.
  - next action now focuses on UI/gizmo controls, threshold tuning, broader mechanical classification, and shaded/scale review.

## Current State

- Production-trial gate: `pass`
- Missing production-trial requirements: none
- Real assets registered: `11`
- Complete evidence entries: `11`
- Score >= 3 count: `10`
- Score >= 3 ratio: `0.909`
- Category counts:
  - humanoid: `7`
  - quadruped: `2`
  - tail creature: `1`
  - prop: `1`
- Unity import pass slots:
  - `H-003`
  - `H-004`
  - `Q-001`
  - `C-001`
  - `P-001`
- P-001 current evidence:
  - suggested category: `prop`
  - template: `prop_hinge`
  - CLI controls: pivot X `0.08`, base/origin X `0.18`
  - prop QA: `pass`
  - deformation score: `3`
  - visual review: `pass`
  - Unity import: `pass`
  - Unreal import: `blocked`

## Important Files / Artifacts

- `tools/blender_asset_workflow.py`: prop diagnostics summary generation and workflow summary output.
- `scripts/generate_asset_evidence_report.py`: prop QA report rendering.
- `tests/test_blender_asset_workflow.py`: RED/GREEN prop threshold coverage.
- `tests/test_generate_asset_evidence_report.py`: RED/GREEN prop QA report coverage.
- `evidence/P-001/workflow-summary.json`: contains current `propDiagnostics`.
- `evidence/P-001/notes.md`: updated P-001 threshold QA notes.
- `docs/asset-evidence-progress.md`: regenerated production evidence report with `prop qa pass`.
- `samples/asset-source-candidates.json`: route notes updated.

## Verification

- Command: focused RED prop threshold/report tests before implementation
  - Result: failed as expected for missing `prop_diagnostics_summary` and missing report rendering.
- Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_prop_diagnostics_summary_passes_balanced_prop_hinge_coverage tests/test_blender_asset_workflow.py::test_prop_diagnostics_summary_warns_for_weak_hinge_coverage tests/test_blender_asset_workflow.py::test_prop_diagnostics_summary_fails_when_moving_part_has_no_coverage -q`
  - Result: passed, `3 passed in 0.01s`.
- Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py::test_generate_asset_evidence_report_shows_prop_weight_region_summary tests/test_generate_asset_evidence_report.py::test_generate_asset_evidence_report_shows_prop_diagnostic_warnings -q`
  - Result: passed, `2 passed in 0.22s`.
- Command: `python3 -m pytest tests/test_blender_asset_workflow.py tests/test_generate_asset_evidence_report.py -q`
  - Result: passed, `57 passed in 0.32s`.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/P-001/P-001-local-wooden-door.fbx --evidence-dir evidence/P-001 --summary evidence/P-001/workflow-summary.json --camera-axis x --template prop_hinge --prop-hinge-pivot-x 0.08 --prop-hinge-base-x 0.18`
  - Result: passed.
- Command: `scripts/verify_unity_fbx_import.sh --fbx evidence/P-001/export-unity.fbx --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240`
  - Result: passed, `{"status":"pass","assetPath":"Assets/MacGameRiggerImportCandidate/export-unity.fbx"}`.
- Command: `scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --output docs/asset-evidence-progress.md --check-evidence-files --evidence-root .`
  - Result: passed.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `149 passed in 10.78s`.
- Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --check-evidence-files --require-production-trial --quiet --output /tmp/mac-game-rigger-prop-thresholds-pass.json`
  - Result: passed; production gate status `pass`, missing `[]`.
- Command: `python3 -m json.tool samples/asset-source-candidates.json >/tmp/asset-source-candidates.checked.json && python3 -m json.tool samples/manifest.json >/tmp/manifest.checked.json && python3 -m json.tool evidence/P-001/workflow-summary.json >/tmp/p001-workflow.checked.json`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.

## Open Questions / Risks

- P-001 remains internal-local evidence, not public CC0 coverage.
- Prop threshold QA exists, but thresholds are tuned only against one door-like prop; they need broader mechanical/accessory samples.
- Pivot/base placement has CLI controls, but not Blender UI sliders or a visual gizmo.
- The prop classifier still needs broader accessory/mechanical heuristics beyond thin door-like slabs.
- Unreal import remains blocked until an Unreal verifier exists.
- H-001 remains score 2 / visual fail and is the clearest registered quality gap.
- Worktree remains intentionally dirty with broad prior implementation/evidence changes; do not revert unrelated files.

## Next Steps

1. Add Blender UI controls for prop hinge pivot/base values.
2. Add visual pivot/gizmo feedback for prop hinge placement.
3. Broaden accessory/mechanical classification beyond thin door-like slabs.
4. Tune prop threshold QA against at least one more mechanical/accessory prop.
5. Improve or replace H-001 to reduce the main score-2 quality gap.

## Resume Prompt

Continue from this checkpoint. First read this file, then inspect `tools/blender_asset_workflow.py`, `addon/mac_game_rigger/ui/panels.py`, `addon/mac_game_rigger/ui/operators.py`, `evidence/P-001/workflow-summary.json`, and `docs/asset-evidence-progress.md`. The next serious work is Blender UI controls for prop hinge pivot/base values, followed by visual pivot/gizmo feedback.
