# Project Checkpoint - 2026-06-19 19:51

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: branch `main`, HEAD `dd5f340 feat: add humanoid stress pose evidence`
- Project-local `AGENTS.md`: not present; use `/Users/kyylian` Codex workflow/preferences.
- Context: Continued from the 19:45 prop threshold QA checkpoint. Production-trial gate still passes, and prop hinge pivot/base placement now has Blender UI sliders in addition to CLI workflow controls.

## Done This Session

- Followed the checkpoint next step: Blender UI controls for prop hinge pivot/base values.
- Used TDD for UI behavior:
  - Added `tests/test_blender_ui_prop_controls.py` with fake `bpy` and `mathutils` modules.
  - Added RED coverage for scene properties:
    - `mgr_prop_hinge_pivot_x`
    - `mgr_prop_hinge_base_x`
  - Added RED coverage for panel drawing:
    - prop_hinge template shows `Prop Hinge Controls`
    - panel draws pivot/base slider properties.
- Implemented scene properties in `addon/mac_game_rigger/ui/operators.py`:
  - `mgr_prop_hinge_pivot_x`
    - name: `Prop Hinge Pivot X`
    - default: `0.16`
    - range: `0.0..1.0`
  - `mgr_prop_hinge_base_x`
    - name: `Prop Hinge Base X`
    - default: `0.28`
    - range: `0.0..1.0`
- Updated `unregister_properties()` to remove both new scene properties.
- Updated `addon/mac_game_rigger/ui/panels.py`:
  - when `mgr_current_template == "prop_hinge"`, the main panel now shows `Prop Hinge Controls`.
  - panel draws both pivot/base sliders before landmark validation/generation controls.
- Ran real Blender headless registration smoke:
  - imported addon
  - registered addon
  - asserted both new scene properties exist on `bpy.types.Scene`
  - unregistered addon
- Updated P-001 route notes:
  - `evidence/P-001/notes.md` now says CLI controls and Blender UI sliders exist.
  - `samples/asset-source-candidates.json` now says P-001 has Blender UI sliders and still needs visual pivot/gizmo feedback.

## Current State

- Production-trial gate: `pass`
- Missing production-trial requirements: none
- Real assets registered: `11`
- Complete evidence entries: `11`
- Score >= 3 count: `10`
- Score >= 3 ratio: `0.909`
- Category counts:
  - humanoid: `7`
  - quadruped: `2`
  - tail creature: `1`
  - prop: `1`
- P-001 current evidence:
  - suggested category: `prop`
  - template: `prop_hinge`
  - CLI controls: pivot X `0.08`, base/origin X `0.18`
  - Blender UI controls: pivot/base sliders available
  - prop QA: `pass`
  - Unity import: `pass`
  - Unreal import: `blocked`

## Important Files / Artifacts

- `addon/mac_game_rigger/ui/operators.py`: registered/unregistered prop hinge pivot/base scene properties.
- `addon/mac_game_rigger/ui/panels.py`: draws prop hinge controls for `prop_hinge` template.
- `tests/test_blender_ui_prop_controls.py`: fake-`bpy` UI regression coverage.
- `evidence/P-001/notes.md`: updated P-001 note.
- `samples/asset-source-candidates.json`: route note updated.

## Verification

- Command: `python3 -m pytest tests/test_blender_ui_prop_controls.py -q`
  - Result: failed before implementation for missing scene properties and missing panel controls, then passed.
- Command: `python3 -m pytest tests/test_blender_ui_prop_controls.py tests/test_blender_asset_workflow.py -q`
  - Result: passed, `49 passed in 0.03s`.
- Command: `blender --background --factory-startup --python-expr "import sys; sys.path.insert(0, 'addon'); import mac_game_rigger; mac_game_rigger.register(); import bpy; assert hasattr(bpy.types.Scene, 'mgr_prop_hinge_pivot_x'); assert hasattr(bpy.types.Scene, 'mgr_prop_hinge_base_x'); mac_game_rigger.unregister(); print('ui_props_register_pass')"`
  - Result: passed, printed `ui_props_register_pass`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `151 passed in 10.88s`.
- Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --check-evidence-files --require-production-trial --quiet --output /tmp/mac-game-rigger-ui-controls-pass.json`
  - Result: passed; production gate status `pass`, missing `[]`.
- Command: `python3 -m json.tool samples/asset-source-candidates.json >/tmp/asset-source-candidates.checked.json && python3 -m json.tool samples/manifest.json >/tmp/manifest.checked.json`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.

## Open Questions / Risks

- P-001 remains internal-local evidence, not public CC0 coverage.
- Pivot/base placement has CLI controls and UI sliders, but not visual gizmo feedback.
- UI sliders are available in Blender; automated workflow still uses CLI args for headless evidence generation.
- Prop threshold QA exists, but thresholds are tuned only against one door-like prop.
- The prop classifier still needs broader accessory/mechanical heuristics beyond thin door-like slabs.
- Unreal import remains blocked until an Unreal verifier exists.
- H-001 remains score 2 / visual fail and is the clearest registered quality gap.
- Worktree remains intentionally dirty with broad prior implementation/evidence changes; do not revert unrelated files.

## Next Steps

1. Add visual pivot/gizmo feedback for prop hinge placement.
2. Add an operator to generate prop hinge landmarks from selected mesh using the UI slider values.
3. Broaden accessory/mechanical classification beyond thin door-like slabs.
4. Tune prop threshold QA against at least one more mechanical/accessory prop.
5. Improve or replace H-001 to reduce the main score-2 quality gap.

## Resume Prompt

Continue from this checkpoint. First read this file, then inspect `addon/mac_game_rigger/ui/operators.py`, `addon/mac_game_rigger/ui/panels.py`, `tests/test_blender_ui_prop_controls.py`, and `tools/blender_asset_workflow.py`. The next serious work is visual pivot/gizmo feedback for prop hinge placement, plus an operator that generates prop hinge landmarks from the selected mesh using UI slider values.
