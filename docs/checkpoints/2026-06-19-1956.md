# Checkpoint - 2026-06-19 19:56 +03

## Context

- Workspace: `/Users/kyylian/mac-game-rigger`
- Branch/worktree: dirty by design; do not revert unrelated files.
- Product direction: standalone Mac-first game rigging tool, not only a UniRig port.
- Current focus: close the practical gaps that block fast game-ready rigging on Mac hardware.

## Completed Since Previous Checkpoint

- Added selected-mesh prop hinge landmark generation in the Blender add-on UI.
- Added `MGR_OT_generate_prop_hinge_landmarks`.
- Added helper coverage for:
  - `prop_hinge_landmarks_from_bbox`
  - `mesh_bbox`
  - `create_or_update_landmark`
- Added a Prop Hinge panel action:
  - `Generate Prop Hinge Landmarks`
- The operator reads existing UI slider values:
  - `mgr_prop_hinge_pivot_x`
  - `mgr_prop_hinge_base_x`
- The operator creates or updates:
  - `MGR_Landmark_base`
  - `MGR_Landmark_hinge`
  - `MGR_Landmark_moving_part`
  - `MGR_Landmark_moving_tip`
- Updated P-001 route notes to reflect that prop hinge now has:
  - CLI controls
  - Blender UI sliders
  - selected-mesh landmark generation

## Current Rigging Success State

- Production-trial gate: pass.
- Real assets registered: 11.
- Complete evidence entries: 11.
- Score >= 3 assets: 10.
- Score >= 3 ratio: 0.909.
- Category coverage:
  - Humanoid: 7
  - Quadruped: 2
  - Tail creature: 1
  - Prop: 1
- Unity import pass slots:
  - H-003
  - H-004
  - Q-001
  - C-001
  - P-001

## P-001 Prop Status

- Asset: local wooden door FBX.
- Template: `prop_hinge`.
- QA: pass.
- Unity import: pass.
- Unreal: blocked.
- Current capability:
  - hinge pivot/base can be driven from CLI
  - hinge pivot/base can be adjusted from Blender UI
  - selected mesh can generate prop hinge landmarks automatically
- Remaining gap:
  - visual pivot/gizmo feedback is still missing.

## Verification

- Focused UI test:
  - `python3 -m pytest tests/test_blender_ui_prop_controls.py -q`
  - Result: 4 passed.
- Real Blender operator smoke:
  - Result: `prop_landmark_operator_pass`.
- Full test suite:
  - `python3 -m pytest tests -q`
  - Result: 153 passed.
- Evidence validator:
  - `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --check-evidence-files --require-production-trial --quiet --output /tmp/mac-game-rigger-prop-landmark-operator-pass.json`
  - Result: pass.
- JSON checks:
  - `samples/asset-source-candidates.json`: pass.
  - `samples/manifest.json`: pass.
- Whitespace/diff hygiene:
  - `rtk git diff --check`: pass.

## Remaining Risks

- Visual pivot/gizmo feedback is still absent; users can generate landmarks, but cannot yet visually drag/inspect hinge placement in a polished way.
- P-001 is an internal/local asset, not a public CC0/reference-grade benchmark.
- Prop QA thresholds are validated against too few prop shapes.
- Accessory/mechanical classification is still narrow.
- H-001 remains the weak asset slot at score 2.
- Unreal validation remains blocked.

## Next Route

1. Add visual pivot/gizmo feedback for prop hinge placement.
2. Add another prop/mechanical/accessory sample to tune prop QA thresholds.
3. Broaden classifier beyond thin slab-like props.
4. Improve or replace H-001.
5. Add Unreal verifier or document the formal blocker with reproducible commands.
