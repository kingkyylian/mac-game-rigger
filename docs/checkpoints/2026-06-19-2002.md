# Checkpoint - 2026-06-19 20:02 +03

## Context

- Workspace: `/Users/kyylian/mac-game-rigger`
- Branch/worktree: dirty by design; do not revert unrelated files.
- Product direction: standalone Mac-first game rigging tool for fast game-ready rigging.
- Current focus: reduce the remaining artist-facing prop hinge setup gaps.

## Completed Since Previous Checkpoint

- Added Empty-based visual hinge guide support for prop hinge placement.
- Added `prop_hinge_visual_guides_from_bbox`.
- Added `MGR_OT_refresh_prop_hinge_visual_guides`.
- Updated `MGR_OT_generate_prop_hinge_landmarks` so landmark generation also creates visual guides.
- Added four visual guide objects:
  - `MGR_Gizmo_prop_hinge_axis_bottom`
  - `MGR_Gizmo_prop_hinge_axis_top`
  - `MGR_Gizmo_prop_hinge_pivot`
  - `MGR_Gizmo_prop_hinge_swing_tip`
- Added panel action:
  - `Refresh Hinge Visual Guides`
- Added TDD coverage in `tests/test_blender_ui_prop_controls.py`.
- Updated P-001 route notes:
  - P-001 now has CLI controls, Blender UI sliders, selected-mesh landmark generation, and Empty-based hinge visual guides.
  - Remaining UX gap is now a polished draggable pivot gizmo, not total absence of visual feedback.

## Current Rigging Success State

- Production-trial gate: pass.
- Real assets registered: 11.
- Complete evidence entries: 11.
- Score >= 3 assets: 10.
- Score >= 3 ratio: 0.909.
- Category coverage:
  - Humanoid: 7
  - Quadruped: 2
  - Tail creature: 1
  - Prop: 1
- Unity import pass slots:
  - H-003
  - H-004
  - Q-001
  - C-001
  - P-001

## P-001 Prop Status

- Asset: local wooden door FBX.
- Template: `prop_hinge`.
- QA: pass.
- Unity import: pass.
- Unreal: blocked.
- Current capability:
  - hinge pivot/base can be driven from CLI
  - hinge pivot/base can be adjusted from Blender UI
  - selected mesh can generate prop hinge landmarks automatically
  - selected mesh can create/refresh viewport-visible hinge guide Empty objects
- Remaining gap:
  - visual guide exists, but it is not yet a polished draggable pivot gizmo.

## Verification

- RED test:
  - `python3 -m pytest tests/test_blender_ui_prop_controls.py -q`
  - Result before implementation: 2 failed, 3 passed.
  - Missing helper/operator failures confirmed the intended gap.
- Focused UI test:
  - `python3 -m pytest tests/test_blender_ui_prop_controls.py -q`
  - Result: 5 passed.
- Real Blender generate smoke:
  - `blender --background --factory-startup --python-expr "... bpy.ops.mgr.generate_prop_hinge_landmarks() ..."`
  - Result: `prop_hinge_visual_guides_pass`.
- Real Blender refresh smoke:
  - `blender --background --factory-startup --python-expr "... bpy.ops.mgr.refresh_prop_hinge_visual_guides() ..."`
  - Result: `prop_hinge_refresh_visual_guides_pass`.
- Full test suite:
  - `python3 -m pytest tests -q`
  - Result: 154 passed.
- Evidence validator:
  - `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --check-evidence-files --require-production-trial --quiet --output /tmp/mac-game-rigger-visual-guides-validator.json`
  - Result: pass.
- JSON checks:
  - `python3 -m json.tool samples/asset-source-candidates.json`
  - Result: pass.
- Whitespace/diff hygiene:
  - `git diff --check`
  - Result: pass.

## Remaining Risks

- Empty-based hinge guides are useful visual feedback, but they are not yet a draggable/custom viewport gizmo.
- P-001 is an internal/local asset, not a public CC0/reference-grade benchmark.
- Prop QA thresholds are validated against too few prop shapes.
- Accessory/mechanical classification is still narrow.
- H-001 remains the weak asset slot at score 2.
- Unreal validation remains blocked.

## Next Route

1. Tune prop threshold QA against another mechanical/accessory prop shape.
2. Broaden accessory/mechanical classification beyond thin door-like slabs.
3. Replace the Empty-based hinge guide with a polished draggable pivot gizmo.
4. Improve or replace H-001.
5. Add Unreal verifier or document the formal blocker with reproducible commands.
