# Checkpoint - 2026-06-19 21:29 +03

## Context

- Workspace: `/Users/kyylian/mac-game-rigger`
- Branch/worktree: dirty by design; do not revert unrelated files.
- Product direction: standalone Mac-first game rigging tool for fast game-ready rigging.
- Current focus: improve prop hinge authoring for chest-like props after adding P-002.

## Completed Since Previous Checkpoint

- Added prop hinge layout axis control.
- Batch workflow now accepts:
  - `--prop-hinge-axis x`
  - `--prop-hinge-axis y`
- Default remains `x`, preserving P-001 door behavior.
- `prop_hinge_landmarks_from_bbox` now supports `layout_axis="y"` for chest-like or depth-oriented props.
- `landmarks_from_bbox` passes the prop hinge axis into prop landmark generation.
- `workflow_controls_summary` now records:
  - `pivotX`
  - `baseX`
  - `axis`
- Blender UI now registers and draws:
  - `mgr_prop_hinge_axis`
- UI prop landmark generation and visual guide refresh now read `mgr_prop_hinge_axis`.
- P-002 evidence was rerun with:
  - `--prop-hinge-axis y`
- P-002 workflow summary now records:
  - `workflowControls.propHinge.axis = "y"`
- P-002 Unity import was rerun after the axis-y export and passed.
- P-002 notes and route notes were updated to reflect text-based hinge axis control.
- `docs/asset-evidence-progress.md` was regenerated.

## Current Rigging Success State

- Production-trial gate: pass.
- Real assets registered: 12.
- Complete evidence entries: 12.
- Score >= 3 assets: 11.
- Score >= 3 ratio: 0.917.
- Category coverage:
  - Humanoid: 7
  - Quadruped: 2
  - Tail creature: 1
  - Prop: 2
- Unity import pass slots:
  - H-003
  - H-004
  - Q-001
  - C-001
  - P-001
  - P-002

## P-002 Prop Status

- Asset: local treasure chest FBX.
- Template: `prop_hinge`.
- Hinge controls:
  - pivot: `0.10`
  - base/origin: `0.22`
  - layout axis: `y`
- QA: pass.
- Prop diagnostics: pass.
- Current prop coverage:
  - `propBase`: 64 dominant vertices
  - `propHinge`: 2 dominant vertices
  - `propMovingPart`: 52 dominant vertices
- Pose deformation: pass, max expansion `1.4359x`, allowed axis `y`.
- Unity import: pass.
- Unreal: blocked.
- Visual score: 3, usable-with-cleanup.

## Verification

- RED tests:
  - `python3 -m pytest tests/test_blender_asset_workflow.py tests/test_blender_ui_prop_controls.py -q`
  - Result before implementation: 7 failed, 49 passed.
  - Failures were missing axis API, missing CLI arg, missing UI property, and missing y-axis landmark behavior.
- Focused tests:
  - `python3 -m pytest tests/test_blender_asset_workflow.py tests/test_blender_ui_prop_controls.py -q`
  - Result: 56 passed.
- Real Blender UI smoke:
  - `bpy.ops.mgr.generate_prop_hinge_landmarks()` with `mgr_prop_hinge_axis='y'`
  - Result: `prop_hinge_ui_y_axis_pass`.
- P-002 workflow:
  - `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/P-002/P-002-local-treasure-chest.fbx --evidence-dir evidence/P-002 --summary evidence/P-002/workflow-summary.json --camera-axis y --template prop_hinge --prop-hinge-pivot-x 0.10 --prop-hinge-base-x 0.22 --prop-hinge-axis y`
  - Result: pass.
- P-002 Unity import:
  - `scripts/verify_unity_fbx_import.sh --fbx evidence/P-002/export-unity.fbx --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240`
  - Result: pass.
- Full test suite:
  - `python3 -m pytest tests -q`
  - Result: 160 passed.
- Evidence validator:
  - `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --check-evidence-files --require-production-trial --quiet --output /tmp/mac-game-rigger-hinge-axis-validator.json`
  - Result: pass.
- JSON checks:
  - `samples/asset-source-candidates.json`: pass.
  - `evidence/P-002/workflow-summary.json`: pass.
- Whitespace/diff hygiene:
  - `git diff --check`
  - Result: pass.

## Remaining Risks

- `mgr_prop_hinge_axis` is currently a text property, not a polished segmented/dropdown control.
- Axis control chooses landmark layout along X/Y, but it is not yet a full custom hinge rotation-plane system.
- Empty-based hinge guides are useful visual feedback, but they are not yet a draggable/custom viewport gizmo.
- P-001 and P-002 are internal-local prop evidence, not public CC0 prop coverage.
- H-001 remains the weak asset slot at score 2.
- Unreal validation remains blocked.

## Next Route

1. Replace text-based `mgr_prop_hinge_axis` with a proper UI enum/dropdown or segmented control.
2. Add polished hinge orientation UX, including clearer visual guide labels for X/Y layouts.
3. Validate prop QA on another real mechanical shape, such as gate, lamp, platform, crate stack, or mechanical panel.
4. Improve or replace H-001.
5. Add Unreal verifier or document the formal blocker with reproducible commands.
