# Checkpoint - 2026-06-19 21:35 +03

## Context

- Workspace: `/Users/kyylian/mac-game-rigger`
- Branch/worktree: dirty by design; do not revert unrelated files.
- Product direction: standalone Mac-first game rigging tool for fast game-ready game rigs.
- Current focus: improve prop hinge authoring UX after adding P-002 chunky treasure chest evidence.

## Completed Since Previous Checkpoint

- Replaced the Blender UI `mgr_prop_hinge_axis` free-text scene property with a proper enum/dropdown.
- Added `PROP_HINGE_AXIS_ITEMS` with:
  - `x`: X Axis
  - `y`: Y Axis
- Kept existing axis normalization and operator behavior intact.
- Updated the UI property registration test to require `EnumProperty` instead of `StringProperty`.
- Verified the RED failure before implementation:
  - expected enum/dropdown
  - actual code still returned `StringProperty`
- Verified Blender runtime registration through RNA:
  - `mgr_prop_hinge_axis` registers as type `ENUM`
  - enum identifiers are `x` and `y`
  - assigning `bpy.context.scene.mgr_prop_hinge_axis = "y"` works
- Updated route notes:
  - `samples/asset-source-candidates.json`
  - `evidence/P-002/notes.md`

## Current Rigging Success State

- Production-trial gate: pass.
- Real assets registered: 12.
- Complete evidence entries: 12.
- Score >= 3 assets: 11.
- Score >= 3 ratio: 0.917.
- Category coverage:
  - Humanoid: 7
  - Quadruped: 2
  - Tail creature: 1
  - Prop: 2
- Unity import pass slots:
  - H-003
  - H-004
  - Q-001
  - C-001
  - P-001
  - P-002

## Verification

- RED test:
  - Command: `python3 -m pytest tests/test_blender_ui_prop_controls.py -q`
  - Result before implementation: 1 failed, 5 passed.
  - Failure reason: `mgr_prop_hinge_axis` was still `StringProperty`, while the test required `EnumProperty`.
- Focused test:
  - Command: `python3 -m pytest tests/test_blender_ui_prop_controls.py -q`
  - Result: 6 passed.
- Blender enum smoke:
  - Command: `blender --background --factory-startup --python-expr "..."`
  - Result: `prop_hinge_axis_enum_pass`.
- Full test suite:
  - Command: `python3 -m pytest tests -q`
  - Result: 160 passed.
- Evidence validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --check-evidence-files --require-production-trial --quiet --output /tmp/mac-game-rigger-axis-enum-validator.json`
  - Result: pass.
  - Summary: 12 real assets, 12 complete evidence, production gate pass, 11 score>=3, ratio 0.917.
- JSON checks:
  - `python3 -m json.tool samples/asset-source-candidates.json`: pass.
  - `python3 -m json.tool evidence/P-002/workflow-summary.json`: pass.
- Whitespace/diff hygiene:
  - Command: `git diff --check`
  - Result: pass.

## Important Files / Artifacts

- `addon/mac_game_rigger/ui/operators.py`: `mgr_prop_hinge_axis` is now registered as a Blender `EnumProperty`.
- `tests/test_blender_ui_prop_controls.py`: regression coverage for the axis enum/dropdown.
- `samples/asset-source-candidates.json`: route now records that the text-based control has been replaced.
- `evidence/P-002/notes.md`: notes now say the axis is artist-adjustable via enum/dropdown.
- `/tmp/mac-game-rigger-axis-enum-validator.json`: latest validator evidence.

## Remaining Risks

- Axis enum only chooses X/Y landmark layout; it is not yet a full hinge rotation-plane/orientation system.
- Empty-based hinge visual guides are useful but still not a draggable viewport gizmo.
- P-001 and P-002 are internal-local prop evidence, not public CC0 prop coverage.
- H-001 remains the weak evidence slot at deformation score 2.
- Unreal validation remains blocked.
- Direct shaded Blender review and scale/orientation notes are still needed for score-3 assets before stronger production claims.

## Next Route

1. Build polished hinge orientation UX beyond coarse X/Y axis selection.
2. Replace Empty-based hinge guides with a draggable pivot/rotation gizmo or equivalent artist control.
3. Validate prop QA on another real mechanical shape such as gate, lamp, platform, crate stack, or mechanical panel.
4. Improve or replace H-001.
5. Add Unreal verifier or document the formal blocker with reproducible commands.

## Resume Prompt

Continue from this checkpoint. First read this file and project rules, then inspect `addon/mac_game_rigger/ui/operators.py`, `tests/test_blender_ui_prop_controls.py`, `samples/asset-source-candidates.json`, and `/tmp/mac-game-rigger-axis-enum-validator.json` before making changes.
