# Checkpoint - 2026-06-19 23:40 +03

## Context

- Workspace: `/Users/kyylian/mac-game-rigger`
- Branch/worktree: dirty by design; do not revert unrelated files.
- Product direction: standalone Mac-first game rigging tool for fast game-ready game rigs.
- Current focus: improve prop hinge authoring UX after adding P-002 chunky treasure chest evidence.

## Completed Since Previous Checkpoint

- Added artist-facing prop hinge pose controls.
- Core pose function `apply_prop_hinge_open_pose(...)` now supports:
  - `open_angle`
  - `moving_part_angle`
  - `swing_direction`
- Default prop hinge pose remains backward-compatible:
  - Hinge: `0.65` radians on Z
  - MovingPart: `0.18` radians on Z
- New Blender UI scene controls:
  - `mgr_prop_hinge_open_angle`
  - `mgr_prop_hinge_swing_direction`
- Swing direction options:
  - `positive`: Positive Z
  - `negative`: Negative Z
- `mgr.pose_prop_hinge_open` now reads the UI controls and applies the selected open angle and direction.
- Added panel controls under the prop hinge section so artists can tune pose review without editing code.
- Added regression tests for:
  - default pose compatibility
  - custom angle + negative swing direction
  - UI property registration and unregister cleanup
  - panel drawing the new controls
- Updated route/evidence notes:
  - `samples/asset-source-candidates.json`
  - `evidence/P-001/notes.md`
  - `evidence/P-002/notes.md`

## Current Rigging Success State

- Production-trial gate: pass.
- Real assets registered: 12.
- Complete evidence entries: 12.
- Score >= 3 assets: 11.
- Score >= 3 ratio: 0.917.
- Category coverage:
  - Humanoid: 7
  - Quadruped: 2
  - Tail creature: 1
  - Prop: 2
- Unity import pass slots:
  - H-003
  - H-004
  - Q-001
  - C-001
  - P-001
  - P-002

## Verification

- RED test:
  - Command: `python3 -m pytest tests/test_pose_tests.py tests/test_blender_ui_prop_controls.py -q`
  - Result before implementation: 3 failed, 15 passed.
  - Failure reasons:
    - `apply_prop_hinge_open_pose` did not accept `open_angle`
    - missing `mgr_prop_hinge_open_angle`
    - panel did not draw the new controls
- Focused test:
  - Command: `python3 -m pytest tests/test_pose_tests.py tests/test_blender_ui_prop_controls.py -q`
  - Result: 18 passed.
- Blender runtime smoke:
  - Command: `blender --background --factory-startup --python-expr "..."`
  - Result: `prop_hinge_artist_pose_controls_pass`.
  - Verified `mgr_prop_hinge_open_angle=0.9` and `mgr_prop_hinge_swing_direction='negative'` produce negative Z hinge rotation.
- Full test suite:
  - Command: `python3 -m pytest tests -q`
  - Result: 162 passed.
- Evidence validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --check-evidence-files --require-production-trial --quiet --output /tmp/mac-game-rigger-hinge-pose-controls-validator.json`
  - Result: pass.
  - Summary: 12 real assets, 12 complete evidence, production gate pass, 11 score>=3, ratio 0.917.
- JSON checks:
  - `python3 -m json.tool samples/asset-source-candidates.json`: pass.
- Whitespace/diff hygiene:
  - Command: `git diff --check`
  - Result: pass.

## Important Files / Artifacts

- `addon/mac_game_rigger/core/pose_tests.py`: prop hinge open pose now accepts artist-controlled angle/direction.
- `addon/mac_game_rigger/ui/operators.py`: Blender operator wiring reads the scene pose controls.
- `addon/mac_game_rigger/ui/panels.py`: prop hinge panel exposes pose controls.
- `tests/test_pose_tests.py`: regression coverage for custom angle and negative swing direction.
- `tests/test_blender_ui_prop_controls.py`: UI registration/panel coverage for pose controls.
- `samples/asset-source-candidates.json`: route notes updated with pose-control status.
- `evidence/P-001/notes.md`: P-001 notes updated.
- `evidence/P-002/notes.md`: P-002 notes updated.
- `/tmp/mac-game-rigger-hinge-pose-controls-validator.json`: latest validator evidence.

## Remaining Risks

- Pose direction still uses coarse positive/negative Z; it is not a full hinge rotation-plane/orientation system.
- Empty guide commit is useful, but it is still not a polished custom Blender gizmo.
- P-001 and P-002 are internal-local prop evidence, not public CC0 prop coverage.
- H-001 remains the weak evidence slot at deformation score 2.
- Unreal validation remains blocked.
- Direct shaded Blender review and scale/orientation notes are still needed for score-3 assets before stronger production claims.

## Next Route

1. Build polished hinge orientation UX beyond coarse axis/direction selection.
2. Replace Empty guide workflow with a custom pivot/rotation gizmo or richer Blender viewport control.
3. Validate prop QA on another real mechanical shape such as gate, lamp, platform, crate stack, or mechanical panel.
4. Improve or replace H-001.
5. Add Unreal verifier or document the formal blocker with reproducible commands.

## Resume Prompt

Continue from this checkpoint. First read this file and project rules, then inspect `addon/mac_game_rigger/core/pose_tests.py`, `addon/mac_game_rigger/ui/operators.py`, `addon/mac_game_rigger/ui/panels.py`, `tests/test_pose_tests.py`, `tests/test_blender_ui_prop_controls.py`, and `/tmp/mac-game-rigger-hinge-pose-controls-validator.json` before making changes.
