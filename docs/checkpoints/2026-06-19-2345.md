# Checkpoint - 2026-06-19 23:45 +03

## Context

- Workspace: `/Users/kyylian/mac-game-rigger`
- Branch/worktree: dirty by design; do not revert unrelated files.
- Product direction: standalone Mac-first game rigging tool for fast game-ready game rigs.
- Current focus: improve prop hinge authoring UX after adding P-002 chunky treasure chest evidence.

## Completed Since Previous Checkpoint

- Added artist-selectable prop hinge pose rotation axis.
- Core pose function `apply_prop_hinge_open_pose(...)` now supports:
  - `open_angle`
  - `moving_part_angle`
  - `swing_direction`
  - `rotation_axis`
- Default behavior remains backward-compatible:
  - default axis: `z`
  - Hinge: `(0.0, 0.0, 0.65)`
  - MovingPart: `(0.0, 0.0, 0.18)`
- New Blender UI scene control:
  - `mgr_prop_hinge_rotation_axis`
- Rotation axis options:
  - `x`: X Rotation
  - `y`: Y Rotation
  - `z`: Z Rotation
- `mgr.pose_prop_hinge_open` now reads:
  - open angle
  - swing direction
  - rotation axis
- Added panel control under the prop hinge section.
- Added regression tests for:
  - custom rotation axis on the core pose function
  - UI property registration and unregister cleanup
  - panel drawing the rotation axis control
- Updated route/evidence notes:
  - `samples/asset-source-candidates.json`
  - `evidence/P-001/notes.md`
  - `evidence/P-002/notes.md`

## Current Rigging Success State

- Production-trial gate: pass.
- Real assets registered: 12.
- Complete evidence entries: 12.
- Score >= 3 assets: 11.
- Score >= 3 ratio: 0.917.
- Category coverage:
  - Humanoid: 7
  - Quadruped: 2
  - Tail creature: 1
  - Prop: 2
- Unity import pass slots:
  - H-003
  - H-004
  - Q-001
  - C-001
  - P-001
  - P-002

## Verification

- RED test:
  - Command: `python3 -m pytest tests/test_pose_tests.py tests/test_blender_ui_prop_controls.py -q`
  - Result before implementation: 3 failed, 16 passed.
  - Failure reasons:
    - `apply_prop_hinge_open_pose` did not accept `rotation_axis`
    - missing `mgr_prop_hinge_rotation_axis`
    - panel did not draw the new control
- Focused test:
  - Command: `python3 -m pytest tests/test_pose_tests.py tests/test_blender_ui_prop_controls.py -q`
  - Result: 19 passed.
- Blender runtime smoke:
  - Command: `blender --background --factory-startup --python-expr "..."`
  - Result: `prop_hinge_rotation_axis_pose_pass`.
  - Verified `rotation_axis='x'`, `swing_direction='negative'`, and `open_angle=0.8` produce negative X hinge rotation.
- Full test suite:
  - Command: `python3 -m pytest tests -q`
  - Result: 163 passed.
- Evidence validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --check-evidence-files --require-production-trial --quiet --output /tmp/mac-game-rigger-hinge-rotation-axis-validator.json`
  - Result: pass.
  - Summary: 12 real assets, 12 complete evidence, production gate pass, 11 score>=3, ratio 0.917.
- JSON checks:
  - `python3 -m json.tool samples/asset-source-candidates.json`: pass.
- Whitespace/diff hygiene:
  - Command: `git diff --check`
  - Result: pass.

## Important Files / Artifacts

- `addon/mac_game_rigger/core/pose_tests.py`: prop hinge open pose now accepts selected rotation axis.
- `addon/mac_game_rigger/ui/operators.py`: Blender operator wiring reads the scene rotation axis.
- `addon/mac_game_rigger/ui/panels.py`: prop hinge panel exposes rotation axis.
- `tests/test_pose_tests.py`: regression coverage for custom rotation axis.
- `tests/test_blender_ui_prop_controls.py`: UI registration/panel coverage for rotation axis.
- `samples/asset-source-candidates.json`: route notes updated with rotation-axis status.
- `evidence/P-001/notes.md`: P-001 notes updated.
- `evidence/P-002/notes.md`: P-002 notes updated.
- `/tmp/mac-game-rigger-hinge-rotation-axis-validator.json`: latest validator evidence.

## Remaining Risks

- Rotation axis selection is still a coarse axis control, not a polished custom Blender pivot/rotation gizmo.
- Empty guide commit is useful, but it is still not a custom viewport gizmo.
- P-001 and P-002 are internal-local prop evidence, not public CC0 prop coverage.
- H-001 remains the weak evidence slot at deformation score 2.
- Unreal validation remains blocked.
- Direct shaded Blender review and scale/orientation notes are still needed for score-3 assets before stronger production claims.

## Next Route

1. Build polished hinge orientation UX beyond coarse axis/direction selection.
2. Replace Empty guide workflow with a custom pivot/rotation gizmo or richer Blender viewport control.
3. Validate prop QA on another real mechanical shape such as gate, lamp, platform, crate stack, or mechanical panel.
4. Improve or replace H-001.
5. Add Unreal verifier or document the formal blocker with reproducible commands.

## Resume Prompt

Continue from this checkpoint. First read this file and project rules, then inspect `addon/mac_game_rigger/core/pose_tests.py`, `addon/mac_game_rigger/ui/operators.py`, `addon/mac_game_rigger/ui/panels.py`, `tests/test_pose_tests.py`, `tests/test_blender_ui_prop_controls.py`, and `/tmp/mac-game-rigger-hinge-rotation-axis-validator.json` before making changes.
