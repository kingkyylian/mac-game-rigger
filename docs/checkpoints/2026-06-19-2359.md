# Checkpoint - 2026-06-19 23:59 +03

## Context

- Workspace: `/Users/kyylian/mac-game-rigger`
- Branch/worktree: dirty by design; do not revert unrelated files.
- Product direction: standalone Mac-first game rigging tool for fast game-ready game rigs.
- Current focus: make prop hinge viewport gizmo infrastructure more interactive.

## Completed Since Previous Checkpoint

- Upgraded the prop hinge GizmoGroup from a single passive marker to bound pivot/swing-tip axis handles.
- `MGR_GGT_prop_hinge_orientation.setup(...)` now creates six `GIZMO_GT_arrow_3d` handles:
  - pivot X/Y/Z
  - swing_tip X/Y/Z
- `draw_prepare(...)` now binds each handle to guide object locations:
  - `MGR_Gizmo_prop_hinge_pivot.location[0..2]`
  - `MGR_Gizmo_prop_hinge_swing_tip.location[0..2]`
- Handles are stored under:
  - `self._guide_handles["pivot"]["x"|"y"|"z"]`
  - `self._guide_handles["swing_tip"]["x"|"y"|"z"]`
- The gizmo still anchors each handle to the corresponding guide object's matrix.
- Added regression coverage with fake Blender gizmos for:
  - six arrow handles created
  - pivot/swing-tip handle groups
  - `target_set_prop("offset", guide, "location", index=axis_index)` bindings
  - matrix anchoring for pivot and swing-tip handles
- Updated route/evidence notes:
  - `samples/asset-source-candidates.json`
  - `evidence/P-001/notes.md`
  - `evidence/P-002/notes.md`

## Current Rigging Success State

- Production-trial gate: pass.
- Real assets registered: 12.
- Complete evidence entries: 12.
- Score >= 3 assets: 11.
- Score >= 3 ratio: 0.917.
- Category coverage:
  - Humanoid: 7
  - Quadruped: 2
  - Tail creature: 1
  - Prop: 2
- Unity import pass slots:
  - H-003
  - H-004
  - Q-001
  - C-001
  - P-001
  - P-002

## Verification

- RED test:
  - Command: `python3 -m pytest tests/test_blender_ui_prop_controls.py -q`
  - Result before implementation: 1 failed, 8 passed.
  - Failure reason: GizmoGroup created one marker instead of six pivot/swing-tip axis handles.
- Focused test:
  - Command: `python3 -m pytest tests/test_blender_ui_prop_controls.py -q`
  - Result: 9 passed.
- Blender registration smoke:
  - Command: `blender --background --factory-startup --python-expr "..."`
  - Result: `prop_hinge_gizmo_handle_binding_registration_pass`.
  - Verified addon register/unregister, prop_hinge poll, and guide object creation do not error.
- Full test suite:
  - Command: `python3 -m pytest tests -q`
  - Result: 165 passed.
- Evidence validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --check-evidence-files --require-production-trial --quiet --output /tmp/mac-game-rigger-gizmo-handle-binding-validator.json`
  - Result: pass.
  - Summary: 12 real assets, 12 complete evidence, production gate pass, 11 score>=3, ratio 0.917.
- JSON checks:
  - `python3 -m json.tool samples/asset-source-candidates.json`: pass.
- Whitespace/diff hygiene:
  - Command: `git diff --check`
  - Result: pass.

## Important Files / Artifacts

- `addon/mac_game_rigger/ui/operators.py`: GizmoGroup now creates pivot/swing-tip axis handles with target bindings.
- `tests/test_blender_ui_prop_controls.py`: regression coverage for handle creation and bindings.
- `samples/asset-source-candidates.json`: route notes updated with handle-binding status.
- `evidence/P-001/notes.md`: P-001 notes updated.
- `evidence/P-002/notes.md`: P-002 notes updated.
- `/tmp/mac-game-rigger-gizmo-handle-binding-validator.json`: latest validator evidence.

## Remaining Risks

- Handle binding is unit-tested and registration-smoked, but polished interaction still needs a real viewport QA pass.
- Empty guide commit remains the reliable workflow for applying moved guides to rig landmarks.
- P-001 and P-002 are internal-local prop evidence, not public CC0 prop coverage.
- H-001 remains the weak evidence slot at deformation score 2.
- Unreal validation remains blocked.
- Direct shaded Blender review and scale/orientation notes are still needed for score-3 assets before stronger production claims.

## Next Route

1. Add a real interactive Blender viewport QA/smoke path for the prop hinge GizmoGroup, if feasible headlessly or with scripted GUI evidence.
2. Validate prop QA on another real mechanical shape such as gate, lamp, platform, crate stack, or mechanical panel.
3. Improve or replace H-001.
4. Add Unreal verifier or document the formal blocker with reproducible commands.

## Resume Prompt

Continue from this checkpoint. First read this file and project rules, then inspect `addon/mac_game_rigger/ui/operators.py`, `tests/test_blender_ui_prop_controls.py`, and `/tmp/mac-game-rigger-gizmo-handle-binding-validator.json` before making changes.
