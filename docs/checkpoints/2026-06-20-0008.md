# Project Checkpoint - 2026-06-20 00:08 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Context: standalone Mac-first game rigger; current focus is making prop hinge rigging evidence less manual and more repeatable.

## Done This Session

- Added `scripts/verify_prop_hinge_gizmo_workflow.py`.
- Added TDD coverage in `tests/test_prop_hinge_gizmo_workflow_smoke.py`.
- Verified the new script has no top-level Blender dependency for normal Python tests.
- Script now supports:
  - `--output`
  - `--quiet`
  - Blender `--` separator argument extraction
  - executable `--help`
- Script runs inside Blender background and produces JSON evidence for:
  - addon registration
  - prop_hinge-only GizmoGroup poll behavior
  - prop hinge landmark and visual guide generation
  - pivot/swing-tip guide object creation
  - simulated guide move
  - commit back to landmarks
  - committed landmarks matching moved guide coordinates
  - prop hinge armature generation
  - hinge-open pose
- Made the new script executable to match existing script conventions.
- Updated route/evidence notes:
  - `samples/asset-source-candidates.json`
  - `evidence/P-001/notes.md`
  - `evidence/P-002/notes.md`

## Current Rigging Success State

- Production-trial gate: pass.
- Real assets registered: 12.
- Complete evidence entries: 12.
- Score >= 3 assets: 11.
- Score >= 3 ratio: 0.917.
- Category coverage:
  - Humanoid: 7
  - Quadruped: 2
  - Tail creature: 1
  - Prop: 2
- Unity import pass slots:
  - H-003
  - H-004
  - Q-001
  - C-001
  - P-001
  - P-002

## Verification

- RED test:
  - Command: `python3 -m pytest tests/test_prop_hinge_gizmo_workflow_smoke.py -q`
  - Result before implementation: 3 failed.
  - Failure reason: `scripts/verify_prop_hinge_gizmo_workflow.py` did not exist.
- Focused test:
  - Command: `python3 -m pytest tests/test_prop_hinge_gizmo_workflow_smoke.py -q`
  - Result: 3 passed.
- Full test suite:
  - Command: `python3 -m pytest tests -q`
  - Result: 168 passed.
- Blender workflow smoke:
  - Command: `blender --background --factory-startup --python scripts/verify_prop_hinge_gizmo_workflow.py -- --output /tmp/mac-game-rigger-prop-hinge-gizmo-workflow.json --quiet`
  - Result: pass.
  - JSON report: status `pass`, `failedChecks: []`, 9 checks passed.
- Evidence validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --check-evidence-files --require-production-trial --quiet --output /tmp/mac-game-rigger-prop-hinge-workflow-validator.json`
  - Result: pass.
  - Summary: 12 real assets, 12 complete evidence, production gate pass, 11 score>=3, ratio 0.917.
- JSON checks:
  - Command: `python3 -m json.tool samples/asset-source-candidates.json`
  - Result: pass.
  - Command: `python3 -m json.tool /tmp/mac-game-rigger-prop-hinge-gizmo-workflow.json`
  - Result: pass.
  - Command: `python3 -m json.tool /tmp/mac-game-rigger-prop-hinge-workflow-validator.json`
  - Result: pass.
- Executable script check:
  - Command: `scripts/verify_prop_hinge_gizmo_workflow.py --help`
  - Result: pass.
- Whitespace/diff hygiene:
  - Command: `git diff --check`
  - Result: pass.

## Important Files / Artifacts

- `scripts/verify_prop_hinge_gizmo_workflow.py`: repeatable Blender smoke for prop hinge guide workflow.
- `tests/test_prop_hinge_gizmo_workflow_smoke.py`: Python-level CLI/report contract tests.
- `samples/asset-source-candidates.json`: next route mentions headless Blender prop hinge smoke.
- `evidence/P-001/notes.md`: P-001 evidence now separates headless smoke coverage from remaining interactive UX QA.
- `evidence/P-002/notes.md`: P-002 evidence now separates headless smoke coverage from remaining interactive UX QA.
- `/tmp/mac-game-rigger-prop-hinge-gizmo-workflow.json`: latest Blender smoke evidence.
- `/tmp/mac-game-rigger-prop-hinge-workflow-validator.json`: latest validator evidence.

## Open Questions / Risks

- Headless guide workflow is now covered, but polished real interactive viewport gizmo UX still needs manual/GUI QA.
- P-001 and P-002 remain internal-local prop evidence, not public CC0 prop coverage.
- H-001 remains the weak evidence slot at deformation score 2.
- Unreal validation remains blocked.
- Direct shaded Blender review and scale/orientation notes are still needed for score-3 assets before stronger production claims.

## Next Steps

1. Polish and QA the real interactive viewport gizmo UX for prop hinge handles.
2. Validate prop QA against another real mechanical shape such as gate, lamp, platform, crate stack, or mechanical panel.
3. Improve or replace H-001 so the weakest humanoid slot no longer drags confidence down.
4. Add Unreal verifier or document a reproducible formal blocker.

## Resume Prompt

Continue from this checkpoint. First read this file and project rules, then inspect `scripts/verify_prop_hinge_gizmo_workflow.py`, `tests/test_prop_hinge_gizmo_workflow_smoke.py`, and `/tmp/mac-game-rigger-prop-hinge-gizmo-workflow.json` before making changes.
