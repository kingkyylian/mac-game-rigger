# Project Checkpoint - 2026-06-20 00:37 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Context: standalone Mac-first game rigger; current focus is making H-001's remaining humanoid rig failure diagnosable before attempting another fix.

## Done This Session

- Continued the H-001 investigation from the previous checkpoint.
- Added TDD coverage for per-bone weight diagnostics:
  - `test_bone_weight_diagnostics_reports_per_bone_bounds_and_weight_samples`
  - RED failure confirmed the new API did not exist.
- Added `bone_weight_diagnostics()` in `tools/blender_asset_workflow.py`.
- Added supporting point/bounds helpers:
  - `vertex_world_point()`
  - `point_xyz()`
  - `point_bounds()`
  - updated `rounded_point()` to support Blender-like `.x/.y/.z` points as well as sequences.
- Wired workflow summaries to write `boneWeightDiagnostics` alongside existing `weightDiagnostics`.
- Re-ran H-001 workflow in place so `evidence/H-001/workflow-summary.json` now contains per-bone:
  - influenced vertex count
  - dominant vertex count
  - total and average weight
  - influenced and dominant world-space bounds
  - top weighted vertex samples
- Updated `evidence/H-001/notes.md` with the new diagnostic finding.
- Regenerated `docs/asset-evidence-progress.md`.

## Current Rigging Success State

- Production-trial gate: pass.
- Real assets registered: 12.
- Complete evidence entries: 12.
- Score >= 3 assets: 11.
- Score >= 3 ratio: 0.917.
- H-001 remains score 2 and visual fail; do not claim this was fixed.
- Category coverage:
  - Humanoid: 7
  - Quadruped: 2
  - Tail creature: 1
  - Prop: 2
- Unity import pass slots:
  - H-003
  - H-004
  - Q-001
  - C-001
  - P-001
  - P-002

## H-001 Diagnostic Findings

- Pose deformation: warn.
- Max axis expansion ratio: `2.591`.
- Warning axis: `x`.
- Cleanup remains clean:
  - 0 unweighted vertices
  - 0 over-limit vertices
  - 0 removed empty groups
- `boneWeightDiagnostics.topBones` starts with:
  - `Chest`: total weight `377.5566`
  - `Foot.L`: total weight `109.0`
  - `Hand.L`: total weight `60.0`
  - `Hand.R`: total weight `60.0`
  - `Foot.R`: total weight `35.3753`
- `Foot.L` now reports:
  - influenced vertices: 109
  - dominant vertices: 109
  - average weight: 1.0
  - bounds: X `0.1431..0.8815`, Y `-0.7253..0.7253`, Z `-0.0219..1.0406`
- `LowerLeg.L` now reports:
  - influenced vertices: 11
  - dominant vertices: 7
  - average weight: `0.7299`
- Interpretation: the remaining H-001 issue is now inspectable at bone/vertex level. Next H-001 fix should compare these bounds and samples against capsule positions and pose output rather than guessing from screenshots.

## Verification

- RED test:
  - Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_bone_weight_diagnostics_reports_per_bone_bounds_and_weight_samples -q`
  - Result before implementation: failed with missing `bone_weight_diagnostics`.
- Narrow GREEN test:
  - Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_bone_weight_diagnostics_reports_per_bone_bounds_and_weight_samples -q`
  - Result: 1 passed.
- Focused tests:
  - Command: `python3 -m pytest tests/test_blender_asset_workflow.py tests/test_generate_asset_evidence_report.py -q`
  - Result: 62 passed.
- H-001 Blender workflow:
  - Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-001/H-001-quaternius-animated-human.fbx --evidence-dir evidence/H-001 --summary evidence/H-001/workflow-summary.json --template humanoid --camera-axis x`
  - Result: pass.
- Report regeneration:
  - Command: `python3 scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --output docs/asset-evidence-progress.md`
  - Result: pass.
- JSON checks:
  - Command: `python3 -m json.tool evidence/H-001/workflow-summary.json`
  - Result: pass.
  - Command: `python3 -m json.tool samples/manifest.json`
  - Result: pass.
- Full test suite:
  - Command: `python3 -m pytest tests -q`
  - Result: 170 passed.
- Evidence validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --output /tmp/mac-game-rigger-H-001-bone-diagnostics-validator.json --check-evidence-files --require-production-trial`
  - Result: pass.
  - Summary: 12 real assets, 12 complete evidence, production gate pass, 11 score>=3, ratio 0.917.
- Whitespace/diff hygiene:
  - Command: `git diff --check`
  - Result: pass.

## Important Files / Artifacts

- `tools/blender_asset_workflow.py`: new per-bone diagnostic implementation and workflow summary wiring.
- `tests/test_blender_asset_workflow.py`: TDD regression coverage for per-bone counts, bounds, and top vertex samples.
- `evidence/H-001/workflow-summary.json`: regenerated H-001 evidence with `boneWeightDiagnostics`.
- `evidence/H-001/notes.md`: updated H-001 diagnostic note; score remains 2.
- `docs/asset-evidence-progress.md`: regenerated progress table.
- `/tmp/mac-game-rigger-H-001-bone-diagnostics-validator.json`: latest production-trial validator evidence.

## Open Questions / Risks

- H-001 still has visible deformation artifacts and remains score 2.
- The new diagnostic layer does not improve rig quality by itself; it makes the next fix evidence-driven.
- Direct shaded Blender review and scale/orientation notes are still needed for score-3 assets.
- Interactive prop hinge viewport UX QA is still pending.
- P-001 and P-002 remain internal-local prop evidence, not public CC0 prop coverage.
- Unreal validation remains blocked.

## Next Steps

1. Use `boneWeightDiagnostics` plus `capsuleDiagnostics` to compare H-001 leg/foot world-space bounds against generated capsule positions.
2. Add a targeted test for the next H-001 root cause only after that comparison identifies a concrete bad assumption.
3. Polish and QA the interactive prop hinge viewport gizmo UX.
4. Add Unreal verifier or document a reproducible formal blocker.

## Resume Prompt

Continue from this checkpoint. First read this file and project rules, then inspect `tools/blender_asset_workflow.py`, `tests/test_blender_asset_workflow.py`, `evidence/H-001/workflow-summary.json`, and `evidence/H-001/preview-pose.png` before making changes. Keep H-001 at score 2 unless fresh visual and validator evidence proves a quality increase.
