# Project Checkpoint - 2026-06-20 01:11 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Context: standalone Mac-first game rigger; current focus is H-001 humanoid deformation root-cause evidence.

## Done This Session

- Resumed from `docs/checkpoints/2026-06-20-0037.md`.
- Compared H-001 `boneWeightDiagnostics` against `capsuleDiagnostics`.
- Confirmed the previous world-space bone bounds are not directly comparable with capsule bounds because actual binding maps mesh vertices into capsule target bounds through `mesh_bind_vertex_points(mesh, target_bounds=_capsule_bounds(bones))`.
- Added TDD coverage:
  - `test_capsule_bind_weight_diagnostics_reports_bone_bounds_in_capsule_space`
  - RED failure confirmed `capsule_bind_weight_diagnostics` did not exist.
- Added `capsule_bind_weight_diagnostics()` in `tools/blender_asset_workflow.py`.
- Added helper functions:
  - `capsule_target_bounds()`
  - `bounds_summary()`
- Extended `bone_weight_diagnostics()` so it can report either world-space samples or caller-provided point-space samples.
- Wired workflow summaries to write `capsuleBindWeightDiagnostics`.
- Re-ran H-001 workflow in place.
- Updated `evidence/H-001/notes.md` with the same-space capsule-bind diagnostic finding.
- Regenerated `docs/asset-evidence-progress.md`.

## Current Rigging Success State

- Production-trial gate: pass.
- Real assets registered: 12.
- Complete evidence entries: 12.
- Score >= 3 assets: 11.
- Score >= 3 ratio: 0.917.
- H-001 remains score 2 and visual fail; do not claim this was fixed.
- Category coverage:
  - Humanoid: 7
  - Quadruped: 2
  - Tail creature: 1
  - Prop: 2
- Unity import pass slots:
  - H-003
  - H-004
  - Q-001
  - C-001
  - P-001
  - P-002

## H-001 Capsule-Bind Findings

- Pose deformation: warn.
- Max axis expansion ratio: `2.591`.
- Warning axis: `x`.
- Cleanup remains clean:
  - 0 unweighted vertices
  - 0 over-limit vertices
  - 0 removed empty groups
- `capsuleBindWeightDiagnostics.space`: `capsuleBind`.
- Target bounds: X `-1.153..1.3231`, Y `-5.5653..0.0603`, Z `-2.9558..3.2425`.
- `Foot.L` in capsule-bind space:
  - influenced vertices: 109
  - dominant vertices: 109
  - average weight: 1.0
  - dominant bounds: X `0.118..1.3231`, Y `-3.2313..-2.2736`, Z `-2.9558..-2.1327`
- Generated `Foot.L` capsule:
  - head: `[0.3159, -2.7525, -2.1224]`
  - tail: `[0.3159, -2.8784, -2.7709]`
  - radius: `0.185`
- Interpretation: H-001's remaining failure is likely tied to fallback/nearest-bone assignment or overly broad distal capture around Foot.L, not merely a reporting-space mismatch.

## Verification

- RED test:
  - Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_capsule_bind_weight_diagnostics_reports_bone_bounds_in_capsule_space -q`
  - Result before implementation: failed with missing `capsule_bind_weight_diagnostics`.
- Narrow GREEN test:
  - Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_capsule_bind_weight_diagnostics_reports_bone_bounds_in_capsule_space -q`
  - Result: 1 passed.
- Focused tests:
  - Command: `python3 -m pytest tests/test_blender_asset_workflow.py tests/test_generate_asset_evidence_report.py -q`
  - Result: 63 passed.
- H-001 Blender workflow:
  - Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-001/H-001-quaternius-animated-human.fbx --evidence-dir evidence/H-001 --summary evidence/H-001/workflow-summary.json --template humanoid --camera-axis x`
  - Result: pass.
- Report regeneration:
  - Command: `python3 scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --output docs/asset-evidence-progress.md`
  - Result: pass.
- Full test suite:
  - Command: `python3 -m pytest tests -q`
  - Result: 171 passed.
- Evidence validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --output /tmp/mac-game-rigger-H-001-capsule-bind-validator.json --check-evidence-files --require-production-trial`
  - Result: pass.
  - Summary: 12 real assets, 12 complete evidence, production gate pass, 11 score>=3, ratio 0.917.
- JSON check:
  - Command: `python3 -m json.tool evidence/H-001/workflow-summary.json`
  - Result: pass.
- Whitespace/diff hygiene:
  - Command: `git diff --check`
  - Result: pass.

## Important Files / Artifacts

- `tools/blender_asset_workflow.py`: added capsule-bind diagnostic implementation and workflow summary wiring.
- `tests/test_blender_asset_workflow.py`: added TDD coverage for capsule-bind diagnostic bounds and samples.
- `evidence/H-001/workflow-summary.json`: regenerated H-001 evidence with `capsuleBindWeightDiagnostics`.
- `evidence/H-001/notes.md`: updated with same-space Foot.L diagnostic finding.
- `docs/asset-evidence-progress.md`: regenerated progress table.
- `/tmp/mac-game-rigger-H-001-capsule-bind-validator.json`: latest production-trial validator evidence.

## Open Questions / Risks

- H-001 still has visible deformation artifacts and remains score 2.
- The next fix should target fallback/nearest-bone assignment behavior only after adding a focused failing test.
- Direct shaded Blender review and scale/orientation notes are still needed for score-3 assets.
- Interactive prop hinge viewport UX QA is still pending.
- P-001 and P-002 remain internal-local prop evidence, not public CC0 prop coverage.
- Unreal validation remains blocked.

## Next Steps

1. Add diagnostic or tests around `_top_capsule_weights()` fallback behavior for distal bones, using the H-001 Foot.L over-capture evidence.
2. Test whether fallback should prefer parent/chain-local bones or avoid assigning 1.0 to distal bones when outside capsule radius.
3. Re-run H-001 only after the fallback behavior has a failing unit test and a minimal fix.
4. Continue prop hinge viewport UX QA after H-001 root-cause loop.

## Resume Prompt

Continue from this checkpoint. First read this file and project rules, then inspect `addon/mac_game_rigger/core/weight_binding.py`, `tools/blender_asset_workflow.py`, `tests/test_capsule_weights.py`, `tests/test_blender_asset_workflow.py`, and `evidence/H-001/workflow-summary.json`. Keep H-001 at score 2 unless fresh visual and validator evidence proves a quality increase.
