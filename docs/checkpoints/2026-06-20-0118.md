# Project Checkpoint - 2026-06-20 01:18 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Context: standalone Mac-first game rigger; current focus is measured H-001 humanoid deformation root cause.

## Done This Session

- Resumed from `docs/checkpoints/2026-06-20-0111.md`.
- Followed the H-001 capsule-bind evidence into the actual binding fallback path.
- Added TDD coverage in `tests/test_capsule_weights.py`:
  - `test_capsule_assignment_details_reports_distal_nearest_fallback_outside_radius`
  - RED failure confirmed `capsule_assignment_details` did not exist.
- Added `capsule_assignment_details()` in `addon/mac_game_rigger/core/weight_binding.py`.
  - `_top_capsule_weights()` now delegates to it and preserves the existing weight output.
  - The new function reports assignment mode, nearest bone, bone region, nearest distance, radius, and distance/radius ratio.
- Added TDD coverage in `tests/test_blender_asset_workflow.py`:
  - `test_capsule_assignment_diagnostics_summarizes_nearest_fallback_by_bone`
  - RED failure confirmed `capsule_assignment_diagnostics` did not exist.
- Added `capsule_assignment_diagnostics()` in `tools/blender_asset_workflow.py`.
- Wired workflow summaries to write `capsuleAssignmentDiagnostics`.
- Re-ran H-001 workflow in place.
- Updated `evidence/H-001/notes.md` with measured fallback counts.
- Regenerated `docs/asset-evidence-progress.md`.

## Current Rigging Success State

- Production-trial gate: pass.
- Real assets registered: 12.
- Complete evidence entries: 12.
- Score >= 3 assets: 11.
- Score >= 3 ratio: 0.917.
- H-001 remains score 2 and visual fail; do not claim this was fixed.
- Category coverage:
  - Humanoid: 7
  - Quadruped: 2
  - Tail creature: 1
  - Prop: 2
- Unity import pass slots:
  - H-003
  - H-004
  - Q-001
  - C-001
  - P-001
  - P-002

## H-001 Assignment Findings

- Pose deformation: warn.
- Max axis expansion ratio: `2.591`.
- Warning axis: `x`.
- Cleanup remains clean:
  - 0 unweighted vertices
  - 0 over-limit vertices
  - 0 removed empty groups
- `capsuleAssignmentDiagnostics` now separates direct capsule assignments from nearest-bone fallbacks.
- H-001 current assignment totals:
  - vertex count: 791
  - capsule-assigned vertices: 440
  - nearest-fallback vertices: 351
- Highest fallback bones:
  - `Foot.L`: 108 vertices, average distance/radius `4.8194`, max `6.0419`
  - `Hand.L`: 60 vertices, average `3.5984`, max `6.6095`
  - `Hand.R`: 60 vertices, average `3.5984`, max `6.6095`
  - `Foot.R`: 33 vertices, average `2.8705`, max `4.1764`
- Interpretation: H-001's weak foot/leg behavior is now tied to distal nearest-fallback over-capture. The next real quality fix should change fallback behavior under test, not adjust notes or score.

## Verification

- RED core test:
  - Command: `python3 -m pytest tests/test_capsule_weights.py::test_capsule_assignment_details_reports_distal_nearest_fallback_outside_radius -q`
  - Result before implementation: failed with missing `capsule_assignment_details`.
- RED workflow test:
  - Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_capsule_assignment_diagnostics_summarizes_nearest_fallback_by_bone -q`
  - Result before implementation: failed with missing `capsule_assignment_diagnostics`.
- Narrow GREEN tests:
  - Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_capsule_assignment_diagnostics_summarizes_nearest_fallback_by_bone tests/test_capsule_weights.py::test_capsule_assignment_details_reports_distal_nearest_fallback_outside_radius -q`
  - Result: 2 passed.
- Focused tests:
  - Command: `python3 -m pytest tests/test_capsule_weights.py tests/test_blender_asset_workflow.py tests/test_generate_asset_evidence_report.py -q`
  - Result: 85 passed.
- H-001 Blender workflow:
  - Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-001/H-001-quaternius-animated-human.fbx --evidence-dir evidence/H-001 --summary evidence/H-001/workflow-summary.json --template humanoid --camera-axis x`
  - Result: pass.
- Report regeneration:
  - Command: `python3 scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --output docs/asset-evidence-progress.md`
  - Result: pass.
- Full test suite:
  - Command: `python3 -m pytest tests -q`
  - Result: 173 passed.
- Evidence validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --output /tmp/mac-game-rigger-H-001-assignment-diagnostics-validator.json --check-evidence-files --require-production-trial`
  - Result: pass.
  - Summary: 12 real assets, 12 complete evidence, production gate pass, 11 score>=3, ratio 0.917.
- JSON check:
  - Command: `python3 -m json.tool evidence/H-001/workflow-summary.json`
  - Result: pass.
- Whitespace/diff hygiene:
  - Command: `git diff --check`
  - Result: pass.

## Important Files / Artifacts

- `addon/mac_game_rigger/core/weight_binding.py`: assignment-detail diagnostic for capsule vs nearest fallback.
- `tools/blender_asset_workflow.py`: workflow-level fallback aggregation in `capsuleAssignmentDiagnostics`.
- `tests/test_capsule_weights.py`: TDD coverage for distal nearest fallback outside radius.
- `tests/test_blender_asset_workflow.py`: TDD coverage for fallback aggregation by bone.
- `evidence/H-001/workflow-summary.json`: regenerated H-001 evidence with assignment diagnostics.
- `evidence/H-001/notes.md`: updated with measured fallback counts.
- `docs/asset-evidence-progress.md`: regenerated progress table.
- `/tmp/mac-game-rigger-H-001-assignment-diagnostics-validator.json`: latest validator evidence.

## Open Questions / Risks

- H-001 still has visible deformation artifacts and remains score 2.
- The next fix should reduce distal fallback over-capture under test.
- A likely candidate is to avoid assigning 1.0 to distal fallback bones when distance/radius is very high and a parent/limb fallback candidate exists.
- Direct shaded Blender review and scale/orientation notes are still needed for score-3 assets.
- Interactive prop hinge viewport UX QA is still pending.
- P-001 and P-002 remain internal-local prop evidence, not public CC0 prop coverage.
- Unreal validation remains blocked.

## Next Steps

1. Add a RED test for desired fallback behavior: high distance/radius distal fallback should prefer a limb/parent candidate or lower-confidence assignment.
2. Implement the smallest fallback change and re-run H-001.
3. Compare H-001 `Foot.L` fallback count and visual pose before considering any score change.
4. Continue prop hinge viewport UX QA after H-001 fallback behavior is stabilized.

## Resume Prompt

Continue from this checkpoint. First read this file and project rules, then inspect `addon/mac_game_rigger/core/weight_binding.py`, `tests/test_capsule_weights.py`, `tools/blender_asset_workflow.py`, `tests/test_blender_asset_workflow.py`, and `evidence/H-001/workflow-summary.json`. Keep H-001 at score 2 unless fresh visual and validator evidence proves a quality increase.
