# Project Checkpoint - 2026-06-20 06:13 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Context: standalone Mac-first game rigger; current focus is measured rigging quality on real game assets.

## Current Rigging Success State

- Production-trial gate: pass.
- Real assets registered: 12.
- Complete evidence entries: 12.
- Score >= 3 assets: 11.
- Score >= 3 ratio: 0.917.
- Category coverage:
  - Humanoid: 7
  - Quadruped: 2
  - Tail creature: 1
  - Prop: 2
- Unity import pass slots:
  - H-003
  - H-004
  - Q-001
  - C-001
  - P-001
  - P-002

## Done Since Previous Checkpoint

- Added a real H-001 fallback behavior fix instead of only adding diagnostics.
- Confirmed RED first with `test_capsule_assignment_details_prefers_lower_ratio_limb_over_distal_fallback`.
- Changed nearest fallback selection in `addon/mac_game_rigger/core/weight_binding.py` to choose by distance/radius ratio instead of absolute distance.
- Re-ran the H-001 Blender workflow in place.
- Updated `evidence/H-001/notes.md` with the new fallback-ratio behavior and post-fix measurements.
- Regenerated `docs/asset-evidence-progress.md`.

## H-001 Result

- H-001 remains deformation score 2 and visual review fail.
- The numeric deformation metric improved from warning to pass:
  - previous max axis expansion: 2.591x on X
  - current max axis expansion: 1.207x
  - warning axes: none
- Cleanup remains clean:
  - 0 unweighted vertices
  - 0 over-limit vertices
  - 0 removed empty groups
- Assignment totals:
  - capsule-assigned vertices: 440
  - nearest-fallback vertices: 351
- Important redistribution:
  - `Foot.L` no longer appears in top fallback bones.
  - `Foot.L` dominant vertices dropped to 3.
  - `LowerLeg.L` now influences 33 vertices and dominates 29.
  - Reported region weights now show foot dominance reduced to 15 while leg dominance is 87.

## Interpretation

- The current tool is strong enough to pass the project production-trial evidence gate across 12 real assets.
- It is not yet a dependable one-click production rigger for arbitrary humanoids.
- The weakest current proof point is H-001: the catastrophic foot fallback behavior is improved, but the pose preview still has visible lower-leg/foot artifacts, so no score bump was claimed.
- For game development this means the tool is already useful as an automation baseline and batch evidence harness, but still needs targeted deformation cleanup before it can replace manual rigging review.

## Verification

- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: 173 passed.
- Evidence validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --output /tmp/mac-game-rigger-H-001-fallback-ratio-validator.json --check-evidence-files --require-production-trial`
  - Result: pass.
  - Summary: 12 real assets, 12 complete evidence, production gate pass, 11 score>=3, ratio 0.917.
- JSON check:
  - Command: `python3 -m json.tool evidence/H-001/workflow-summary.json`
  - Result: pass.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: pass.
- Report check:
  - H-001 row now reports `pass 1.207x`, score `2`, visual `fail`, and region weights `core 615 arm 74 leg 87 foot 15`.

## Important Files / Artifacts

- `addon/mac_game_rigger/core/weight_binding.py`: fallback now chooses lowest distance/radius ratio.
- `tests/test_capsule_weights.py`: RED/GREEN coverage for lower-ratio limb fallback over distal fallback.
- `evidence/H-001/workflow-summary.json`: updated post-fix H-001 evidence.
- `evidence/H-001/notes.md`: updated H-001 review notes.
- `docs/asset-evidence-progress.md`: regenerated progress table.
- `/tmp/mac-game-rigger-H-001-fallback-ratio-validator.json`: latest validator output.

## Next Steps

1. Add stricter visual/geometry scoring for knee, ankle, and foot artifacts so H-001 cannot hide behind bbox pass metrics.
2. Add a second fallback behavior pass: blended fallback weights for far outside capsules instead of hard 1.0 nearest assignment.
3. Run the same fallback-ratio workflow on H-003/H-006/H-009/H-010 to ensure no regression on previously score-3 humanoids.
4. Add a small curated humanoid stress set with thin limbs, bulky shoulders, and nonstandard proportions.
5. Continue prop hinge viewport UX QA after H-001 humanoid fallback behavior is stable.

## Resume Prompt

Continue from this checkpoint. First inspect `docs/asset-evidence-progress.md`, `evidence/H-001/notes.md`, `evidence/H-001/workflow-summary.json`, `addon/mac_game_rigger/core/weight_binding.py`, and `tests/test_capsule_weights.py`. Keep H-001 at score 2 until fresh visual scoring proves the lower-leg/foot artifacts are actually acceptable.
