# Project Checkpoint - 2026-06-20 06:20 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Context: standalone Mac-first game rigger; current focus is measured humanoid deformation quality on real game assets.

## Done This Session

- Continued from `docs/checkpoints/2026-06-20-0613.md`.
- Added a stricter humanoid diagnostic signal so H-001 cannot look clean only because bbox-based pose deformation now passes.
- Followed TDD:
  - Added RED test `test_humanoid_diagnostics_summary_warns_when_foot_coverage_is_too_low`.
  - Confirmed it failed because `humanoid_diagnostics_summary` did not exist.
  - Implemented `humanoid_diagnostics_summary()` in `tools/blender_asset_workflow.py`.
  - Added RED report test `test_generate_asset_evidence_report_shows_humanoid_diagnostic_warnings`.
  - Confirmed it failed because the report did not surface humanoid diagnostics.
  - Added `humanoidDiagnostics` report rendering in `scripts/generate_asset_evidence_report.py`.
- Wired `humanoidDiagnostics` into workflow summaries.
- Re-ran the H-001 Blender workflow in place.
- Updated `evidence/H-001/notes.md`.
- Regenerated `docs/asset-evidence-progress.md`.

## Current Rigging Success State

- Production-trial gate: pass.
- Real assets registered: 12.
- Complete evidence entries: 12.
- Score >= 3 assets: 11.
- Score >= 3 ratio: 0.917.
- H-001 remains deformation score 2 and visual fail.
- H-001 now visibly reports `humanoid qa warn weakHumanoidFootCoverage` in the progress report.

## H-001 Current Evidence

- Pose QA: pass, max axis expansion 1.207x.
- Visual review: fail.
- Weight row in `docs/asset-evidence-progress.md`:
  - `core 615 arm 74 leg 87 foot 15 humanoid qa warn weakHumanoidFootCoverage`
- Humanoid diagnostic ratios:
  - core: 0.7775
  - arm: 0.0936
  - leg: 0.11
  - foot: 0.019
- Interpretation: previous distal foot over-capture is reduced, but foot coverage is now weak enough to keep the lower-leg/foot artifact risk explicit.

## Verification

- RED diagnostic test:
  - Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_humanoid_diagnostics_summary_warns_when_foot_coverage_is_too_low -q`
  - Result before implementation: failed with missing `humanoid_diagnostics_summary`.
- RED report test:
  - Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py::test_generate_asset_evidence_report_shows_humanoid_diagnostic_warnings -q`
  - Result before implementation: failed because `humanoid qa warn weakHumanoidFootCoverage` was absent.
- H-001 Blender workflow:
  - Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-001/H-001-quaternius-animated-human.fbx --evidence-dir evidence/H-001 --summary evidence/H-001/workflow-summary.json --template humanoid --camera-axis x`
  - Result: pass.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: 175 passed.
- Evidence validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --output /tmp/mac-game-rigger-humanoid-diagnostics-validator.json --check-evidence-files --require-production-trial`
  - Result: pass.
  - Summary: 12 real assets, 12 complete evidence, production gate pass, 11 score>=3, ratio 0.917.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: pass.
- Report check:
  - Command: `rg -n "Production trial gate|Score >= 3|H-001|weakHumanoidFootCoverage|humanoid qa" docs/asset-evidence-progress.md evidence/H-001/notes.md`
  - Result: H-001 row includes the humanoid QA warning.

## Important Files / Artifacts

- `tools/blender_asset_workflow.py`: added `humanoid_diagnostics_summary()` and writes `humanoidDiagnostics`.
- `scripts/generate_asset_evidence_report.py`: reports `humanoid qa <status> <warnings>` in the Weight column.
- `tests/test_blender_asset_workflow.py`: TDD coverage for weak humanoid foot coverage.
- `tests/test_generate_asset_evidence_report.py`: TDD coverage for report visibility.
- `evidence/H-001/workflow-summary.json`: regenerated with `humanoidDiagnostics`.
- `evidence/H-001/notes.md`: documents the weak foot coverage warning.
- `docs/asset-evidence-progress.md`: regenerated; H-001 now shows the humanoid warning.
- `/tmp/mac-game-rigger-humanoid-diagnostics-validator.json`: latest validator output.

## Open Risks

- H-001 is still not production-clean; do not raise score without fresh visual/geometry evidence.
- The new humanoid diagnostic is intentionally a warning, not a scoring gate.
- Other score-3 humanoids should be re-run with the latest workflow so `humanoidDiagnostics` exists across the set.
- Unreal validation remains blocked locally.

## Next Steps

1. Re-run latest workflow on H-003, H-004, H-005, H-006, H-009, and H-010 to populate humanoid diagnostics and check for regressions.
2. Add a second fallback behavior pass: blend far-out fallback weights instead of hard 1.0 nearest assignment.
3. Add stricter visual/geometry metrics for ankle/knee/foot artifacts beyond bbox expansion.
4. Decide whether `weakHumanoidFootCoverage` should block future score >=3 claims after comparing all humanoid diagnostics.
5. Continue prop hinge viewport UX QA after humanoid fallback gates are stable.

## Resume Prompt

Continue from this checkpoint. First inspect `docs/asset-evidence-progress.md`, `evidence/H-001/workflow-summary.json`, `tools/blender_asset_workflow.py`, `scripts/generate_asset_evidence_report.py`, and the two new tests. Keep H-001 score 2 until fresh evidence proves the lower-leg/foot artifacts are acceptable.
