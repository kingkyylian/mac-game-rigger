# Project Checkpoint - 2026-06-20 06:35 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Context: standalone Mac-first game rigger; current focus is enforcing honest humanoid quality gates.

## Done This Session

- Continued from `docs/checkpoints/2026-06-20-0628.md`.
- Added validator policy for score >= 3 humanoid evidence:
  - `humanoidDiagnostics` is now required for humanoid score >= 3.
  - `humanoidDiagnostics.status` must be `pass`.
  - `warn` or `fail` now makes that asset incomplete for score >= 3 evidence.
- Followed TDD:
  - Added RED test `test_asset_evidence_validator_blocks_score_three_plus_humanoid_with_warning_diagnostics`.
  - Confirmed RED failed because incomplete assets were empty.
  - Implemented `humanoid_quality_issue_for_score()` in `scripts/validate_asset_evidence.py`.
  - Updated test fixtures to emit pass humanoid diagnostics where they intentionally model valid score >= 3 humanoids.
- Regenerated `docs/asset-evidence-progress.md`.

## Current State

- Schema status: pass.
- Real assets registered: 12.
- Complete evidence entries under new quality policy: 7.
- Production-trial gate: blocked.
- Score >= 3 count among complete evidence: 6.
- Score >= 3 ratio among complete evidence: 0.857.
- Missing production gate requirements:
  - `completeRealAssetsAtLeast10`
  - `humanoidsAtLeast3`
  - `thinLimbHumanoidIncluded`
  - `wideShoulderOrBulkyIncluded`

## Newly Blocked Humanoid Evidence

- H-003: `humanoidDiagnostics.status=warn` blocks score >= 3 due to `weakHumanoidFootCoverage`.
- H-004: `humanoidDiagnostics.status=warn` blocks score >= 3 due to `weakHumanoidFootCoverage`.
- H-005: `humanoidDiagnostics.status=warn` blocks score >= 3 due to `weakHumanoidFootCoverage`.
- H-009: `humanoidDiagnostics.status=warn` blocks score >= 3 due to `weakHumanoidFootCoverage`.
- H-010: `humanoidDiagnostics.status=warn` blocks score >= 3 due to `weakHumanoidFootCoverage`.
- H-006 remains the only score-3 humanoid that passes the new humanoid diagnostic.

## Interpretation

- This is an intentional quality gate tightening, not a runtime failure.
- The old production-trial pass was too permissive for humanoid rigging quality.
- The tool remains useful as a pipeline/evidence harness, but current humanoid rig quality is not strong enough to claim reliable one-click score-3 coverage across the set.
- Next work should improve weighting behavior before trying to restore the gate.

## Verification

- RED validator test:
  - Command: `python3 -m pytest tests/test_asset_evidence_validator.py::test_asset_evidence_validator_blocks_score_three_plus_humanoid_with_warning_diagnostics -q`
  - Result before implementation: failed because `incompleteRealAssets` was empty.
- Focused validator tests:
  - Command: `python3 -m pytest tests/test_asset_evidence_validator.py -q`
  - Result: 17 passed.
- Focused report tests:
  - Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py -q`
  - Result: 11 passed.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: 176 passed.
- Real manifest validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --output /tmp/mac-game-rigger-humanoid-policy-validator.json --check-evidence-files --require-production-trial --quiet`
  - Result: exit 1, expected under new policy.
  - Output summary: schema pass, production gate blocked, complete evidence 7, score3 count 6, score3 ratio 0.857.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: pass.

## Important Files / Artifacts

- `scripts/validate_asset_evidence.py`: enforces humanoid diagnostics for score >= 3 evidence.
- `tests/test_asset_evidence_validator.py`: covers score >= 3 humanoid warning rejection.
- `tests/test_generate_asset_evidence_report.py`: fixture updated for pass humanoid diagnostics.
- `docs/asset-evidence-progress.md`: now reports production trial as blocked with humanoid diagnostic issues.
- `/tmp/mac-game-rigger-humanoid-policy-validator.json`: latest real-manifest validator output.

## Open Risks

- Current manifest scores remain optimistic; validator now blocks affected assets, but manifest data still says score 3.
- H-004 may need an explicit hidden-foot/robe exception, but no exception is implemented yet.
- The weighting system likely needs a fallback blending or limb/extremity coverage fix before restoring H-003/H-005/H-009/H-010.
- Unreal validation remains blocked locally.

## Next Steps

1. Add a targeted weight-binding RED test for H-003-like core-heavy fallback: far-out fallback should not leave arms/feet with only minimum coverage.
2. Implement a small fallback blending or extremity coverage improvement.
3. Re-run H-003 first and compare `humanoidDiagnostics`, `weightDiagnostics`, and preview images.
4. Only after H-003 improves, re-run H-005/H-009/H-010.
5. Decide whether H-004 needs explicit `hiddenFootOrRobe` exception metadata.

## Resume Prompt

Continue from this checkpoint. First inspect `scripts/validate_asset_evidence.py`, `tests/test_asset_evidence_validator.py`, `docs/asset-evidence-progress.md`, and H-003/H-006 workflow summaries. The production gate is now intentionally blocked by humanoid quality policy; next work should fix weighting quality, not weaken the validator.
