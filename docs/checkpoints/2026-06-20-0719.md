# Project Checkpoint - 2026-06-20 07:19 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Context: standalone Mac-first game rigger; current focus is proving Unity engine-readiness beyond raw FBX import.

## Done This Session

- Found a serious Unity-readiness gap:
  - Unity verifier had been checking only raw FBX import/load.
  - Refreshed metadata initially showed `skinnedMeshRendererCount=0`, `meshFilterCount=1`, and `childCount=1`.
  - Root cause: workflow exported with `use_selection=true` while only meshes were selected; `MGR_Armature` was omitted from the FBX selection, so Unity saw static meshes.
- Added Unity checker depth:
  - imports the FBX;
  - instantiates the imported model prefab;
  - records child transform count;
  - records renderer, skinned mesh renderer, mesh filter, and animator counts;
  - records Unity `ModelImporter` metadata.
- Fixed workflow export selection:
  - added `select_export_objects()`;
  - export now selects meshes plus `MGR_Armature`;
  - workflow uses this helper before `export_unity_fbx`.
- Tightened validator:
  - `unityImport.status=pass` now requires `evidence/<slot>/unity-import.json` when file checks are enabled;
  - Unity result must have `instantiated=true`;
  - score >= 3 Unity pass now requires `skinnedMeshRendererCount >= 1`;
  - Unity `ModelImporter` metadata must be available.
- Re-exported score-3 Unity evidence set with the armature included:
  - H-003, H-004, H-005, H-006, H-009, H-010
  - Q-001, Q-002
  - C-001
  - P-001, P-002
- Re-ran Unity verifier for the refreshed exports.
- Updated all Unity pass evidence JSON files to include skinned/instantiate metadata.
- Regenerated `docs/asset-evidence-progress.md`.
- Updated `docs/unity-import-verification.md`.

## Current State

- Schema status: pass.
- Production-trial gate: pass.
- Complete evidence entries: 12.
- Incomplete real assets: 0.
- Score >= 3 count among complete evidence: 11.
- Score >= 3 ratio among complete evidence: 0.917.
- Unity import pass count: 11.
- Every score >= 3 Unity pass now has:
  - `instantiated: true`
  - `skinnedMeshRendererCount: 1`
  - `meshFilterCount: 0`
  - `modelImporter.available: true`
  - `modelImporter.animationType: Generic`

## Unity Skinned Import Counts

- H-003/H-004/H-005/H-006/H-009/H-010:
  - childCount 20, skinnedMeshRendererCount 1.
- Q-001/Q-002:
  - childCount 26, skinnedMeshRendererCount 1.
- C-001:
  - childCount 28, skinnedMeshRendererCount 1.
- P-001/P-002:
  - childCount 6, skinnedMeshRendererCount 1.

## Verification

- RED export-selection test:
  - Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_select_export_objects_includes_mgr_armature_with_meshes -q`
  - Result before implementation: failed because `select_export_objects` did not exist.
- Focused tests:
  - Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py tests/test_asset_evidence_validator.py tests/test_blender_asset_workflow.py::test_select_export_objects_includes_mgr_armature_with_meshes tests/test_unity_import_check_source.py tests/test_unity_import_verifier.py -q`
  - Result: 35 passed.
- Unity verifier:
  - Command pattern: `scripts/verify_unity_fbx_import.sh --fbx evidence/<slot>/export-unity.fbx --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240`
  - Result: all 11 Unity-pass score-3 assets passed with skinned mesh metadata.
- Production validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --output /tmp/mac-game-rigger-skinned-final-validator.json --check-evidence-files --require-production-trial --quiet`
  - Result: exit 0.
  - Output summary: schema pass, production gate pass, complete evidence 12, incomplete 0, Unity pass 11.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: 180 passed.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: pass.

## Important Files / Artifacts

- `tools/unity_import_check/Assets/Editor/MacGameRiggerFbxImportCheck.cs`: deeper Unity import checker.
- `tools/blender_asset_workflow.py`: export now selects mesh plus MGR armature.
- `scripts/validate_asset_evidence.py`: Unity pass now requires instantiated skinned import evidence.
- `tests/test_unity_import_check_source.py`: source-level guard for Unity checker depth.
- `tests/test_blender_asset_workflow.py`: export selection regression test.
- `tests/test_asset_evidence_validator.py`: Unity skinned import validator coverage.
- `tests/test_generate_asset_evidence_report.py`: updated Unity evidence fixtures.
- `evidence/*/unity-import.json`: refreshed Unity pass evidence for the score-3 set.
- `docs/unity-import-verification.md`: documents richer verifier scope.
- `docs/asset-evidence-progress.md`: latest production evidence report.
- `/tmp/mac-game-rigger-skinned-final-validator.json`: latest validator evidence.

## Open Risks

- Unity now proves imported skinned model prefabs instantiate, but still does not prove animation playback or avatar retargeting quality.
- `animatorCount` remains 0 because the checker instantiates imported model prefabs, not configured scene prefabs with Animator components.
- Unreal validation remains blocked locally.
- Score-3 assets remain usable-with-cleanup, not score-4 production-clean.
- H-001 remains low-quality score-2 and visual fail.

## Next Steps

1. Add Unity animation clip or bone-transform smoke: instantiate model, rotate/imported bones or sample a clip, and verify SkinnedMeshRenderer remains present.
2. Add Unity scale/orientation checks from instantiated bounds.
3. Add stricter score-4 production-clean gate with shaded/engine-side review evidence.
4. Investigate Unreal import path or document the blocker with equivalent depth.

## Resume Prompt

Continue from this checkpoint. Unity raw import was upgraded into skinned prefab instantiate evidence, and the validator now blocks score >= 3 Unity pass claims without `skinnedMeshRendererCount >= 1`. Next, add Unity animation/bone-transform smoke or scale/orientation checks beyond static instantiate.
