# Project Checkpoint - 2026-06-20 13:49 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Context: standalone Mac-first game rigger; current focus is proving Unity engine-readiness beyond static/skinned import.

## Done This Session

- Raised the Unity verification gate from skinned prefab instantiate evidence to skinned prefab plus mutable bone-transform smoke evidence.
- Added Unity checker logic that samples `SkinnedMeshRenderer.bones`, rotates one linked bone in editor batchmode, records the rotation delta, then restores the original local rotation.
- Made the Unity checker fail when a skinned mesh renderer exists but bone-transform smoke does not pass.
- Tightened production evidence validation so score >= 3 Unity-pass assets require:
  - `instantiated: true`
  - `skinnedMeshRendererCount >= 1`
  - `boneTransformSmoke.passed: true`
  - `boneCandidateCount >= 1`
  - `rotationDeltaDegrees > 0`
  - available Unity `ModelImporter` metadata.
- Ran the stricter Unity verifier across all 11 Unity-pass score-3 assets.
- Updated all 11 matching `evidence/<slot>/unity-import.json` files with `boneTransformSmoke` results.
- Updated `docs/unity-import-verification.md` and regenerated `docs/asset-evidence-progress.md`.

## Current State

- Schema status: pass.
- Production-trial gate: pass.
- Complete evidence entries: 12.
- Incomplete real assets: 0.
- Score >= 3 count among complete evidence: 11.
- Score >= 3 ratio among complete evidence: 0.917.
- Unity import pass count: 11.
- Every score >= 3 Unity pass now has skinned import evidence and bone-transform smoke evidence.

## Unity Bone Smoke Counts

- H-003/H-004/H-005/H-006/H-009/H-010:
  - `boneCandidateCount: 17`, tested bone `Hips`, rotation delta about 5 degrees.
- Q-001/Q-002:
  - `boneCandidateCount: 23`, tested bone `Pelvis`, rotation delta about 5 degrees.
- C-001:
  - `boneCandidateCount: 25`, tested bone `Pelvis`, rotation delta about 5 degrees.
- P-001/P-002:
  - `boneCandidateCount: 3`, tested bone `PropBase`, rotation delta about 5 degrees.

## Verification

- RED tests:
  - `tests/test_unity_import_check_source.py` failed first because bone smoke source markers were absent.
  - `tests/test_asset_evidence_validator.py` failed first because validator did not block failed bone smoke.
- Targeted tests:
  - Command: `python3 -m pytest tests/test_unity_import_check_source.py tests/test_unity_import_verifier.py tests/test_asset_evidence_validator.py tests/test_generate_asset_evidence_report.py -q`
  - Result: 36 passed.
- Unity verifier:
  - Command pattern: `scripts/verify_unity_fbx_import.sh --fbx evidence/<slot>/export-unity.fbx --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240`
  - Result: all 11 Unity-pass score-3 assets passed with `boneTransformSmoke.passed: true`.
- Production validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --output /tmp/mac-game-rigger-bone-smoke-validator.json --check-evidence-files --require-production-trial --quiet`
  - Result: exit 0.
  - Output summary: schema pass, production gate pass, complete evidence 12, incomplete real assets 0, score3 ratio 0.917.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: 182 passed.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: pass.

## Important Files / Artifacts

- `tools/unity_import_check/Assets/Editor/MacGameRiggerFbxImportCheck.cs`: now records and gates bone-transform smoke.
- `scripts/validate_asset_evidence.py`: score >= 3 Unity pass now requires valid bone smoke evidence.
- `tests/test_unity_import_check_source.py`: source-level regression guard for bone smoke checker behavior.
- `tests/test_asset_evidence_validator.py`: validator regression guard for failed/missing bone smoke.
- `tests/test_generate_asset_evidence_report.py`: report fixtures updated to produce valid stricter Unity evidence.
- `evidence/*/unity-import.json`: refreshed with bone smoke metadata for the 11 Unity-pass score-3 assets.
- `docs/unity-import-verification.md`: documents the stronger verifier scope.
- `docs/asset-evidence-progress.md`: regenerated production evidence report.
- `/tmp/mac-game-rigger-bone-smoke-validator.json`: latest stricter validator output.

## Open Risks

- Unity now proves imported skinned model prefabs instantiate and expose mutable bone links, but still does not prove animation clip playback or avatar retargeting quality.
- `animatorCount` remains 0 because the checker instantiates imported model prefabs, not configured scene prefabs with Animator components.
- Unreal validation remains blocked locally.
- Score-3 assets remain usable-with-cleanup, not score-4 production-clean.
- H-001 remains low-quality score-2 and visual fail.

## Next Steps

1. Add Unity animation clip sampling or generated clip playback smoke, not just direct bone transform mutation.
2. Add Unity scale/orientation checks from instantiated bounds.
3. Add stricter score-4 production-clean gate with shaded/engine-side review evidence.
4. Investigate Unreal import path or document the blocker with equivalent depth.

## Resume Prompt

Continue from this checkpoint. Unity import verification now requires skinned prefab instantiate evidence plus mutable bone-transform smoke for score >= 3 Unity-pass assets. Next, add Unity animation playback/clip sampling or scale/orientation checks beyond direct bone mutation.
