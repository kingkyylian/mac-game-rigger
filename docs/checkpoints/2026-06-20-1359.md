# Project Checkpoint - 2026-06-20 13:59 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Context: standalone Mac-first game rigger; current focus is proving Unity engine-readiness beyond direct bone mutation.

## Done This Session

- Raised the Unity verification gate from mutable bone-transform smoke to generated Unity `AnimationClip` sampling smoke.
- Added Unity checker logic that:
  - resolves a sampled bone path under the imported model instance;
  - creates a generated `AnimationClip`;
  - writes an `AnimationCurve` onto the sampled bone transform;
  - calls `SampleAnimation`;
  - records `animationClipSmoke` with sampled bone, sampled path, and sampled rotation delta;
  - fails the verifier when a skinned mesh renderer exists but animation clip sampling smoke does not pass.
- Tightened production evidence validation so score >= 3 Unity-pass assets now require:
  - `instantiated: true`
  - `skinnedMeshRendererCount >= 1`
  - `boneTransformSmoke.passed: true`
  - `boneCandidateCount >= 1`
  - `rotationDeltaDegrees > 0`
  - `animationClipSmoke.passed: true`
  - non-empty `sampledBone`
  - `sampledRotationDeltaDegrees > 0`
  - available Unity `ModelImporter` metadata.
- Ran the upgraded Unity verifier across all 11 Unity-pass score-3 assets.
- Updated all 11 matching `evidence/<slot>/unity-import.json` files with `animationClipSmoke` results.
- Updated `docs/unity-import-verification.md` and regenerated `docs/asset-evidence-progress.md`.

## Current State

- Schema status: pass.
- Production-trial gate: pass.
- Complete evidence entries: 12.
- Incomplete real assets: 0.
- Score >= 3 count among complete evidence: 11.
- Score >= 3 ratio among complete evidence: 0.917.
- Unity import pass count: 11.
- Every score >= 3 Unity pass now has skinned import evidence, mutable bone-transform smoke, and generated animation clip sampling smoke.

## Unity Animation Clip Smoke Counts

- H-003/H-004/H-005/H-006/H-009/H-010:
  - sampled bone `Hips`, sampled path `MGR_Armature/Hips`, positive sampled rotation delta.
- Q-001/Q-002:
  - sampled bone `Pelvis`, sampled path `MGR_Armature/Pelvis`, positive sampled rotation delta.
- C-001:
  - sampled bone `Pelvis`, sampled path `MGR_Armature/Pelvis`, positive sampled rotation delta.
- P-001/P-002:
  - sampled bone `PropBase`, sampled path `MGR_Armature/PropBase`, positive sampled rotation delta.

## Verification

- RED tests:
  - `tests/test_unity_import_check_source.py` failed first because animation clip source markers were absent.
  - `tests/test_asset_evidence_validator.py` failed first because validator did not block failed animation clip smoke.
- Targeted tests:
  - Command: `python3 -m pytest tests/test_unity_import_check_source.py tests/test_unity_import_verifier.py tests/test_asset_evidence_validator.py tests/test_generate_asset_evidence_report.py -q`
  - Result: 38 passed.
- Unity verifier:
  - Command pattern: `scripts/verify_unity_fbx_import.sh --fbx evidence/<slot>/export-unity.fbx --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240`
  - Result: all 11 Unity-pass score-3 assets passed with `animationClipSmoke.passed: true`.
- Production validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --output /tmp/mac-game-rigger-animation-smoke-validator.json --check-evidence-files --require-production-trial --quiet`
  - Result: exit 0.
  - Output summary: schema pass, production gate pass, complete evidence 12, incomplete real assets 0, score3 ratio 0.917.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: 184 passed.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: pass.

## Important Files / Artifacts

- `tools/unity_import_check/Assets/Editor/MacGameRiggerFbxImportCheck.cs`: now records and gates generated animation clip sampling smoke.
- `scripts/validate_asset_evidence.py`: score >= 3 Unity pass now requires valid animation clip smoke evidence.
- `tests/test_unity_import_check_source.py`: source-level regression guard for animation clip sampling checker behavior.
- `tests/test_asset_evidence_validator.py`: validator regression guard for failed/missing animation clip smoke.
- `tests/test_generate_asset_evidence_report.py`: report fixtures updated to produce valid stricter Unity evidence.
- `evidence/*/unity-import.json`: refreshed with animation clip smoke metadata for the 11 Unity-pass score-3 assets.
- `docs/unity-import-verification.md`: documents the stronger verifier scope.
- `docs/asset-evidence-progress.md`: regenerated production evidence report.
- `/tmp/mac-game-rigger-animation-smoke-validator.json`: latest stricter validator output.

## Open Risks

- Unity now proves generated clip sampling can affect linked bones on imported skinned model prefabs, but still does not prove imported source animation playback or humanoid avatar retargeting quality.
- `animatorCount` remains 0 because the checker instantiates imported model prefabs, not configured scene prefabs with Animator components.
- Unreal validation remains blocked locally.
- Score-3 assets remain usable-with-cleanup, not score-4 production-clean.
- H-001 remains low-quality score-2 and visual fail.

## Next Steps

1. Add imported animation clip playback or Unity Avatar/Animator smoke for humanoids.
2. Add Unity scale/orientation checks from instantiated bounds.
3. Add stricter score-4 production-clean gate with shaded/engine-side review evidence.
4. Investigate Unreal import path or document the blocker with equivalent depth.

## Resume Prompt

Continue from this checkpoint. Unity import verification now requires skinned prefab instantiate evidence, mutable bone-transform smoke, and generated animation clip sampling smoke for score >= 3 Unity-pass assets. Next, add imported animation playback/Avatar/Animator checks or Unity scale/orientation checks.
