# Project Checkpoint - 2026-06-20 14:09 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Context: standalone Mac-first game rigger; current focus is proving Unity engine-readiness beyond animation sampling by adding scale/bounds evidence.

## Done This Session

- Raised the Unity verification gate with renderer bounds/scale smoke.
- Added Unity checker logic that:
  - collects instantiated `Renderer[]` components;
  - unions Unity `Renderer.bounds`;
  - records `boundsSmoke` with center, size, height, and max dimension;
  - fails the verifier when renderers exist but bounds are non-finite or non-positive.
- Tightened production evidence validation so score >= 3 Unity-pass assets now also require:
  - `boundsSmoke.passed: true`
  - `maxDimension > 0`
  - `boundsHeight > 0`.
- Ran the upgraded Unity verifier across all 11 Unity-pass score-3 assets.
- Updated all 11 matching `evidence/<slot>/unity-import.json` files with `boundsSmoke` results.
- Updated `docs/unity-import-verification.md` and regenerated `docs/asset-evidence-progress.md`.

## Current State

- Schema status: pass.
- Production-trial gate: pass.
- Complete evidence entries: 12.
- Incomplete real assets: 0.
- Score >= 3 count among complete evidence: 11.
- Score >= 3 ratio among complete evidence: 0.917.
- Unity import pass count: 11.
- Every score >= 3 Unity pass now has:
  - skinned import evidence;
  - mutable bone-transform smoke;
  - generated animation clip sampling smoke;
  - finite positive renderer bounds smoke.

## Unity Bounds Smoke Notes

- H-003/H-004/H-005/H-009/H-010: humanoid max dimensions are about 3.28-4.10 Unity units.
- Q-001/Q-002: quadruped max dimensions are about 6.17-7.16 Unity units.
- C-001: tail creature max dimension is about 17.43 Unity units.
- P-001/P-002: prop max dimensions are about 1.24 and 0.68 Unity units.
- H-006: `maxDimension: 539.882446`, which passes finite/positive bounds smoke but is a clear scale anomaly candidate for the next stricter threshold gate.

## Verification

- RED tests:
  - `tests/test_unity_import_check_source.py` failed first because bounds smoke source markers were absent.
  - `tests/test_asset_evidence_validator.py` failed first because validator did not block failed bounds smoke.
- Targeted tests:
  - Command: `python3 -m pytest tests/test_unity_import_check_source.py tests/test_unity_import_verifier.py tests/test_asset_evidence_validator.py tests/test_generate_asset_evidence_report.py -q`
  - Result: 40 passed.
- Unity verifier:
  - Command pattern: `scripts/verify_unity_fbx_import.sh --fbx evidence/<slot>/export-unity.fbx --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240`
  - Result: all 11 Unity-pass score-3 assets passed with `boundsSmoke.passed: true`.
- Production validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --output /tmp/mac-game-rigger-bounds-smoke-validator.json --check-evidence-files --require-production-trial --quiet`
  - Result: exit 0.
  - Output summary: schema pass, production gate pass, complete evidence 12, incomplete real assets 0, score3 ratio 0.917.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: 186 passed.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: pass.

## Important Files / Artifacts

- `tools/unity_import_check/Assets/Editor/MacGameRiggerFbxImportCheck.cs`: now records and gates bounds smoke.
- `scripts/validate_asset_evidence.py`: score >= 3 Unity pass now requires valid bounds smoke evidence.
- `tests/test_unity_import_check_source.py`: source-level regression guard for bounds checker behavior.
- `tests/test_asset_evidence_validator.py`: validator regression guard for failed/missing bounds smoke.
- `tests/test_generate_asset_evidence_report.py`: report fixtures updated to produce valid stricter Unity evidence.
- `evidence/*/unity-import.json`: refreshed with bounds smoke metadata for the 11 Unity-pass score-3 assets.
- `docs/unity-import-verification.md`: documents the stronger verifier scope.
- `docs/asset-evidence-progress.md`: regenerated production evidence report.
- `/tmp/mac-game-rigger-bounds-smoke-validator.json`: latest stricter validator output.

## Open Risks

- Unity now proves finite positive renderer bounds, but does not yet enforce category-specific scale thresholds.
- H-006 has a large Unity max dimension around 540 and should be treated as a scale anomaly candidate.
- Unity now proves generated clip sampling can affect linked bones, but still does not prove imported source animation playback or humanoid avatar retargeting quality.
- `animatorCount` remains 0 because the checker instantiates imported model prefabs, not configured scene prefabs with Animator components.
- Unreal validation remains blocked locally.
- Score-3 assets remain usable-with-cleanup, not score-4 production-clean.
- H-001 remains low-quality score-2 and visual fail.

## Next Steps

1. Add category-specific Unity scale thresholds or warning/fail classification using recorded bounds data.
2. Add imported animation clip playback or Unity Avatar/Animator smoke for humanoids.
3. Add stricter score-4 production-clean gate with shaded/engine-side review evidence.
4. Investigate Unreal import path or document the blocker with equivalent depth.

## Resume Prompt

Continue from this checkpoint. Unity import verification now requires skinned prefab instantiate evidence, mutable bone-transform smoke, generated animation clip sampling smoke, and finite positive renderer bounds smoke for score >= 3 Unity-pass assets. Next, add category-specific scale thresholds/warnings or imported animation playback/Avatar/Animator checks.
