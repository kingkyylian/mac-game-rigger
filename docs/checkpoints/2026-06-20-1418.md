# Project Checkpoint - 2026-06-20 14:18 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Context: standalone Mac-first game rigger; current focus is turning measured Unity bounds into actionable scale diagnostics without hiding production risks.

## Done This Session

- Added non-blocking Unity scale warnings based on recorded `boundsSmoke.maxDimension`.
- Added category-specific warning limits:
  - humanoid: 10 Unity units
  - quadruped: 12 Unity units
  - tail creature: 30 Unity units
  - wing creature: 30 Unity units
  - prop: 5 Unity units.
- Kept production evidence completion separate from scale warnings:
  - warnings do not mark a slot incomplete;
  - warnings are included in validator JSON under each slot;
  - warnings are shown in `docs/asset-evidence-progress.md`.
- Updated the Markdown report table with a dedicated `Warnings` column.
- Regenerated `docs/asset-evidence-progress.md`.
- Updated `docs/unity-import-verification.md` to document the warning thresholds.

## Current State

- Schema status: pass.
- Production-trial gate: pass.
- Complete evidence entries: 12.
- Incomplete real assets: 0.
- Score >= 3 count among complete evidence: 11.
- Score >= 3 ratio among complete evidence: 0.917.
- Unity import pass count: 11.
- H-006 remains evidence-complete, but now has a visible Unity scale warning:
  - `H-006: Unity import maxDimension 539.882 exceeds humanoid warning limit 10`.

## Verification

- RED test:
  - `tests/test_asset_evidence_validator.py::test_asset_evidence_validator_warns_for_large_unity_scale_without_blocking_evidence` failed first because validator slots had no `warnings`.
  - `tests/test_generate_asset_evidence_report.py` failed first because the report had no `Warnings` column.
- Targeted tests:
  - Command: `python3 -m pytest tests/test_asset_evidence_validator.py tests/test_generate_asset_evidence_report.py tests/test_unity_import_check_source.py tests/test_unity_import_verifier.py -q`
  - Result: 42 passed.
- Production validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --output /tmp/mac-game-rigger-scale-warning-validator.json --check-evidence-files --require-production-trial --quiet`
  - Result: exit 0.
  - H-006 warning confirmed in `/tmp/mac-game-rigger-scale-warning-validator.json`.
- Report:
  - Command: `python3 scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --output docs/asset-evidence-progress.md`
  - Result: exit 0.
  - H-006 warning confirmed in `docs/asset-evidence-progress.md`.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: 188 passed.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: pass.

## Important Files / Artifacts

- `scripts/validate_asset_evidence.py`: now emits non-blocking Unity scale warnings.
- `scripts/generate_asset_evidence_report.py`: report table now includes `Warnings`.
- `tests/test_asset_evidence_validator.py`: regression coverage for warning without blocking evidence completion.
- `tests/test_generate_asset_evidence_report.py`: regression coverage for warning visibility in Markdown report.
- `docs/asset-evidence-progress.md`: regenerated with H-006 scale warning.
- `docs/unity-import-verification.md`: documents current warning thresholds.
- `/tmp/mac-game-rigger-scale-warning-validator.json`: latest validator output with warning data.

## Open Risks

- H-006 still has a large Unity max dimension around 540; the current warning does not fail production gate.
- Category-specific thresholds are warning-only; no auto-normalization or hard fail exists yet.
- Unity still does not prove imported source animation playback or humanoid avatar retargeting quality.
- `animatorCount` remains 0 because the checker instantiates imported model prefabs, not configured scene prefabs with Animator components.
- Unreal validation remains blocked locally.
- Score-3 assets remain usable-with-cleanup, not score-4 production-clean.
- H-001 remains low-quality score-2 and visual fail.

## Next Steps

1. Add automatic Unity export scale normalization or a hard fail policy for severe scale anomalies.
2. Add imported animation clip playback or Unity Avatar/Animator smoke for humanoids.
3. Add stricter score-4 production-clean gate with shaded/engine-side review evidence.
4. Investigate Unreal import path or document the blocker with equivalent depth.

## Resume Prompt

Continue from this checkpoint. Unity scale anomalies are now represented as non-blocking validator/report warnings, with H-006 flagged at maxDimension 539.882. Next, either add scale normalization/hard-fail policy or add imported animation playback/Avatar/Animator checks.
