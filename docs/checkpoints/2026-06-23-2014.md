# Project Checkpoint - 2026-06-23 20:14 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Context: standalone Mac-first game rigger; current focus is turning Unity scale anomalies from warning-only into a hard production gate.

## Done This Session

- Added category-specific severe Unity scale limits:
  - humanoid: 100 Unity units
  - quadruped: 120 Unity units
  - tail creature: 300 Unity units
  - wing creature: 300 Unity units
  - prop: 50 Unity units.
- Kept warning limits separate from severe limits:
  - warning limits still produce non-blocking `warnings`;
  - severe limits now produce blocking `issues` for score >= 3 Unity-pass assets.
- H-006 now fails evidence completion because its Unity `boundsSmoke.maxDimension` is `539.882446`, exceeding the humanoid severe limit of 100.
- Regenerated `docs/asset-evidence-progress.md`; production gate is now intentionally blocked until H-006 scale is normalized or replaced.
- Updated `docs/unity-import-verification.md` to document severe scale limits and the blocking behavior.

## Current State

- Schema status: pass.
- Production-trial gate: blocked.
- Complete evidence entries: 11.
- Incomplete real assets: 1.
- Score >= 3 count among complete evidence: 10.
- Score >= 3 ratio among complete evidence: 0.909.
- Missing production requirement: `lowPolyHumanoidIncluded`.
- H-006 status:
  - evidence complete: false
  - warning: `H-006: Unity import maxDimension 539.882 exceeds humanoid warning limit 10`
  - issue: `H-006: Unity import maxDimension 539.882 exceeds humanoid severe limit 100`

## Verification

- RED test:
  - `tests/test_asset_evidence_validator.py::test_asset_evidence_validator_blocks_score_three_unity_pass_with_severe_scale_anomaly` failed first because severe scale anomaly did not block evidence completion.
- Targeted tests:
  - Command: `python3 -m pytest tests/test_asset_evidence_validator.py tests/test_generate_asset_evidence_report.py tests/test_unity_import_check_source.py tests/test_unity_import_verifier.py -q`
  - Result: 43 passed.
- Production validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --output /tmp/mac-game-rigger-severe-scale-validator.json --check-evidence-files --require-production-trial --quiet`
  - Result: exit 1, expected because the stricter gate blocks production on H-006 severe scale.
  - Confirmed output: production gate blocked, complete evidence 11, missing `lowPolyHumanoidIncluded`, H-006 incomplete with severe scale issue.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: 189 passed.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: pass.

## Important Files / Artifacts

- `scripts/validate_asset_evidence.py`: severe Unity scale limits now produce blocking issues.
- `tests/test_asset_evidence_validator.py`: regression coverage for severe scale hard-fail.
- `tests/test_generate_asset_evidence_report.py`: report fixture adjusted to keep warning-only scale separate from severe scale.
- `docs/asset-evidence-progress.md`: regenerated with production gate blocked on H-006.
- `docs/unity-import-verification.md`: documents warning and severe scale limits.
- `/tmp/mac-game-rigger-severe-scale-validator.json`: latest validator output proving the blocked gate.

## Open Risks

- H-006 must be normalized, replaced, or explicitly downgraded before production-trial gate can pass again.
- No automatic export scale normalization exists yet.
- Unity still does not prove imported source animation playback or humanoid avatar retargeting quality.
- `animatorCount` remains 0 because the checker instantiates imported model prefabs, not configured scene prefabs with Animator components.
- Unreal validation remains blocked locally.
- Score-3 assets remain usable-with-cleanup, not score-4 production-clean.
- H-001 remains low-quality score-2 and visual fail.

## Next Steps

1. Fix H-006 scale at export time, re-export it, and re-run Unity verifier/evidence validator.
2. If H-006 cannot be normalized reliably, replace it with another low-poly humanoid asset to satisfy `lowPolyHumanoidIncluded`.
3. Add imported animation clip playback or Unity Avatar/Animator smoke for humanoids.
4. Investigate Unreal import path or document the blocker with equivalent depth.

## Resume Prompt

Continue from this checkpoint. Severe Unity scale limits now hard-block score >= 3 Unity-pass assets. Production gate is intentionally blocked because H-006 has maxDimension 539.882 and exceeds the humanoid severe limit of 100. Next, normalize or replace H-006, then regenerate evidence and rerun the production validator.
