# Project Checkpoint - 2026-06-25 00:00 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Context: Mac-first game rigger evidence pipeline. Current milestone: production trial gate now passes after fixing H-006 Unity scale.

## Done This Session

- Investigated H-006 scale anomaly from current evidence.
- Confirmed root cause symptom:
  - imported source bbox was normal, roughly 5.3 units tall;
  - post-rig preview/bind-space bbox had grown to roughly 530 units;
  - Unity imported `maxDimension` was previously about `539.882`.
- Added workflow-side Unity export scale normalization:
  - detects severe category scale anomalies after capsule weighting/cleanup;
  - scales mesh and `MGR_Armature` together before preview/export diagnostics;
  - uses category warning limits with 5% headroom, so humanoid target is `9.5`.
- Added regression tests for:
  - severe humanoid scale normalization planning;
  - no-op behavior for normal humanoid scale;
  - applying scale only to mesh objects and `MGR_Armature`, not landmarks.
- Regenerated H-006 workflow evidence and Unity import evidence.
- Regenerated `docs/asset-evidence-progress.md`.

## Current State

- Schema status: pass.
- Production trial gate: pass.
- Real assets registered: 12.
- Complete evidence entries: 12.
- Score >= 3 count: 11.
- Score >= 3 ratio: 0.917.
- H-006 current Unity import:
  - `boundsSmoke.maxDimension`: `9.673617`
  - `boundsSmoke.boundsHeight`: `9.673617`
  - `boneTransformSmoke.passed`: true
  - `animationClipSmoke.passed`: true
- H-006 workflow summary:
  - `unityScaleNormalization.applied`: true
  - `sourceMaxDimension`: `530.1919`
  - `targetMaxDimension`: `9.5`
  - `scaleFactor`: `0.017918`

## Important Files / Artifacts

- `tools/blender_asset_workflow.py`: adds export scale normalization and records `unityScaleNormalization`.
- `tests/test_blender_asset_workflow.py`: regression tests for the scale normalization plan and application.
- `evidence/H-006/workflow-summary.json`: regenerated H-006 workflow evidence with normalized preview/bind-space size.
- `evidence/H-006/unity-import.json`: Unity import now passes with max dimension below humanoid warning limit.
- `docs/asset-evidence-progress.md`: production trial gate now reports pass.

## Verification

- Command: `python3 -m pytest tests/test_blender_asset_workflow.py::test_unity_export_scale_normalization_plan_scales_severe_humanoid_to_warning_limit tests/test_blender_asset_workflow.py::test_unity_export_scale_normalization_plan_leaves_normal_humanoid_size_unchanged tests/test_blender_asset_workflow.py::test_apply_unity_export_scale_normalization_scales_meshes_and_mgr_armature -q`
  - Result: passed, 3 passed.
- Command: `blender --background --factory-startup --python tools/blender_asset_workflow.py -- --asset local_assets/H-006/H-006-quaternius-animated-woman.fbx --evidence-dir evidence/H-006 --summary evidence/H-006/workflow-summary.json --camera-axis x --template humanoid`
  - Result: passed, regenerated H-006 previews, QA, and Unity FBX export.
- Command: `scripts/verify_unity_fbx_import.sh --fbx evidence/H-006/export-unity.fbx --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240`
  - Result: passed, Unity reported `maxDimension: 9.673617`.
- Command: `python3 scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --output docs/asset-evidence-progress.md --check-evidence-files`
  - Result: passed, report regenerated.
- Command: `python3 -m pytest tests/test_blender_asset_workflow.py tests/test_asset_evidence_validator.py tests/test_generate_asset_evidence_report.py tests/test_unity_import_check_source.py tests/test_unity_import_verifier.py -q`
  - Result: passed, 102 passed.
- Command: `python3 -m pytest tests -q`
  - Result: passed, 192 passed.
- Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial --quiet`
  - Result: passed, exit 0.
- Command: `git diff --check`
  - Result: passed, exit 0.

## Open Questions / Risks

- Score-3 assets are usable-with-cleanup, not score-4 production-clean.
- H-001 is still score 2 and visual fail.
- Unity checker still instantiates imported model prefabs; `animatorCount` remains 0 because configured scene prefabs with Animator components are not part of this smoke.
- Humanoid Avatar/retarget quality is not proven yet.
- Unreal validation remains locally blocked, though production gate accepts explicit Unreal blockers.
- The worktree is intentionally broad and dirty from the larger project; avoid reverting unrelated files.

## Next Steps

1. Add Unity Avatar/Animator smoke for humanoids, including imported clip playback through a configured prefab or generated test scene.
2. Raise at least one humanoid from score 3 to score 4 by improving shoulder/arm/foot deformation and visual QA.
3. Decide Unreal path: install/run Unreal import smoke locally or write a deeper explicit blocker with equivalent evidence.
4. Review prop hinge coverage warnings for P-001/P-002; they pass gate but still report `missingPropHingeCoverage`.

## Resume Prompt

Continue from this checkpoint. First inspect `docs/asset-evidence-progress.md`, `evidence/H-006/workflow-summary.json`, `evidence/H-006/unity-import.json`, and the scale normalization code in `tools/blender_asset_workflow.py`. Production trial gate is now passing; next serious work is Unity Avatar/Animator evidence and pushing selected assets from score 3 toward score 4.
