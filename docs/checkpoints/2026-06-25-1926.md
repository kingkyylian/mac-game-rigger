# Project Checkpoint - 2026-06-25 19:26 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Context: Mac-first game rigger evidence pipeline. Current milestone: production trial gate passes; H-006 now has configured Unity Animator smoke evidence.

## Done This Session

- Extended Unity import checker with configured Animator smoke:
  - adds a temporary `Animator` to the imported instance when needed;
  - creates a temporary `AnimatorController`;
  - adds one state using a generated animation clip;
  - samples a linked bone and reports positive rotation delta.
- Added `configuredAnimatorSmoke` JSON output:
  - `passed`
  - `animatorCount`
  - `controllerAssigned`
  - `stateCount`
  - `sampledBone`
  - `sampledBonePath`
  - `sampledRotationDeltaDegrees`.
- Added validator support:
  - if a humanoid score >= 3 Unity evidence file includes `configuredAnimatorSmoke`, failed/invalid smoke blocks evidence completion;
  - missing `configuredAnimatorSmoke` does not yet block older humanoid evidence until those Unity imports are regenerated.
- Added regression tests for Unity checker source coverage and validator blocking behavior.
- Ran real Unity verifier on H-006 with the new checker.
- Updated `evidence/H-006/unity-import.json` with real configured Animator smoke output.
- Updated `docs/unity-import-verification.md` with the new smoke field and migration note.

## Current State

- Production trial gate: pass.
- Full test suite: 194 passed.
- H-006 current Unity import evidence:
  - `status`: pass
  - `animatorCount`: 1
  - `configuredAnimatorSmoke.passed`: true
  - `configuredAnimatorSmoke.controllerAssigned`: true
  - `configuredAnimatorSmoke.stateCount`: 1
  - `configuredAnimatorSmoke.sampledBone`: `Hips`
  - `configuredAnimatorSmoke.sampledBonePath`: `MGR_Armature/Hips`
  - `configuredAnimatorSmoke.sampledRotationDeltaDegrees`: `90.2450943`
  - `boundsSmoke.maxDimension`: `9.673617`
- H-003 migration attempt:
  - first sandboxed Unity run failed with Unity Package Manager socket `EPERM`;
  - escalated retry reached Unity startup but timed out on Unity Licensing Client before import validation.

## Important Files / Artifacts

- `tools/unity_import_check/Assets/Editor/MacGameRiggerFbxImportCheck.cs`: configured Animator smoke implementation.
- `scripts/validate_asset_evidence.py`: validates present configured Animator smoke for humanoid score >= 3 evidence.
- `tests/test_unity_import_check_source.py`: source guard for configured Animator smoke fields.
- `tests/test_asset_evidence_validator.py`: validator regression for failed configured Animator smoke.
- `evidence/H-006/unity-import.json`: first real configured Animator smoke evidence.
- `docs/unity-import-verification.md`: documents the new Unity verifier behavior and migration status.

## Verification

- RED tests:
  - `python3 -m pytest tests/test_unity_import_check_source.py::test_unity_import_checker_records_configured_animator_smoke_metadata tests/test_asset_evidence_validator.py::test_asset_evidence_validator_blocks_score_three_humanoid_unity_pass_without_configured_animator_smoke -q`
  - Result before implementation: failed for missing checker source fields and missing validator issue.
- GREEN targeted tests:
  - Command: `python3 -m pytest tests/test_unity_import_check_source.py::test_unity_import_checker_records_configured_animator_smoke_metadata tests/test_asset_evidence_validator.py::test_asset_evidence_validator_blocks_score_three_humanoid_unity_pass_without_configured_animator_smoke -q`
  - Result: passed, 2 passed.
- Real Unity verifier:
  - Command: `scripts/verify_unity_fbx_import.sh --fbx evidence/H-006/export-unity.fbx --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240`
  - Result: passed; emitted `configuredAnimatorSmoke.passed: true`.
- Targeted tests:
  - Command: `python3 -m pytest tests/test_unity_import_check_source.py tests/test_asset_evidence_validator.py tests/test_unity_import_verifier.py -q`
  - Result: passed, 33 passed.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: passed, 194 passed.
- Production validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial --quiet`
  - Result: passed, exit 0.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: passed, exit 0.

## Open Questions / Risks

- Configured Animator smoke is currently proven only for H-006.
- H-003/H-004/H-005/H-009/H-010 still need regenerated Unity evidence with `configuredAnimatorSmoke`.
- Unity batchmode is currently flaky because of Unity Licensing Client startup; rerun after Unity Hub/licensing is healthy.
- This is still Generic animation smoke, not full Humanoid Avatar retarget validation.
- Score-3 assets remain usable-with-cleanup, not score-4 production-clean.
- H-001 is still score 2 and visual fail.
- Unreal validation remains locally blocked.

## Next Steps

1. Fix Unity Licensing Client startup, then regenerate Unity import evidence for H-003, H-004, H-005, H-009, and H-010.
2. Once all score >= 3 humanoids have `configuredAnimatorSmoke`, make missing configured Animator smoke a blocking validator requirement.
3. Add a Humanoid Avatar/retarget-specific smoke check separate from Generic Animator smoke.
4. Start score-4 uplift on the strongest humanoid asset after Animator evidence migration.

## Resume Prompt

Continue from this checkpoint. First inspect `evidence/H-006/unity-import.json`, `tools/unity_import_check/Assets/Editor/MacGameRiggerFbxImportCheck.cs`, `scripts/validate_asset_evidence.py`, and `docs/unity-import-verification.md`. H-006 has real configured Animator smoke evidence. Next, resolve Unity Licensing Client flakiness and regenerate configured Animator smoke evidence for the remaining score >= 3 humanoid Unity-pass assets.
