# Project Checkpoint - 2026-06-25 19:33 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Context: production trial gate passes; current focus is migrating all score >= 3 humanoid Unity evidence to configured Animator smoke.

## Done This Session

- Added non-blocking migration warnings for missing humanoid `configuredAnimatorSmoke`.
- The validator now reports a warning when:
  - slot category is `humanoid`;
  - deformation score is >= 3;
  - Unity import status is pass;
  - `evidence/<slot>/unity-import.json` has no `result.configuredAnimatorSmoke`.
- Existing failed/invalid `configuredAnimatorSmoke` remains blocking for humanoid score >= 3 evidence.
- Regenerated `docs/asset-evidence-progress.md`.
- Report now shows configured Animator smoke migration gaps in the `Warnings` column.

## Current State

- Production trial gate: pass.
- Complete evidence entries: 12.
- Score >= 3 count: 11.
- Score >= 3 ratio: 0.917.
- H-006 has real configured Animator smoke evidence and no migration warning.
- Remaining configured Animator smoke migration warnings:
  - H-003
  - H-004
  - H-005
  - H-009
  - H-010

## Important Files / Artifacts

- `scripts/validate_asset_evidence.py`: adds `unity_import_animator_migration_warnings`.
- `tests/test_asset_evidence_validator.py`: covers non-blocking missing `configuredAnimatorSmoke` warning.
- `tests/test_generate_asset_evidence_report.py`: covers warning visibility in Markdown report.
- `docs/asset-evidence-progress.md`: regenerated with migration warnings.
- `docs/checkpoints/LATEST.md`: updated to this checkpoint.

## Verification

- RED tests:
  - Command: `python3 -m pytest tests/test_asset_evidence_validator.py::test_asset_evidence_validator_warns_for_missing_configured_animator_smoke_without_blocking_evidence tests/test_generate_asset_evidence_report.py::test_generate_asset_evidence_report_shows_missing_configured_animator_smoke_warning -q`
  - Result before implementation: failed, warnings were missing.
- GREEN tests:
  - Command: `python3 -m pytest tests/test_asset_evidence_validator.py::test_asset_evidence_validator_warns_for_missing_configured_animator_smoke_without_blocking_evidence tests/test_generate_asset_evidence_report.py::test_generate_asset_evidence_report_shows_missing_configured_animator_smoke_warning -q`
  - Result: passed, 2 passed.
- Report generation:
  - Command: `python3 scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --output docs/asset-evidence-progress.md --check-evidence-files`
  - Result: passed, report regenerated.
- Targeted tests:
  - Command: `python3 -m pytest tests/test_asset_evidence_validator.py tests/test_generate_asset_evidence_report.py tests/test_unity_import_check_source.py tests/test_unity_import_verifier.py -q`
  - Result: passed, 47 passed.
- Production validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial --quiet`
  - Result: passed, exit 0.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: passed, 196 passed.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: passed, exit 0.

## Open Questions / Risks

- Unity Licensing Client flakiness still blocks reliable batch regeneration for H-003/H-004/H-005/H-009/H-010.
- Missing `configuredAnimatorSmoke` is intentionally warning-only until remaining humanoid evidence is regenerated.
- Current smoke is Generic Animator/controller sampling, not Humanoid Avatar retarget validation.
- H-001 remains score 2 and visual fail.
- Unreal validation remains locally blocked.

## Next Steps

1. Fix Unity Licensing Client startup or run Unity verifier from a clean GUI-authenticated Unity Hub state.
2. Regenerate Unity import evidence for H-003, H-004, H-005, H-009, and H-010.
3. Once all score >= 3 humanoids record `configuredAnimatorSmoke`, make missing smoke a blocking validator issue.
4. Add Humanoid Avatar/retarget-specific smoke after Generic Animator migration is complete.

## Resume Prompt

Continue from this checkpoint. First inspect `docs/asset-evidence-progress.md`, `scripts/validate_asset_evidence.py`, and `evidence/H-006/unity-import.json`. Production gate passes, H-006 has configured Animator smoke, and H-003/H-004/H-005/H-009/H-010 show non-blocking migration warnings. Next, resolve Unity Licensing Client flakiness and regenerate those five Unity evidence files.
