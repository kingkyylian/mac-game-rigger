# Project Checkpoint - 2026-06-25 19:41 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Recent commits:
  - `dd5f340 feat: add humanoid stress pose evidence`
  - `b3cf7b9 fix: normalize humanoid preview orientation`
  - `8fd8974 fix: validate readable preview evidence`
  - `d7f66a0 data: add H-006 low-poly failure evidence`
  - `b039d49 tool: improve H-001 pose evidence`
- Context: production trial gate passes; current focus is hardening repeatable Unity evidence for game-ready rig confidence.

## Done This Session

- Added `scripts/record_unity_import_evidence.py`.
- The recorder:
  - runs `scripts/verify_unity_fbx_import.sh`;
  - wraps the raw Unity verifier payload in the existing `unity-import.json` evidence schema;
  - writes to `evidence/<slot>/unity-import.json` by default;
  - records `schemaVersion`, `status`, `checkedOn`, `unityEditor`, `fbx`, `verificationCommand`, and raw `result`;
  - exits non-zero without overwriting existing evidence when Unity/verifier fails.
- Added focused TDD coverage in `tests/test_record_unity_import_evidence.py`.
- Documented recorder usage in `docs/unity-import-verification.md`.

## Current State

- Production trial gate: pass.
- Full Python test suite: pass.
- Complete evidence entries: 12.
- Score >= 3 count: 11.
- Score >= 3 ratio: 0.917.
- H-006 has real configured Animator smoke evidence and no migration warning.
- Remaining configured Animator smoke migration warnings:
  - H-003
  - H-004
  - H-005
  - H-009
  - H-010

## Important Files / Artifacts

- `scripts/record_unity_import_evidence.py`: repeatable Unity evidence recorder.
- `tests/test_record_unity_import_evidence.py`: covers default output path, evidence wrapper shape, successful CLI write, and preserving existing evidence on verifier failure.
- `docs/unity-import-verification.md`: documents recorder-first evidence workflow.
- `scripts/validate_asset_evidence.py`: still emits non-blocking warnings for missing humanoid `configuredAnimatorSmoke`.
- `evidence/H-006/unity-import.json`: current strongest Unity rig evidence, with configured Animator smoke passing.
- `docs/asset-evidence-progress.md`: shows remaining migration warnings.

## Verification

- RED test:
  - Command: `python3 -m pytest tests/test_record_unity_import_evidence.py -q`
  - Result before implementation: failed, 4 failed because `scripts/record_unity_import_evidence.py` was missing.
- Focused recorder tests:
  - Command: `python3 -m pytest tests/test_record_unity_import_evidence.py -q`
  - Result: passed, 4 passed.
- Targeted Unity/evidence tests:
  - Command: `python3 -m pytest tests/test_record_unity_import_evidence.py tests/test_unity_import_verifier.py tests/test_asset_evidence_validator.py tests/test_generate_asset_evidence_report.py tests/test_unity_import_check_source.py -q`
  - Result: passed, 51 passed.
- Production validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial --quiet`
  - Result: passed, exit 0.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: passed, 200 passed.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: passed, exit 0.

## Open Questions / Risks

- Unity Licensing Client flakiness still blocks reliable batch regeneration for H-003/H-004/H-005/H-009/H-010.
- Missing `configuredAnimatorSmoke` is intentionally warning-only until remaining humanoid evidence is regenerated.
- Current Unity smoke is Generic Animator/controller sampling, not Humanoid Avatar retarget validation.
- Unreal validation remains locally blocked.
- H-001 remains score 2 and visual fail.

## Next Steps

1. Run `scripts/record_unity_import_evidence.py` for H-003, H-004, H-005, H-009, and H-010 from a Unity-authenticated environment.
2. Regenerate `docs/asset-evidence-progress.md` and verify warnings disappear for those slots.
3. Make missing humanoid `configuredAnimatorSmoke` blocking for score >= 3 Unity-pass humanoids.
4. Add Humanoid Avatar/retarget-specific smoke after Generic Animator migration is complete.

## Resume Prompt

Continue from this checkpoint. First inspect `docs/asset-evidence-progress.md`, `scripts/record_unity_import_evidence.py`, `scripts/validate_asset_evidence.py`, and `evidence/H-006/unity-import.json`. Production gate passes; H-006 has configured Animator smoke; H-003/H-004/H-005/H-009/H-010 still need regenerated Unity evidence using the new recorder. Follow project rules: Turkish concise updates, TDD for behavior changes, and fresh verification before completion claims.
