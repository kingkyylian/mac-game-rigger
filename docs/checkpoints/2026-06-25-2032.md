# Project Checkpoint - 2026-06-25 20:32 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Recent commits:
  - `dd5f340 feat: add humanoid stress pose evidence`
  - `b3cf7b9 fix: normalize humanoid preview orientation`
  - `8fd8974 fix: validate readable preview evidence`
  - `d7f66a0 data: add H-006 low-poly failure evidence`
  - `b039d49 tool: improve H-001 pose evidence`
- Context: production trial gate passes; current focus is hardening repeatable Unity evidence for game-ready rig confidence.

## Done This Session

- Attempted to regenerate H-003 Unity import evidence with the new recorder:
  - `scripts/record_unity_import_evidence.py --fbx evidence/H-003/export-unity.fbx --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240`
- Sandboxed run failed before validation with Unity Package Manager socket `listen EPERM`.
- Sandbox-external run reached Unity startup but timed out after 240 seconds on Unity Licensing Client/headless bootstrap:
  - `Timed-out after 60.00s, waiting for channel: "LicenseClient-kyylian"`
  - `Error: 'com.unity.editor.headless' was not found.`
- Existing H-003 evidence was preserved; the recorder did not overwrite on failure.
- Added failure classification to `scripts/record_unity_import_evidence.py`:
  - Unity licensing/bootstrap failures now emit an explicit recovery hint.
  - Unity Package Manager socket failures now emit a sandbox-specific hint.
- Added TDD coverage for licensing/bootstrap classification.
- Updated `docs/unity-import-verification.md` with the observed external licensing/headless blocker and recovery path.

## Current State

- Production trial gate: pass.
- Full Python test suite: pass.
- Complete evidence entries: 12.
- Score >= 3 count: 11.
- Score >= 3 ratio: 0.917.
- H-006 has real configured Animator smoke evidence and no migration warning.
- Remaining configured Animator smoke migration warnings:
  - H-003
  - H-004
  - H-005
  - H-009
  - H-010
- Current evidence inspection:
  - H-003: Unity `status=pass`, no `configuredAnimatorSmoke`.
  - H-004: Unity `status=pass`, no `configuredAnimatorSmoke`.
  - H-005: Unity `status=pass`, no `configuredAnimatorSmoke`.
  - H-009: Unity `status=pass`, no `configuredAnimatorSmoke`.
  - H-010: Unity `status=pass`, no `configuredAnimatorSmoke`.

## Important Files / Artifacts

- `scripts/record_unity_import_evidence.py`: repeatable Unity evidence recorder; now classifies licensing/bootstrap and Package Manager socket failures.
- `tests/test_record_unity_import_evidence.py`: covers wrapper behavior, failure preservation, and licensing/bootstrap failure hints.
- `docs/unity-import-verification.md`: documents recorder workflow plus sandbox and external licensing blockers.
- `docs/asset-evidence-progress.md`: still shows H-003/H-004/H-005/H-009/H-010 configured Animator smoke migration warnings.
- `evidence/H-006/unity-import.json`: current strongest Unity rig evidence, with configured Animator smoke passing.

## Verification

- Real H-003 recorder run:
  - Sandbox command: `scripts/record_unity_import_evidence.py --fbx evidence/H-003/export-unity.fbx --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240`
  - Result: failed with Unity Package Manager `listen EPERM`; no evidence overwrite.
  - Sandbox-external command: same command with escalation.
  - Result: failed after 240 seconds with Unity Licensing Client/headless bootstrap timeout; no evidence overwrite.
- RED test:
  - Command: `python3 -m pytest tests/test_record_unity_import_evidence.py::test_cli_classifies_unity_licensing_bootstrap_failures -q`
  - Result before implementation: failed because the licensing hint was missing.
- Focused recorder tests:
  - Command: `python3 -m pytest tests/test_record_unity_import_evidence.py -q`
  - Result: passed, 5 passed.
- Targeted Unity/evidence tests:
  - Command: `python3 -m pytest tests/test_record_unity_import_evidence.py tests/test_unity_import_verifier.py tests/test_asset_evidence_validator.py tests/test_generate_asset_evidence_report.py tests/test_unity_import_check_source.py -q`
  - Result: passed, 52 passed.
- Production validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial --quiet`
  - Result: passed, exit 0.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: passed, 201 passed.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: passed, exit 0.

## Open Questions / Risks

- Unity Licensing Client/headless bootstrap is now the active blocker for regenerating H-003/H-004/H-005/H-009/H-010 configured Animator smoke evidence.
- The next Unity run likely needs Unity Hub or the Unity Editor opened once interactively to refresh licensing before rerunning the recorder outside the sandbox.
- `ps` inspection was blocked by sandbox permissions, so process cleanup was not attempted.
- Missing `configuredAnimatorSmoke` is intentionally warning-only until remaining humanoid evidence is regenerated.
- Current Unity smoke is Generic Animator/controller sampling, not Humanoid Avatar retarget validation.
- Unreal validation remains locally blocked.

## Next Steps

1. Open Unity Hub or Unity Editor interactively on the Mac and confirm licensing is healthy.
2. Rerun `scripts/record_unity_import_evidence.py` for H-003, H-004, H-005, H-009, and H-010 outside the sandbox.
3. Regenerate `docs/asset-evidence-progress.md` and verify the configured Animator smoke warnings disappear.
4. Make missing humanoid `configuredAnimatorSmoke` blocking for score >= 3 Unity-pass humanoids.
5. Add Humanoid Avatar/retarget-specific smoke after Generic Animator migration is complete.

## Resume Prompt

Continue from this checkpoint. First inspect `docs/asset-evidence-progress.md`, `scripts/record_unity_import_evidence.py`, `docs/unity-import-verification.md`, and `evidence/H-006/unity-import.json`. Production gate passes; H-006 has configured Animator smoke; H-003/H-004/H-005/H-009/H-010 still need regenerated Unity evidence. The latest real H-003 recorder attempt failed outside the sandbox with Unity Licensing Client/headless bootstrap timeout, not FBX validation failure. Refresh Unity licensing interactively, then rerun the recorder outside the sandbox.
