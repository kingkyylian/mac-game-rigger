# Project Checkpoint - 2026-06-25 20:46 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Recent commits:
  - `dd5f340 feat: add humanoid stress pose evidence`
  - `b3cf7b9 fix: normalize humanoid preview orientation`
  - `8fd8974 fix: validate readable preview evidence`
  - `d7f66a0 data: add H-006 low-poly failure evidence`
  - `b039d49 tool: improve H-001 pose evidence`
- Context: production trial gate passes; current focus is hardening repeatable Unity evidence for game-ready rig confidence.

## Done This Session

- Re-read the latest checkpoint and current evidence state.
- Opened Unity Hub with `open -a "Unity Hub"` to try refreshing Unity licensing.
- Reran H-003 recorder outside the sandbox:
  - `scripts/record_unity_import_evidence.py --fbx evidence/H-003/export-unity.fbx --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240`
- Result was still a Unity Licensing Client/headless bootstrap timeout, not an FBX validation failure.
- Existing H-003 evidence remained preserved:
  - H-003: Unity `status=pass`, no `configuredAnimatorSmoke`, checkedOn `2026-06-20`.
- Added `scripts/plan_unity_animator_smoke_migration.py`.
- The planner scans manifest/evidence and prints exact recorder commands for humanoid score >= 3 Unity-pass assets missing `configuredAnimatorSmoke`.
- Added TDD coverage in `tests/test_plan_unity_animator_smoke_migration.py`.
- Documented the planner in `docs/unity-import-verification.md`.

## Current State

- Production trial gate: pass.
- Full Python test suite: pass.
- Complete evidence entries: 12.
- Score >= 3 count: 11.
- Score >= 3 ratio: 0.917.
- H-006 has real configured Animator smoke evidence and no migration warning.
- Planner currently reports these migration gaps:
  - H-003
  - H-004
  - H-005
  - H-009
  - H-010
- Current evidence inspection:
  - H-003: Unity `status=pass`, no `configuredAnimatorSmoke`, checkedOn `2026-06-20`.
  - H-004: Unity `status=pass`, no `configuredAnimatorSmoke`, checkedOn `2026-06-20`.
  - H-005: Unity `status=pass`, no `configuredAnimatorSmoke`, checkedOn `2026-06-20`.
  - H-009: Unity `status=pass`, no `configuredAnimatorSmoke`, checkedOn `2026-06-20`.
  - H-010: Unity `status=pass`, no `configuredAnimatorSmoke`, checkedOn `2026-06-20`.

## Important Files / Artifacts

- `scripts/record_unity_import_evidence.py`: repeatable Unity evidence recorder; classifies licensing/bootstrap and Package Manager socket failures.
- `tests/test_record_unity_import_evidence.py`: covers recorder wrapper behavior, failure preservation, and licensing/bootstrap hints.
- `scripts/plan_unity_animator_smoke_migration.py`: lists current configured Animator smoke migration gaps and recorder commands.
- `tests/test_plan_unity_animator_smoke_migration.py`: covers gap filtering, JSON output, and runnable text commands.
- `docs/unity-import-verification.md`: documents recorder, planner, sandbox failure, and licensing/headless blocker.
- `docs/asset-evidence-progress.md`: still shows H-003/H-004/H-005/H-009/H-010 configured Animator smoke warnings.
- `evidence/H-006/unity-import.json`: current strongest Unity rig evidence, with configured Animator smoke passing.

## Verification

- Unity Hub refresh attempt:
  - Command: `open -a "Unity Hub"`
  - Result: command returned exit 0.
- Real H-003 recorder retry:
  - Command: `scripts/record_unity_import_evidence.py --fbx evidence/H-003/export-unity.fbx --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240`
  - Result: failed after 240 seconds with Unity Licensing Client/headless bootstrap timeout; recorder emitted the licensing/bootstrap hint and did not overwrite evidence.
- RED test for planner:
  - Command: `python3 -m pytest tests/test_plan_unity_animator_smoke_migration.py -q`
  - Result before implementation: failed, 3 failed because `scripts/plan_unity_animator_smoke_migration.py` was missing.
- Focused planner tests:
  - Command: `python3 -m pytest tests/test_plan_unity_animator_smoke_migration.py -q`
  - Result: passed, 3 passed.
- Planner real manifest smoke:
  - Command: `scripts/plan_unity_animator_smoke_migration.py --manifest samples/manifest.json --evidence-root . --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240`
  - Result: passed and listed H-003/H-004/H-005/H-009/H-010.
- Targeted Unity/evidence tests:
  - Command: `python3 -m pytest tests/test_plan_unity_animator_smoke_migration.py tests/test_record_unity_import_evidence.py tests/test_unity_import_verifier.py tests/test_asset_evidence_validator.py tests/test_generate_asset_evidence_report.py tests/test_unity_import_check_source.py -q`
  - Result: passed, 55 passed.
- Production validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial --quiet`
  - Result: passed, exit 0.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: passed, 204 passed.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: passed, exit 0.

## Open Questions / Risks

- Unity Licensing Client/headless bootstrap is still the active blocker for regenerating H-003/H-004/H-005/H-009/H-010 configured Animator smoke evidence.
- Opening Unity Hub from CLI was not enough; the user may need to interactively open Unity Hub/Editor, confirm account/license state, and leave it healthy before rerunning the recorder.
- Missing `configuredAnimatorSmoke` is intentionally warning-only until remaining humanoid evidence is regenerated.
- Current Unity smoke is Generic Animator/controller sampling, not Humanoid Avatar retarget validation.
- Unreal validation remains locally blocked.

## Next Steps

1. Interactively open Unity Hub or Unity Editor and confirm the Unity account/license is active.
2. Run `scripts/plan_unity_animator_smoke_migration.py --manifest samples/manifest.json --evidence-root . --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240`.
3. Execute the listed `scripts/record_unity_import_evidence.py` commands outside the sandbox.
4. Regenerate `docs/asset-evidence-progress.md` and verify the configured Animator smoke warnings disappear.
5. Make missing humanoid `configuredAnimatorSmoke` blocking for score >= 3 Unity-pass humanoids.

## Resume Prompt

Continue from this checkpoint. First inspect `docs/asset-evidence-progress.md`, `scripts/plan_unity_animator_smoke_migration.py`, `scripts/record_unity_import_evidence.py`, `docs/unity-import-verification.md`, and `evidence/H-006/unity-import.json`. Production gate passes; H-006 has configured Animator smoke; planner reports H-003/H-004/H-005/H-009/H-010 as gaps. Opening Unity Hub via CLI did not fix the headless licensing timeout. The next real external step is interactive Unity Hub/Editor license confirmation, then rerun the planner commands outside the sandbox.
