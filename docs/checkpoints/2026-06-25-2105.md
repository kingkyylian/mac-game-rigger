# Project Checkpoint - 2026-06-25 21:05 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Recent commits:
  - `dd5f340 feat: add humanoid stress pose evidence`
  - `b3cf7b9 fix: normalize humanoid preview orientation`
  - `8fd8974 fix: validate readable preview evidence`
  - `d7f66a0 data: add H-006 low-poly failure evidence`
  - `b039d49 tool: improve H-001 pose evidence`
- Context: production trial gate passes; current focus is hardening repeatable Unity evidence for game-ready rig confidence.

## Done This Session

- Added `scripts/check_unity_batchmode_health.py`.
- The health checker:
  - starts Unity with `-batchmode -quit -nographics`;
  - writes/reads a temporary Unity log;
  - fails quickly on timeout or non-zero Unity exit;
  - reuses the recorder licensing/socket failure classifier.
- Integrated preflight into `scripts/run_unity_animator_smoke_migration.py`.
- Batch runner now:
  - runs Unity batchmode health check before recorder commands by default;
  - supports `--skip-preflight` when health was already checked;
  - supports `--health-checker` and `--preflight-timeout-seconds`;
  - still supports `--dry-run` without starting Unity.
- Added focused TDD coverage:
  - `tests/test_check_unity_batchmode_health.py`
  - updated `tests/test_run_unity_animator_smoke_migration.py`
- Updated `docs/unity-import-verification.md` with preflight-first migration flow.

## Current State

- Production trial gate: pass.
- Full Python test suite: pass.
- Complete evidence entries: 12.
- Score >= 3 count: 11.
- Score >= 3 ratio: 0.917.
- H-006 has real configured Animator smoke evidence and no migration warning.
- Current configured Animator smoke migration gaps:
  - H-003
  - H-004
  - H-005
  - H-009
  - H-010
- Unity Licensing Client/headless bootstrap remains the active external blocker.
- Current real health check evidence:
  - Unity Package Manager IPC connected quickly.
  - Unity still timed out after 45 seconds with licensing not initialized.
  - The health checker classified it as `Unity licensing/bootstrap failure detected`.

## Important Files / Artifacts

- `scripts/check_unity_batchmode_health.py`: fast Unity batchmode health/preflight checker.
- `tests/test_check_unity_batchmode_health.py`: health checker tests.
- `scripts/run_unity_animator_smoke_migration.py`: batch runner with default preflight.
- `tests/test_run_unity_animator_smoke_migration.py`: batch runner/preflight tests.
- `scripts/record_unity_import_evidence.py`: single-FBX Unity evidence recorder.
- `scripts/plan_unity_animator_smoke_migration.py`: configured Animator smoke gap planner.
- `docs/unity-import-verification.md`: documents preflight, planner, batch runner, sandbox failure, and licensing/headless blocker.
- `docs/asset-evidence-progress.md`: still shows H-003/H-004/H-005/H-009/H-010 configured Animator smoke warnings.
- `evidence/H-006/unity-import.json`: current strongest Unity rig evidence, with configured Animator smoke passing.

## Verification

- RED tests:
  - Command: `python3 -m pytest tests/test_check_unity_batchmode_health.py -q`
  - Result before implementation: failed, 3 failed because `scripts/check_unity_batchmode_health.py` was missing.
  - Command: `python3 -m pytest tests/test_run_unity_animator_smoke_migration.py -q`
  - Result before implementation: failed, 5 failed because preflight args were missing.
- Focused preflight/runner tests:
  - Command: `python3 -m pytest tests/test_check_unity_batchmode_health.py tests/test_run_unity_animator_smoke_migration.py -q`
  - Result: passed, 8 passed.
- Real Unity health check:
  - Command: `scripts/check_unity_batchmode_health.py --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 45`
  - Result: failed with exit 124 after 45 seconds; classified licensing/bootstrap failure; no evidence files touched.
- Real batch dry-run:
  - Command: `scripts/run_unity_animator_smoke_migration.py --manifest samples/manifest.json --evidence-root . --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240 --dry-run`
  - Result: passed and listed H-003/H-004/H-005/H-009/H-010 recorder commands without starting Unity.
- Targeted Unity/evidence tests:
  - Command: `python3 -m pytest tests/test_check_unity_batchmode_health.py tests/test_run_unity_animator_smoke_migration.py tests/test_plan_unity_animator_smoke_migration.py tests/test_record_unity_import_evidence.py tests/test_unity_import_verifier.py tests/test_asset_evidence_validator.py tests/test_generate_asset_evidence_report.py tests/test_unity_import_check_source.py -q`
  - Result: passed, 63 passed.
- Production validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial --quiet`
  - Result: passed, exit 0.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: passed, 212 passed.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: passed, exit 0.

## Open Questions / Risks

- Unity licensing is still not healthy enough for headless batchmode import validation.
- The user likely needs to interactively open Unity Hub/Editor, confirm account/license state, and leave it healthy before running the batch runner.
- Missing `configuredAnimatorSmoke` is intentionally warning-only until remaining humanoid evidence is regenerated.
- Current Unity smoke is Generic Animator/controller sampling, not Humanoid Avatar retarget validation.
- Unreal validation remains locally blocked.

## Next Steps

1. Interactively open Unity Hub or Unity Editor and confirm Unity account/license is active.
2. Run `scripts/check_unity_batchmode_health.py --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 90` outside the sandbox.
3. If health passes, run `scripts/run_unity_animator_smoke_migration.py --manifest samples/manifest.json --evidence-root . --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240` outside the sandbox.
4. Regenerate `docs/asset-evidence-progress.md` and verify configured Animator smoke warnings disappear.
5. Make missing humanoid `configuredAnimatorSmoke` blocking for score >= 3 Unity-pass humanoids.

## Resume Prompt

Continue from this checkpoint. First inspect `scripts/check_unity_batchmode_health.py`, `scripts/run_unity_animator_smoke_migration.py`, `scripts/record_unity_import_evidence.py`, `docs/asset-evidence-progress.md`, and `evidence/H-006/unity-import.json`. Production gate passes; H-006 has configured Animator smoke; batch dry-run lists H-003/H-004/H-005/H-009/H-010 as gaps. Current real Unity preflight fails after 45 seconds with licensing/bootstrap not initialized, while Package Manager IPC connects. Once Unity licensing is interactively healthy, rerun health check and then batch migration outside the sandbox.
