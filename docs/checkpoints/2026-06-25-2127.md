# Project Checkpoint - 2026-06-25 21:27 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, dirty worktree by design; do not revert unrelated changes.
- Recent commits:
  - `dd5f340 feat: add humanoid stress pose evidence`
  - `b3cf7b9 fix: normalize humanoid preview orientation`
  - `8fd8974 fix: validate readable preview evidence`
  - `d7f66a0 data: add H-006 low-poly failure evidence`
  - `b039d49 tool: improve H-001 pose evidence`
- Context: production trial gate passes; current focus is hardening repeatable Unity evidence for game-ready rig confidence.

## Done This Session

- Kept the generated evidence report's `## Strict Quality Gates` section, but changed the source of truth:
  - before: strict configured Animator gate was inferred from warning strings in the normal report;
  - now: `scripts/generate_asset_evidence_report.py` also runs `validate_manifest(... require_configured_animator_smoke=True)` and renders the gate from that strict validator result.
- Added regression coverage for a failed configured Animator smoke case:
  - a humanoid score >= 3 Unity-pass asset with `configuredAnimatorSmoke: {"passed": false}` now blocks `configuredAnimatorSmokeForHumanoidScore3`.
- Regenerated `docs/asset-evidence-progress.md` with the new report path.

## Current State

- Production trial gate: pass.
- Strict configured Animator smoke gate: blocked.
- Strict gate missing/failing slots:
  - H-003
  - H-004
  - H-005
  - H-009
  - H-010
- H-006 has real configured Animator smoke evidence and no migration warning.
- Normal production validator: pass.
- Strict configured Animator smoke validator: expected fail until the remaining five humanoid Unity evidence files are regenerated.
- Unity Licensing Client/headless bootstrap remains the active external blocker for recording those five gaps.

## Important Files / Artifacts

- `scripts/generate_asset_evidence_report.py`: report now computes a strict validator report and renders `configuredAnimatorSmokeForHumanoidScore3` from that result.
- `tests/test_generate_asset_evidence_report.py`: added failed configured Animator smoke regression coverage.
- `docs/asset-evidence-progress.md`: regenerated; production gate pass, strict configured Animator gate blocked with H-003/H-004/H-005/H-009/H-010.
- `scripts/validate_asset_evidence.py`: opt-in `--require-configured-animator-smoke` strict validator flag remains available.
- `scripts/check_unity_batchmode_health.py`: fast Unity batchmode health/preflight checker.
- `scripts/run_unity_animator_smoke_migration.py`: batch runner with default preflight.
- `evidence/H-006/unity-import.json`: current strongest Unity rig evidence, with configured Animator smoke passing.

## Verification

- RED regression test:
  - Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py::test_generate_asset_evidence_report_strict_gate_blocks_failed_configured_animator_smoke -q`
  - Result before implementation: failed because the strict gate did not block H-001 when `configuredAnimatorSmoke.passed` was false.
- GREEN regression test:
  - Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py::test_generate_asset_evidence_report_strict_gate_blocks_failed_configured_animator_smoke -q`
  - Result: passed, 1 passed.
- Focused report tests:
  - Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py -q`
  - Result: passed, 15 passed.
- Report regeneration:
  - Command: `python3 scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --output docs/asset-evidence-progress.md`
  - Result: passed, report regenerated.
- Normal production validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial --quiet`
  - Result: passed, exit 0.
- Strict configured Animator smoke validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-configured-animator-smoke --quiet`
  - Result: failed with exit 1 as expected because H-003/H-004/H-005/H-009/H-010 are missing `configuredAnimatorSmoke`.
- Focused validator/report tests:
  - Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py tests/test_asset_evidence_validator.py -q`
  - Result: passed, 41 passed.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: passed, 215 passed.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: passed, exit 0.

## Open Questions / Risks

- Unity licensing is still not healthy enough for headless batchmode import validation.
- The strict gate is visible in the report and opt-in in validator, but should not become default production-trial enforcement until H-003/H-004/H-005/H-009/H-010 are regenerated.
- Current Unity smoke is Generic Animator/controller sampling, not full Humanoid Avatar retarget validation.
- Unreal validation remains locally blocked.

## Next Steps

1. Interactively open Unity Hub or Unity Editor and confirm Unity account/license is active.
2. Run `scripts/check_unity_batchmode_health.py --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 90` outside the sandbox.
3. If health passes, run `scripts/run_unity_animator_smoke_migration.py --manifest samples/manifest.json --evidence-root . --unity /Applications/Unity/Hub/Editor/6000.4.1f1/Unity.app/Contents/MacOS/Unity --timeout-seconds 240` outside the sandbox.
4. Regenerate `docs/asset-evidence-progress.md` and verify strict gate changes from blocked to pass.
5. Flip strict configured Animator smoke enforcement into the default production trial path when the five gaps are closed.

## Resume Prompt

Continue from this checkpoint. First inspect `docs/asset-evidence-progress.md`, `scripts/generate_asset_evidence_report.py`, `scripts/validate_asset_evidence.py`, `scripts/check_unity_batchmode_health.py`, and `scripts/run_unity_animator_smoke_migration.py`. Production gate passes, but the report shows strict `configuredAnimatorSmokeForHumanoidScore3` blocked for H-003/H-004/H-005/H-009/H-010. Remaining external blocker is Unity Licensing Client/headless bootstrap.
