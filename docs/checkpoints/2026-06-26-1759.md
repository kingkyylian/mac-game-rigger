# Project Checkpoint - 2026-06-26 17:59 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, tracking `origin/main`.
- GitHub: `https://github.com/kingkyylian/mac-game-rigger`, private.
- Context: moving from pushed alpha snapshot toward product-level release discipline.

## Done This Session

- Added `.github/workflows/ci.yml`.
  - Runs on push to `main` and pull requests.
  - Uses Python 3.11.
  - Installs `pytest`.
  - Runs `scripts/run_full_alpha_smoke.sh --skip-blender`.
  - Uploads `dist/MacGameRigger-0.1.0.zip` as an artifact.
- Updated `docs/release-checklist.md` to current evidence reality:
  - real asset production trial gate is now `pass`;
  - strict humanoid configured Animator gate is still `blocked`;
  - CI smoke gate is configured but remote execution is blocked by GitHub billing/spending limit.
- Pushed two commits:
  - `7728587 ci: add alpha smoke gate`
  - `e018b1b docs: record remote ci billing blocker`

## Current State

- Local working tree: clean.
- Remote `main`: `e018b1b2f7c88b52b36433308eba03d50e60464e`.
- Local alpha smoke gate: pass.
- Production evidence validator: pass.
- GitHub Actions workflow exists, but remote runs fail before any step starts.
- Remote CI blocker:
  - GitHub Actions annotation says recent account payments failed or spending limit must be increased.
  - This is an account/repo billing state issue, not a test or YAML failure.

## Important Files / Artifacts

- `.github/workflows/ci.yml`: new CI smoke gate.
- `docs/release-checklist.md`: updated product/release truth table.
- `docs/asset-evidence-progress.md`: production trial pass; strict configured Animator smoke gate blocked for H-003/H-004/H-005/H-009/H-010.
- `scripts/run_full_alpha_smoke.sh`: local gate used by CI.

## Verification

- Local alpha smoke:
  - Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed, includes 216 Python tests, compileall, package build, and zip content check.
- Production evidence validator:
  - Command: `python3 scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial --quiet`
  - Result: passed, exit 0.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: passed, exit 0.
- Remote branch:
  - Command: `git ls-remote --heads origin main`
  - Result: `e018b1b2f7c88b52b36433308eba03d50e60464e refs/heads/main`.
- GitHub Actions:
  - Run `28246102354` for `7728587`: failed before steps due billing/spending-limit annotation.
  - Run `28246195358` for `e018b1b`: failed before steps for the same account-level reason.

## Open Questions / Risks

- To get remote CI green, either fix GitHub billing/spending limit for private Actions or intentionally change repo visibility to public. Do not change visibility without explicit approval.
- Unity Licensing Client/headless bootstrap still blocks recording configured Animator smoke for H-003/H-004/H-005/H-009/H-010.
- Unreal real import validation remains blocked by missing local `UnrealEditor`.
- Blender 4.2 target compatibility still needs direct evidence.
- Formal performance benchmark across 10k / 50k / 100k vertex meshes is still missing.

## Next Steps

1. Decide whether to keep the repo private and fix GitHub Actions billing, or make the repo public intentionally.
2. After Actions can run, rerun/confirm CI on `main`.
3. Add local performance benchmark tooling for representative mesh sizes.
4. Resolve Unity licensing interactively, then run configured Animator smoke migration for H-003/H-004/H-005/H-009/H-010.
5. Continue product hardening: Blender 4.2 matrix, Unreal import evidence, and benchmark report.

## Resume Prompt

Continue from this checkpoint. First inspect `git status -sb`, `docs/release-checklist.md`, `.github/workflows/ci.yml`, and recent `gh run list --repo kingkyylian/mac-game-rigger`. The local CI gate passes, but remote GitHub Actions fails before steps due private-repo billing/spending-limit restrictions. Do not change repo visibility without explicit user approval.
