# Project Checkpoint - 2026-06-26 18:10 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, tracking `origin/main`.
- GitHub: `https://github.com/kingkyylian/mac-game-rigger`, private.
- Context: continuing product hardening after CI setup; this slice focused on measurable performance risk.

## Done This Session

- Added `scripts/run_performance_benchmark.py`.
  - Benchmarks deterministic capsule weight-binding math without Blender.
  - Supports repeated `--vertex-count`.
  - Supports `--output` JSON report.
  - Supports `--max-seconds-per-case` runtime budget failure.
- Added `tests/test_performance_benchmark.py`.
  - Covers JSON report shape.
  - Covers runtime-budget failure behavior.
- Wired a quick 1k vertex performance smoke into `scripts/run_full_alpha_smoke.sh`.
- Added the benchmark script to package content checks and py_compile checks.
- Added `docs/performance-benchmarks.md` with the first 10k / 50k / 100k capsule weight-binding baseline.
- Updated `docs/benchmark-scorecard.md` and `docs/release-checklist.md`.
- Pushed commit:
  - `d4969e6 perf: add capsule weight benchmark gate`

## Current State

- Local working tree: clean.
- Remote `main`: `d4969e66b378a5dd9fa9ee43967a9fbc2b5d57eb`.
- Local alpha smoke gate: pass.
- Full Python test suite: pass.
- Performance baseline:
  - 10k vertices: 0.488191s, 20,483.79 vertices/sec.
  - 50k vertices: 2.249841s, 22,223.79 vertices/sec.
  - 100k vertices: 4.604221s, 21,719.20 vertices/sec.
- Remote GitHub Actions still fails before steps start because private-repo Actions billing/spending limit needs attention.

## Important Files / Artifacts

- `scripts/run_performance_benchmark.py`: deterministic capsule weight-binding benchmark CLI.
- `tests/test_performance_benchmark.py`: benchmark CLI coverage.
- `docs/performance-benchmarks.md`: recorded 10k/50k/100k baseline.
- `scripts/run_full_alpha_smoke.sh`: now includes quick performance smoke.
- `docs/release-checklist.md`: release table now includes performance smoke gate.

## Verification

- RED test:
  - Command: `python3 -m pytest tests/test_performance_benchmark.py -q`
  - Result before implementation: failed because `scripts/run_performance_benchmark.py` did not exist.
- Focused benchmark tests:
  - Command: `python3 -m pytest tests/test_performance_benchmark.py -q`
  - Result: passed, 2 passed.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: passed, 218 passed.
- Local alpha smoke:
  - Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; includes 218 tests, quick performance benchmark, compileall, package build, and zip content check.
- Formal capsule benchmark:
  - Command: `python3 scripts/run_performance_benchmark.py --vertex-count 10000 --vertex-count 50000 --vertex-count 100000 --output build/performance-benchmark.json`
  - Result: passed, wrote JSON report.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: passed.
- Remote CI:
  - Run `28246825102` for `d4969e6`: failed before any step started.
  - Annotation: recent account payments failed or spending limit must be increased.

## Open Questions / Risks

- Current benchmark covers capsule weight-binding math only. It does not measure Blender automatic weights, preview rendering, FBX export, UI overhead, or multi-mesh scenes.
- GitHub Actions remote CI remains blocked by account billing/spending limit.
- Unity Licensing Client/headless bootstrap still blocks configured Animator smoke regeneration for H-003/H-004/H-005/H-009/H-010.
- Unreal real import validation remains blocked by missing local `UnrealEditor`.
- Blender 4.2 target compatibility still needs direct evidence.

## Next Steps

1. Add an end-to-end Blender performance benchmark for 10k / 50k / 100k synthetic meshes.
2. Fix GitHub Actions billing/spending limit or intentionally change repo visibility after explicit approval.
3. Resolve Unity licensing interactively and run configured Animator smoke migration.
4. Capture Blender 4.2 compatibility evidence.
5. Capture real Unreal import evidence when `UnrealEditor` is available.

## Resume Prompt

Continue from this checkpoint. First inspect `git status -sb`, `docs/performance-benchmarks.md`, `scripts/run_performance_benchmark.py`, and `scripts/run_full_alpha_smoke.sh`. Local tests and alpha smoke pass; remote GitHub Actions still fails before steps due billing/spending-limit restrictions.
