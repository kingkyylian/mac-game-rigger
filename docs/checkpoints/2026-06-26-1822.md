# Project Checkpoint - 2026-06-26 18:22 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, tracking `origin/main`.
- GitHub: `https://github.com/kingkyylian/mac-game-rigger`, private.
- Context: continuing product hardening toward a product-level Mac-first game rigging tool.

## Done This Session

- Added `scripts/run_blender_workflow_benchmark.py`.
  - Runs the existing `tools/blender_asset_workflow.py` through Blender headless.
  - Supports repeated `--asset`.
  - Supports `--template`, `--camera-axis`, prop hinge passthrough options, `--output`, `--timeout-seconds`, and `--max-seconds-per-case`.
  - Writes a structured JSON report with duration, exit code, timeout state, budget state, stdout/stderr tails, summary presence, and key workflow summary signals.
- Added `tests/test_blender_workflow_benchmark.py`.
  - Uses a fake Blender executable so unit tests do not require real Blender.
  - Covers successful report generation.
  - Covers nonzero Blender exit failure behavior.
- Integrated the new script into `scripts/run_full_alpha_smoke.sh`.
  - `py_compile` gate now includes the workflow benchmark script.
  - package content check now requires `scripts/run_blender_workflow_benchmark.py`.
- Updated product docs.
  - `docs/performance-benchmarks.md` now includes the first full Blender workflow baseline.
  - `docs/benchmark-scorecard.md` documents the workflow benchmark command and next scaling target.
  - `docs/release-checklist.md` records the new Blender workflow benchmark gate.
- Committed implementation:
  - `e505c36 perf: add blender workflow benchmark`

## Current State

- Local H-006 full Blender workflow benchmark passed outside the sandbox.
- Latest benchmark report: `build/blender-workflow-benchmark.json`.
- H-006 baseline:
  - Asset: `local_assets/H-006/H-006-quaternius-animated-woman.fbx`
  - Template: `humanoid`
  - Blender: 4.5.10 LTS from `blender` on PATH
  - Runtime: 2.727215 seconds
  - Mesh count: 1
  - Vertex count: 954
  - Bone count: 17
  - QA: 0 unweighted vertices, 0 over-limit vertices, 0 warnings, 0 errors
  - Pose deformation: pass
  - Artifacts: QA JSON, four preview PNGs, Unity FBX, export QA JSON
- Blender inside the Codex sandbox crashed with exit code 139 before summary output; the same command passed outside the sandbox.

## Verification

- RED test:
  - Command: `python3 -m pytest tests/test_blender_workflow_benchmark.py -q`
  - Result before implementation: failed because `scripts/run_blender_workflow_benchmark.py` did not exist.
- Focused tests:
  - Command: `python3 -m pytest tests/test_blender_workflow_benchmark.py -q`
  - Result: passed, 2 passed.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: passed, 220 passed.
- Local alpha smoke:
  - Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; includes manifest validation, 220 tests, quick performance benchmark, compileall, package build, and package content check.
- Real Blender workflow benchmark:
  - Command: `scripts/run_blender_workflow_benchmark.py --blender blender --asset local_assets/H-006/H-006-quaternius-animated-woman.fbx --template humanoid --evidence-root build/blender-workflow-benchmark --output build/blender-workflow-benchmark.json --timeout-seconds 300 --max-seconds-per-case 120`
  - Result: passed outside the sandbox, wrote JSON report, duration 2.727215s.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: passed.

## Open Questions / Risks

- Current full workflow benchmark covers one 954-vertex real humanoid asset only.
- Product performance still needs 10k / 50k / 100k end-to-end Blender workflow cases.
- Blender 4.2 target compatibility remains unproven; local direct evidence is still Blender 4.5.10 LTS.
- Remote GitHub Actions remains blocked by private-repo billing/spending-limit state.
- Unity configured Animator smoke migration remains incomplete for H-003/H-004/H-005/H-009/H-010.
- Unreal real import validation remains blocked by missing local `UnrealEditor`.

## Next Steps

1. Add synthetic 10k / 50k / 100k Blender workflow asset generation and benchmark cases.
2. Capture Blender 4.2 compatibility evidence.
3. Resolve Unity Licensing Client/headless bootstrap and regenerate configured Animator evidence.
4. Capture real Unreal import evidence when `UnrealEditor` is available.
5. Fix GitHub Actions billing/spending-limit blocker or intentionally change repo visibility after explicit approval.

## Resume Prompt

Continue from this checkpoint. First inspect `git status -sb`, `scripts/run_blender_workflow_benchmark.py`, `tests/test_blender_workflow_benchmark.py`, `docs/performance-benchmarks.md`, and `build/blender-workflow-benchmark.json`. The next product hardening step is synthetic 10k / 50k / 100k end-to-end Blender workflow benchmarking.
