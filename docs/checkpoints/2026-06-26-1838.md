# Project Checkpoint - 2026-06-26 18:38 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, tracking `origin/main`.
- GitHub: `https://github.com/kingkyylian/mac-game-rigger`, private.
- Context: continuing product hardening toward a product-level Mac-first game rigging tool.

## Done This Session

- Extended `scripts/run_blender_workflow_benchmark.py` with synthetic humanoid scaling cases.
  - New flag: `--synthetic-humanoid-vertices`.
  - Generates deterministic OBJ humanoid stress meshes under the benchmark evidence root.
  - Preserves explicit `--asset` benchmark behavior.
  - Supports mixed explicit asset cases and synthetic cases in the same report.
  - Adds `syntheticSpec` to each generated benchmark case.
- Updated `tests/test_blender_workflow_benchmark.py`.
  - The fake Blender now reads OBJ vertex lines and mirrors the count into fake QA.
  - New test covers generated 10k / 50k / 100k synthetic cases.
  - TDD RED failure was confirmed before implementation: the runner required `--asset` and did not support synthetic cases.
- Updated benchmark documentation.
  - `docs/performance-benchmarks.md` now records synthetic 10k / 50k / 100k end-to-end Blender workflow results.
  - `docs/benchmark-scorecard.md` now includes the synthetic benchmark command and scaling baseline.
  - `docs/release-checklist.md` now includes a `Synthetic workflow scaling` gate.
- Committed implementation:
  - `5407d5f perf: add synthetic blender workflow scaling`

## Current State

- Local synthetic full Blender workflow benchmark passed outside the sandbox.
- Latest synthetic benchmark report: `build/blender-workflow-synthetic-benchmark.json`.
- Synthetic baseline:
  - 10,000 target/imported vertices: 2.321225s, 17 bones, 0 unweighted, 0 over-limit, pose warn, Unity FBX export pass.
  - 50,000 target/imported vertices: 5.956758s, 17 bones, 0 unweighted, 0 over-limit, pose warn, Unity FBX export pass.
  - 100,000 target/imported vertices: 10.666243s, 17 bones, 0 unweighted, 0 over-limit, pose warn, Unity FBX export pass.
- Pose deformation warning is expected for the synthetic geometry and is documented as a workflow scalability signal, not visual quality evidence.
- Blender workflow still needs sandbox-external execution; Blender 4.5.10 previously crashed inside the Codex sandbox with exit code 139.

## Verification

- RED test:
  - Command: `python3 -m pytest tests/test_blender_workflow_benchmark.py -q`
  - Result before implementation: failed because `--synthetic-humanoid-vertices` was unsupported and `--asset` was required.
- Focused tests:
  - Command: `python3 -m pytest tests/test_blender_workflow_benchmark.py -q`
  - Result: passed, 3 passed.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: passed, 221 passed.
- Local alpha smoke:
  - Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; includes manifest validation, 221 tests, quick performance benchmark, compileall, package build, and package content check.
- Real synthetic Blender workflow benchmark:
  - Command: `scripts/run_blender_workflow_benchmark.py --blender blender --synthetic-humanoid-vertices 10000 --synthetic-humanoid-vertices 50000 --synthetic-humanoid-vertices 100000 --evidence-root build/blender-workflow-synthetic-benchmark --output build/blender-workflow-synthetic-benchmark.json --timeout-seconds 600 --max-seconds-per-case 180`
  - Result: passed outside the sandbox, wrote JSON report.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: passed.

## Open Questions / Risks

- Synthetic benchmarks prove workflow scalability, not artist-approved deformation quality.
- Multi-mesh humanoids with hair/accessory parts still need timing.
- Quadruped, tail creature, and prop hinge full-workflow timing still need coverage.
- Blender 4.2 target compatibility remains unproven; local direct evidence is still Blender 4.5.10 LTS.
- Remote GitHub Actions remains blocked by private-repo billing/spending-limit state.
- Unity configured Animator smoke migration remains incomplete for H-003/H-004/H-005/H-009/H-010.
- Unreal real import validation remains blocked by missing local `UnrealEditor`.

## Next Steps

1. Add multi-mesh humanoid timing cases with separated hair/accessory meshes.
2. Add quadruped, tail creature, and prop hinge timing cases.
3. Capture Blender 4.2 compatibility evidence.
4. Resolve Unity Licensing Client/headless bootstrap and regenerate configured Animator evidence.
5. Capture real Unreal import evidence when `UnrealEditor` is available.
6. Fix GitHub Actions billing/spending-limit blocker or intentionally change repo visibility after explicit approval.

## Resume Prompt

Continue from this checkpoint. First inspect `git status -sb`, `scripts/run_blender_workflow_benchmark.py`, `docs/performance-benchmarks.md`, and `build/blender-workflow-synthetic-benchmark.json`. The next product hardening step is adding full-workflow timing coverage for multi-mesh humanoids, quadrupeds, tail creatures, and prop hinge assets.
