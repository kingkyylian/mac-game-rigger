# Project Checkpoint - 2026-06-26 18:49 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, tracking `origin/main`.
- GitHub: `https://github.com/kingkyylian/mac-game-rigger`, private.
- Context: continuing product hardening toward a product-level Mac-first game rigging tool.

## Done This Session

- Extended `scripts/run_blender_workflow_benchmark.py` with synthetic template-family cases.
  - New flags:
    - `--synthetic-multimesh-humanoid-vertices`
    - `--synthetic-quadruped-vertices`
    - `--synthetic-tail-creature-vertices`
    - `--synthetic-prop-hinge-vertices`
  - Multi-mesh humanoid generation now writes real glTF with six mesh nodes.
  - Quadruped, tail creature, and prop hinge generation write deterministic OBJ assets.
  - Existing explicit `--asset` and `--synthetic-humanoid-vertices` behavior remains intact.
- Updated `tests/test_blender_workflow_benchmark.py`.
  - Fake Blender now reads OBJ and glTF generated vertex counts.
  - New test covers all four synthetic template-family flags.
  - RED failure was confirmed before implementation: the new flags were unrecognized.
- Captured a real Blender template-family benchmark outside the sandbox.
- Updated product docs.
  - `docs/performance-benchmarks.md` now records multi-mesh humanoid, quadruped, tail creature, and prop hinge timing.
  - `docs/benchmark-scorecard.md` now documents the template-family benchmark command.
  - `docs/release-checklist.md` now includes `Template-family workflow scaling`.
- Committed implementation:
  - `451c77b perf: add synthetic template family benchmarks`

## Current State

- Latest template-family benchmark report: `build/blender-workflow-template-family-benchmark.json`.
- Template-family baseline:
  - Multi-mesh humanoid glTF: 6 meshes, 10,000 vertices, 17 bones, 3.329060s, 0 unweighted, 0 over-limit, pose fail, Unity FBX export pass.
  - Quadruped OBJ: 1 mesh, 10,000 vertices, 23 bones, 2.809406s, 0 unweighted, 0 over-limit, pose pass, Unity FBX export pass.
  - Tail creature OBJ: 1 mesh, 10,000 vertices, 25 bones, 2.875646s, 0 unweighted, 0 over-limit, pose pass, Unity FBX export pass.
  - Prop hinge OBJ: 1 mesh, 10,000 vertices, 3 bones, 1.785991s, 0 unweighted, 0 over-limit, pose pass, Unity FBX export pass.
- The multi-mesh humanoid pose deformation failure is now documented as a quality gap, not a performance blocker.
- Blender workflow benchmarks still need sandbox-external execution; Blender 4.5.10 previously crashed inside the Codex sandbox with exit code 139.

## Verification

- RED test:
  - Command: `python3 -m pytest tests/test_blender_workflow_benchmark.py -q`
  - Result before implementation: failed because `--synthetic-multimesh-humanoid-vertices`, `--synthetic-quadruped-vertices`, `--synthetic-tail-creature-vertices`, and `--synthetic-prop-hinge-vertices` were unrecognized.
- Focused tests:
  - Command: `python3 -m pytest tests/test_blender_workflow_benchmark.py -q`
  - Result: passed, 4 passed.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: passed, 222 passed.
- Local alpha smoke:
  - Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; includes manifest validation, 222 tests, quick performance benchmark, compileall, package build, and package content check.
- Real template-family Blender workflow benchmark:
  - Command: `scripts/run_blender_workflow_benchmark.py --blender blender --synthetic-multimesh-humanoid-vertices 10000 --synthetic-quadruped-vertices 10000 --synthetic-tail-creature-vertices 10000 --synthetic-prop-hinge-vertices 10000 --evidence-root build/blender-workflow-template-family-benchmark --output build/blender-workflow-template-family-benchmark.json --timeout-seconds 600 --max-seconds-per-case 180`
  - Result: passed outside the sandbox, wrote JSON report.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: passed.

## Open Questions / Risks

- Multi-mesh humanoid synthetic workflow passes structurally but pose deformation fails; this is the next quality target.
- Real multi-mesh humanoid, quadruped, tail creature, and prop assets still need measured timing.
- Blender 4.2 target compatibility remains unproven; local direct evidence is still Blender 4.5.10 LTS.
- Remote GitHub Actions remains blocked by private-repo billing/spending-limit state.
- Unity configured Animator smoke migration remains incomplete for H-003/H-004/H-005/H-009/H-010.
- Unreal real import validation remains blocked by missing local `UnrealEditor`.

## Next Steps

1. Improve multi-mesh humanoid pose deformation behavior or add diagnostics that localize the synthetic failure.
2. Capture real multi-mesh humanoid, quadruped, tail creature, and prop timing.
3. Capture Blender 4.2 compatibility evidence.
4. Resolve Unity Licensing Client/headless bootstrap and regenerate configured Animator evidence.
5. Capture real Unreal import evidence when `UnrealEditor` is available.
6. Fix GitHub Actions billing/spending-limit blocker or intentionally change repo visibility after explicit approval.

## Resume Prompt

Continue from this checkpoint. First inspect `git status -sb`, `scripts/run_blender_workflow_benchmark.py`, `docs/performance-benchmarks.md`, and `build/blender-workflow-template-family-benchmark.json`. The next product hardening step is improving or diagnosing the multi-mesh humanoid pose deformation failure while preserving the new template-family benchmark coverage.
