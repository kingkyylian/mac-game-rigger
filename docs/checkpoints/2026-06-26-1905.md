# Project Checkpoint - 2026-06-26 19:05 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, tracking `origin/main`; working tree has the multi-mesh orientation fix and docs ready to commit.
- GitHub: `https://github.com/kingkyylian/mac-game-rigger`, private.
- Context: continuing product hardening toward a product-level Mac-first game rigging tool.

## Done This Session

- Investigated the synthetic multi-mesh humanoid pose deformation failure from the template-family benchmark.
- Fixed `tools/blender_asset_workflow.py` so multi-mesh orientation normalization uses one scene-level plan from the combined mesh bounding box instead of rotating each mesh from its own local dimensions.
- Added `scene_dimensions_from_bbox` and a regression test proving separated mesh parts receive the same scene-level rotation plan.
- Fixed synthetic multi-mesh glTF generation in `scripts/run_blender_workflow_benchmark.py`.
  - Added `gltf_source_vertex_from_blender_vertex` to write glTF Y-up source coordinates from Blender-space generated vertices.
  - Added `synthetic_multimesh_humanoid_parts` so the multi-mesh stress asset has enough depth to make pose-deformation checks meaningful.
- Updated benchmark docs after rerunning real Blender evidence.
  - `docs/performance-benchmarks.md`
  - `docs/benchmark-scorecard.md`
  - `docs/release-checklist.md`

## Current State

- The previous multi-mesh humanoid pose failure is resolved for the deterministic synthetic glTF benchmark.
- Latest H-006 real Blender workflow benchmark:
  - 954 vertices, 17 bones, 3.142040s, structural QA pass, pose deformation pass, Unity FBX export pass.
- Latest synthetic humanoid scaling benchmark:
  - 10k vertices: 3.195480s, pose deformation pass, Unity FBX export pass.
  - 50k vertices: 5.995934s, pose deformation pass, Unity FBX export pass.
  - 100k vertices: 10.817145s, pose deformation pass, Unity FBX export pass.
- Latest template-family benchmark:
  - Multi-mesh humanoid glTF: 6 meshes, 10k vertices, 17 bones, 2.609815s, pose deformation pass, Unity FBX export pass.
  - Quadruped OBJ: 1 mesh, 10k vertices, 23 bones, 2.669343s, pose deformation pass, Unity FBX export pass.
  - Tail creature OBJ: 1 mesh, 10k vertices, 25 bones, 2.642737s, pose deformation pass, Unity FBX export pass.
  - Prop hinge OBJ: 1 mesh, 10k vertices, 3 bones, 1.958864s, pose deformation pass, Unity FBX export pass.
- Blender workflow benchmarks still need sandbox-external execution; Blender 4.5.10 previously crashed inside the Codex sandbox with exit code 139.

## Important Files / Artifacts

- `tools/blender_asset_workflow.py`: scene-level multi-mesh orientation normalization fix.
- `scripts/run_blender_workflow_benchmark.py`: glTF coordinate conversion and deeper multi-mesh humanoid synthetic asset generation.
- `tests/test_blender_asset_workflow.py`: regression coverage for scene-level orientation on separated meshes.
- `tests/test_blender_workflow_benchmark.py`: regression coverage for glTF Y-up conversion and multi-mesh depth.
- `build/blender-workflow-benchmark.json`: latest real H-006 workflow evidence.
- `build/blender-workflow-synthetic-benchmark.json`: latest 10k/50k/100k synthetic humanoid scaling evidence.
- `build/blender-workflow-template-family-benchmark.json`: latest synthetic template-family evidence.

## Verification

- Focused tests:
  - Command: `python3 -m pytest tests/test_blender_asset_workflow.py tests/test_blender_workflow_benchmark.py -q`
  - Result: passed, 66 passed.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: passed, 225 passed.
- Local alpha smoke:
  - Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; includes manifest validation, 225 tests, quick performance benchmark, compileall, package build, and package content check.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: passed.
- Real Blender workflow benchmarks already captured outside the sandbox in this checkpoint state:
  - H-006 real workflow: passed, 3.142040s.
  - Synthetic humanoid scaling 10k/50k/100k: passed, all pose deformation pass.
  - Template-family synthetic workflow: passed, all four cases pose deformation pass and Unity FBX export pass.

## Open Questions / Risks

- Real multi-mesh humanoid, quadruped, tail creature, and prop assets still need measured timing and visual review; synthetic pass is not enough for product quality.
- Blender 4.2 target compatibility remains unproven; local direct evidence is still Blender 4.5.10 LTS.
- Remote GitHub Actions remains blocked by private-repo billing/spending-limit state.
- Unity configured Animator smoke migration remains incomplete for H-003/H-004/H-005/H-009/H-010.
- Unreal real import validation remains blocked by missing local `UnrealEditor`.
- Advanced deformation quality remains out of scope for the current deterministic capsule weighting layer: fingers, face, twist bones, corrective shapes, cloth/coat behavior, and accessory-specific weighting need dedicated product work.

## Next Steps

1. Commit and push the multi-mesh orientation fix plus benchmark docs.
2. Add real multi-mesh asset evidence for hair/armor/coat/accessory cases, not only deterministic synthetic assets.
3. Capture Blender 4.2 compatibility evidence.
4. Finish Unity configured Animator smoke migration for the remaining humanoid slots.
5. Add or document Unreal import evidence once `UnrealEditor` is available.
6. Resolve GitHub Actions private-repo billing/spending-limit blocker or change repo visibility only after explicit approval.

## Resume Prompt

Continue from this checkpoint. First inspect `git status -sb`, `tools/blender_asset_workflow.py`, `scripts/run_blender_workflow_benchmark.py`, `docs/performance-benchmarks.md`, and the three benchmark JSON files under `build/`. The immediate next action is committing/pushing the multi-mesh orientation fix, then moving to real multi-mesh asset evidence.
