# Project Checkpoint - 2026-06-26 19:25 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, tracking `origin/main`; working tree has real asset family benchmark support ready to commit.
- GitHub: `https://github.com/kingkyylian/mac-game-rigger`, private.
- Context: continuing product hardening toward a product-level Mac-first game rigging tool.

## Done This Session

- Added manifest-slot support to `scripts/run_blender_workflow_benchmark.py`.
  - New `--manifest` option points to `samples/manifest.json`.
  - New repeatable `--slot` option resolves each slot's `realAsset.externalPath`.
  - Benchmark cases infer the workflow template from canonical manifest category / rig target.
  - Reports now include `slotId` for manifest-derived cases.
- Fixed template inference for `C-001`.
  - `C-001` is category `tail creature` with rig target text containing `Quadruped + tail helper baseline`.
  - Category now wins, so the benchmark uses `tail_creature`, not `quadruped`.
- Added regression coverage in `tests/test_blender_workflow_benchmark.py`.
  - Verified `--manifest/--slot` is accepted.
  - Verified H-003 -> `humanoid`, Q-001 -> `quadruped`, C-001 -> `tail_creature`.
- Ran a real 10-slot Blender workflow benchmark outside the sandbox.
- Updated docs:
  - `docs/performance-benchmarks.md`
  - `docs/benchmark-scorecard.md`
  - `docs/release-checklist.md`

## Current State

- Latest real asset family benchmark report: `build/blender-workflow-real-asset-family-benchmark.json`.
- Benchmark status: `pass`, 10/10 cases.
- Real benchmark cases:
  - H-003 knight: humanoid, 1,484 vertices, 17 bones, 1.774947s, QA pass, pose pass, Unity FBX export pass.
  - H-004 wizard: humanoid, 3,366 vertices, 17 bones, 1.901814s, QA pass, pose pass, Unity FBX export pass.
  - H-005 pirate: humanoid, 1,383 vertices, 17 bones, 1.760538s, QA pass, pose pass, Unity FBX export pass.
  - H-009 soldier: humanoid, 1,515 vertices, 17 bones, 1.771729s, QA pass, pose pass, Unity FBX export pass.
  - H-010 elf: humanoid, 1,314 vertices, 17 bones, 1.731110s, QA pass, pose pass, Unity FBX export pass.
  - Q-001 husky: quadruped, 962 vertices, 23 bones, 1.820218s, QA pass, pose pass, Unity FBX export pass.
  - Q-002 fox: quadruped, 926 vertices, 23 bones, 1.855413s, QA pass, pose pass, Unity FBX export pass.
  - C-001 apatosaurus: tail_creature, 723 vertices, 25 bones, 1.600824s, QA pass, pose pass, Unity FBX export pass.
  - P-001 wooden door: prop_hinge, 360 vertices, 3 bones, 1.477657s, QA pass, pose pass, Unity FBX export pass.
  - P-002 treasure chest: prop_hinge, 118 vertices, 3 bones, 1.446091s, QA pass, pose pass, Unity FBX export pass.
- This closes the previous generic gap for real quadruped, tail creature, and prop workflow timing.
- Remaining real workflow timing gap is narrower: separate-mesh hair/accessory humanoids and Blender 4.2 target timing.

## Important Files / Artifacts

- `scripts/run_blender_workflow_benchmark.py`: manifest-slot benchmark support and template inference.
- `tests/test_blender_workflow_benchmark.py`: regression coverage for manifest slot cases and tail creature category precedence.
- `docs/performance-benchmarks.md`: new real asset family workflow section.
- `docs/benchmark-scorecard.md`: new manifest-slot benchmark command and baseline summary.
- `docs/release-checklist.md`: new real asset family benchmark release gate row.
- `build/blender-workflow-real-asset-family-benchmark.json`: local Blender 4.5.10 real asset family benchmark evidence; build artifact, not committed.

## Verification

- RED test:
  - Command: `python3 -m pytest tests/test_blender_workflow_benchmark.py -q`
  - Result before implementation: failed because `--manifest` and `--slot` were unrecognized.
- RED test for category precedence:
  - Command: `python3 -m pytest tests/test_blender_workflow_benchmark.py -q`
  - Result before inference fix: failed because C-001 resolved to `quadruped` instead of `tail_creature`.
- Focused tests:
  - Command: `python3 -m pytest tests/test_blender_workflow_benchmark.py -q`
  - Result: passed, 7 passed.
- Focused workflow tests:
  - Command: `python3 -m pytest tests/test_blender_workflow_benchmark.py tests/test_blender_asset_workflow.py -q`
  - Result: passed, 67 passed.
- Real Blender benchmark:
  - Command: `scripts/run_blender_workflow_benchmark.py --blender blender --manifest samples/manifest.json --slot H-003 --slot H-004 --slot H-005 --slot H-009 --slot H-010 --slot Q-001 --slot Q-002 --slot C-001 --slot P-001 --slot P-002 --evidence-root build/blender-workflow-real-asset-family-benchmark --output build/blender-workflow-real-asset-family-benchmark.json --timeout-seconds 300 --max-seconds-per-case 120`
  - Result: passed outside the sandbox, 10/10 cases pass.
- Full tests:
  - Command: `python3 -m pytest tests -q`
  - Result: passed, 227 passed.
- Local alpha smoke:
  - Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; includes manifest validation, 227 tests, quick performance benchmark, compileall, package build, and package content check.
- Diff hygiene:
  - Command: `git diff --check`
  - Result: passed.

## Open Questions / Risks

- Unity configured Animator migration is ready to run, but Unity licensing/bootstrap must be refreshed first.
- Real separate-mesh hair/accessory humanoids still need measured workflow timing.
- Blender 4.2 target compatibility remains unproven; local direct evidence is still Blender 4.5.10 LTS.
- Remote GitHub Actions remains blocked by private-repo billing/spending-limit state.
- Unreal real import validation remains blocked by missing local `UnrealEditor`.
- Advanced deformation quality remains out of scope for the current deterministic capsule weighting layer: fingers, face, twist bones, corrective shapes, cloth/coat behavior, and accessory-specific weighting need dedicated product work.

## Next Steps

1. Commit and push the manifest-slot real asset benchmark support plus docs.
2. Open Unity Hub or Unity Editor once on the Mac to refresh licensing, then rerun `scripts/run_unity_animator_smoke_migration.py --manifest samples/manifest.json --evidence-root .`.
3. Capture Blender 4.2 compatibility evidence.
4. Add or source a true separate-mesh hair/accessory humanoid and run it through the manifest-slot benchmark.
5. Capture real Unreal import evidence when `UnrealEditor` is available.
6. Resolve GitHub Actions private-repo billing/spending-limit blocker or change repo visibility only after explicit approval.

## Resume Prompt

Continue from this checkpoint. First inspect `git status -sb`, `scripts/run_blender_workflow_benchmark.py`, `build/blender-workflow-real-asset-family-benchmark.json`, and `docs/performance-benchmarks.md`. The immediate next action is committing/pushing the manifest-slot real asset benchmark support, then moving to Unity licensing or Blender 4.2 compatibility.
