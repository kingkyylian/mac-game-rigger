# Project Checkpoint - 2026-06-26 19:43 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, tracking `origin/main`; working tree has the real separate-mesh humanoid gate ready to commit.
- GitHub: `https://github.com/kingkyylian/mac-game-rigger`, private.
- Context: continuing product hardening toward a product-level Mac-first game rigging tool.

## Done This Session

- Added a machine-readable strict quality gate for real separate-mesh humanoid evidence.
  - `scripts/generate_asset_evidence_report.py` now reads `meshCount` from `evidence/<slot>/workflow-summary.json`.
  - It falls back to `qa.mesh_count` if top-level `meshCount` is absent.
  - Bad workflow-summary JSON is surfaced as `invalid` instead of being silently treated as proof.
- Added `realSeparateMeshHumanoidEvidence` to the Strict Quality Gates table in `docs/asset-evidence-progress.md`.
- Added regression coverage in `tests/test_generate_asset_evidence_report.py`.
  - One test proves the gate blocks a complete score >= 3 real humanoid when `meshCount` is 1.
  - One test proves the gate passes when at least one complete score >= 3 real humanoid has `meshCount` greater than 1.
- Updated `docs/release-checklist.md` so the production-trial pass does not hide the product gap.
  - Added a blocked `Strict separate-mesh humanoid gate` row.
  - Added the report generator command to verification commands.
  - Updated known issues to state that all complete score >= 3 real humanoids currently report `meshCount` 1.

## Current State

- Production trial gate still passes.
- Strict configured Animator gate is blocked for H-003, H-004, H-005, H-009, and H-010.
- Strict real separate-mesh humanoid gate is blocked for H-003, H-004, H-005, H-006, H-009, and H-010.
- Current complete score >= 3 real humanoid workflow summaries are all single mesh, so hair/accessory/clothing split-mesh behavior is not proven on real assets.
- This is intentionally a product-readiness gate, not an alpha production-trial gate breaker.

## Important Files / Artifacts

- `scripts/generate_asset_evidence_report.py`: loads workflow `meshCount` and computes `realSeparateMeshHumanoidEvidence`.
- `tests/test_generate_asset_evidence_report.py`: focused regression coverage for the new gate.
- `docs/asset-evidence-progress.md`: generated report now shows both strict blocked quality gates.
- `docs/release-checklist.md`: release checklist now records the separate-mesh humanoid blocker.
- `dist/MacGameRigger-0.1.0.zip`: rebuilt by the alpha smoke script.

## Verification

- Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py -q`
  - Result: passed, `17 passed`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `231 passed`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; manifest validation, Python tests, quick performance benchmark, compileall, package build, and package content check completed.
- Command: `git diff --check`
  - Result: passed.

## Open Questions / Risks

- Need a true real separate-mesh humanoid asset with hair, clothing, accessories, or other split mesh parts, then run it through the manifest-slot workflow benchmark.
- Unity configured Animator migration remains blocked by Unity Licensing Client/bootstrap timeout until Unity Hub or Unity Editor is opened once on the Mac.
- Unreal real import validation remains blocked by missing local `UnrealEditor`.
- Blender 4.2 target compatibility remains unproven locally; only Blender 4.5.10 LTS is discovered.
- GitHub Actions remains blocked by private-repo billing/spending-limit state.
- Advanced deformation quality remains out of scope for the deterministic capsule weighting layer: fingers, face, twist bones, corrective shapes, cloth/coat behavior, and accessory-specific weighting need dedicated product work.

## Next Steps

1. Commit and push the separate-mesh humanoid gate plus release checklist update.
2. Source or create a real separate-mesh humanoid asset and run `scripts/run_blender_workflow_benchmark.py --manifest samples/manifest.json --slot <slot> ...`.
3. Refresh Unity licensing through Unity Hub or Editor, then rerun `scripts/run_unity_animator_smoke_migration.py --manifest samples/manifest.json --evidence-root .`.
4. Install or provide Blender 4.2, then run the target-version compatibility matrix.
5. Install or provide Unreal Editor, then capture real Unreal import evidence.

## Resume Prompt

Continue from this checkpoint. First inspect `git status -sb`, `docs/asset-evidence-progress.md`, `docs/release-checklist.md`, and `scripts/generate_asset_evidence_report.py`. The immediate next action is committing/pushing the separate-mesh humanoid gate, then sourcing a true split-mesh humanoid asset for real workflow evidence.
