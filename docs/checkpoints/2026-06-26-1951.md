# Project Checkpoint - 2026-06-26 19:51 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, tracking `origin/main`; working tree clean.
- GitHub: `https://github.com/kingkyylian/mac-game-rigger`, private.
- Context: continuing product hardening toward a product-level Mac-first game rigging tool.

## Done This Session

- Added a strict product-readiness gate for real separate-mesh humanoid evidence.
  - `scripts/generate_asset_evidence_report.py` reads `meshCount` from `evidence/<slot>/workflow-summary.json`.
  - It falls back to `qa.mesh_count`.
  - It reports `realSeparateMeshHumanoidEvidence` as blocked unless at least one complete score >= 3 real humanoid has `meshCount > 1`.
- Added regression tests for the new gate in `tests/test_generate_asset_evidence_report.py`.
  - Single-mesh score >= 3 real humanoid blocks.
  - Multi-mesh score >= 3 real humanoid passes.
- Regenerated `docs/asset-evidence-progress.md`.
  - Strict `configuredAnimatorSmokeForHumanoidScore3` remains blocked.
  - Strict `realSeparateMeshHumanoidEvidence` is now visible and blocked.
  - Slot Status now has a `Meshes` column so each slot exposes mesh count directly.
- Updated `docs/release-checklist.md`.
  - Production-trial pass now explicitly points to stricter product gates.
  - Added blocked `Strict separate-mesh humanoid gate`.
  - Added report generation to verification commands.
  - Known issues now state that current complete score >= 3 humanoids are all single mesh.
- Audited local real humanoid assets from committed evidence:
  - H-001: source import `meshCount` 1, workflow `meshCount` 1.
  - H-003: source import `meshCount` 1, workflow `meshCount` 1.
  - H-004: source import `meshCount` 1, workflow `meshCount` 1.
  - H-005: source import `meshCount` 1, workflow `meshCount` 1.
  - H-006: source import `meshCount` 1, workflow `meshCount` 1.
  - H-009: source import `meshCount` 1, workflow `meshCount` 1.
  - H-010: source import `meshCount` 1, workflow `meshCount` 1.
- Committed and pushed:
  - `ea97656 test: add real separate mesh humanoid gate`
  - `016e5d3 docs: checkpoint separate mesh humanoid gate`
  - `e515e21 docs: show mesh counts in evidence progress`

## Current State

- Production trial gate: pass.
- Complete real evidence entries: 12.
- Score >= 3 ratio: 0.917.
- Strict configured Animator gate: blocked for H-003, H-004, H-005, H-009, H-010.
- Strict real separate-mesh humanoid gate: blocked for H-003, H-004, H-005, H-006, H-009, H-010.
- Local real humanoid asset pool does not contain a true split-mesh humanoid candidate; all committed humanoid import-smoke evidence reports one mesh.
- Worktree is clean after push.

## Important Files / Artifacts

- `scripts/generate_asset_evidence_report.py`: strict separate-mesh gate and mesh count table rendering.
- `tests/test_generate_asset_evidence_report.py`: regression tests for mesh-count report behavior.
- `docs/asset-evidence-progress.md`: current generated evidence table with `Meshes` column.
- `docs/release-checklist.md`: product-readiness checklist now separates production-trial pass from stricter game-ready gates.
- `docs/checkpoints/2026-06-26-1951.md`: this checkpoint.

## Verification

- Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py -q`
  - Result: passed, `17 passed`.
- Command: `scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --output docs/asset-evidence-progress.md`
  - Result: passed.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `231 passed`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; manifest validation, Python tests, quick performance benchmark, compileall, package build, and package content check completed.
- Command: `git diff --check`
  - Result: passed.
- Command: `gh api 'repos/kingkyylian/mac-game-rigger/check-runs/83707099542/annotations' ...`
  - Result: remote CI still fails before job steps because GitHub reports account payment/spending-limit action required.

## Open Questions / Risks

- Need a true real separate-mesh humanoid asset with hair, clothing, accessories, or other split mesh parts, then run it through the manifest-slot workflow benchmark.
- Unity configured Animator migration remains blocked by Unity Licensing Client/bootstrap timeout until Unity Hub or Unity Editor is opened once on the Mac.
- Unreal real import validation remains blocked by missing local `UnrealEditor`.
- Blender 4.2 target compatibility remains unproven locally; only Blender 4.5.10 LTS is discovered.
- GitHub Actions remains blocked by private-repo billing/spending-limit state.
- Advanced deformation quality remains out of scope for the deterministic capsule weighting layer: fingers, face, twist bones, corrective shapes, cloth/coat behavior, and accessory-specific weighting need dedicated product work.

## Next Steps

1. Source or create a real separate-mesh humanoid asset and add it to an open humanoid manifest slot.
2. Run `scripts/run_blender_workflow_benchmark.py --manifest samples/manifest.json --slot <slot> ...` against that split-mesh asset and regenerate `docs/asset-evidence-progress.md`.
3. Refresh Unity licensing through Unity Hub or Editor, then rerun `scripts/run_unity_animator_smoke_migration.py --manifest samples/manifest.json --evidence-root .`.
4. Install or provide Blender 4.2, then run the target-version compatibility matrix.
5. Install or provide Unreal Editor, then capture real Unreal import evidence.

## Resume Prompt

Continue from this checkpoint. First inspect `git status -sb`, `docs/asset-evidence-progress.md`, `docs/release-checklist.md`, and `scripts/generate_asset_evidence_report.py`. The immediate next product-level task is sourcing or creating a true split-mesh humanoid asset, running it through a manifest slot benchmark, and using the new `realSeparateMeshHumanoidEvidence` gate as the acceptance check.
