# Project Checkpoint - 2026-06-26 20:01 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, tracking `origin/main`; working tree clean.
- GitHub: `https://github.com/kingkyylian/mac-game-rigger`, private.
- Context: continuing product hardening toward a product-level Mac-first game rigging tool.

## Done This Session

- Strengthened the real separate-mesh humanoid gate so it cannot close from workflow mesh count alone.
  - `scripts/generate_asset_evidence_report.py` now reads source import mesh count from `evidence/<slot>/asset-import-smoke.json`.
  - `realSeparateMeshHumanoidEvidence` now requires both source import mesh count > 1 and rig workflow mesh count > 1 for at least one complete score >= 3 real humanoid.
  - Bad or missing source import smoke evidence no longer counts as split-mesh proof.
- Updated the generated evidence table in `docs/asset-evidence-progress.md`.
  - Replaced the single `Meshes` column with `Source Meshes` and `Rig Meshes`.
  - Current evidence shows all complete score >= 3 real humanoids are still 1 source mesh and 1 rig mesh.
- Updated `docs/release-checklist.md`.
  - The blocked strict separate-mesh gate now explicitly references both source import and rig workflow mesh counts.
- Added regression coverage in `tests/test_generate_asset_evidence_report.py`.
  - Single source mesh + multi-mesh workflow still blocks.
  - Multi-mesh source + multi-mesh workflow passes.
- Committed and pushed:
  - `057c6bd test: require source mesh proof for split humanoids`

## Current State

- Production trial gate: pass.
- Complete real evidence entries: 12.
- Score >= 3 ratio: 0.917.
- Strict configured Animator gate: blocked for H-003, H-004, H-005, H-009, H-010.
- Strict real separate-mesh humanoid gate: blocked for H-003, H-004, H-005, H-006, H-009, H-010.
- Current real humanoid evidence is not split-mesh:
  - H-003, H-004, H-005, H-006, H-009, H-010 each show source meshes 1 and rig meshes 1.
- Worktree is clean after push.

## Important Files / Artifacts

- `scripts/generate_asset_evidence_report.py`: source mesh proof loader and stricter gate logic.
- `tests/test_generate_asset_evidence_report.py`: focused gate regression tests.
- `docs/asset-evidence-progress.md`: current source-vs-rig mesh evidence table.
- `docs/release-checklist.md`: product-readiness checklist updated for source+rig split-mesh proof.
- `docs/checkpoints/2026-06-26-2001.md`: this checkpoint.

## Verification

- Command: `python3 -m pytest tests/test_generate_asset_evidence_report.py -q`
  - Result: passed, `18 passed`.
- Command: `scripts/generate_asset_evidence_report.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --output docs/asset-evidence-progress.md`
  - Result: passed.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `232 passed`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; manifest validation, Python tests, quick performance benchmark, compileall, package build, and package content check completed.
- Command: `git diff --check`
  - Result: passed.

## Open Questions / Risks

- Need a true real separate-mesh humanoid asset with hair, clothing, accessories, or other split mesh parts, then run it through both asset import smoke and manifest-slot workflow benchmark.
- Unity configured Animator migration remains blocked by Unity Licensing Client/bootstrap timeout until Unity Hub or Unity Editor is opened once on the Mac.
- Unreal real import validation remains blocked by missing local `UnrealEditor`.
- Blender 4.2 target compatibility remains unproven locally; only Blender 4.5.10 LTS is discovered.
- GitHub Actions remains blocked by private-repo billing/spending-limit state.
- Advanced deformation quality remains out of scope for the deterministic capsule weighting layer: fingers, face, twist bones, corrective shapes, cloth/coat behavior, and accessory-specific weighting need dedicated product work.

## Next Steps

1. Source or create a real separate-mesh humanoid asset and add it to an open humanoid manifest slot.
2. Run `tools/blender_asset_import_smoke.py` for that slot so source mesh count is recorded.
3. Run `scripts/run_blender_workflow_benchmark.py --manifest samples/manifest.json --slot <slot> ...` and copy/register the workflow evidence.
4. Regenerate `docs/asset-evidence-progress.md`; `realSeparateMeshHumanoidEvidence` should pass only if source and rig mesh counts are both > 1.
5. Refresh Unity licensing through Unity Hub or Editor, then rerun `scripts/run_unity_animator_smoke_migration.py --manifest samples/manifest.json --evidence-root .`.

## Resume Prompt

Continue from this checkpoint. First inspect `git status -sb`, `docs/asset-evidence-progress.md`, `docs/release-checklist.md`, and `scripts/generate_asset_evidence_report.py`. The immediate next product-level task is sourcing or creating a true split-mesh humanoid asset, running source import smoke plus workflow benchmark, and using the stricter `realSeparateMeshHumanoidEvidence` gate as the acceptance check.
