# Project Checkpoint - 2026-06-26 20:10 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, tracking `origin/main`; working tree clean.
- GitHub: `https://github.com/kingkyylian/mac-game-rigger`, private.
- Context: continuing product hardening toward a product-level Mac-first game rigging tool.

## Done This Session

- Added `scripts/plan_split_mesh_humanoid_intake.py`.
  - Reports open humanoid manifest slots for a split-mesh real asset.
  - Current open humanoid slots: H-002, H-007, H-008.
  - Recommends H-002 by default.
  - With `--asset`, emits the exact commands for:
    - source import smoke,
    - Blender rig workflow,
    - manifest/evidence registration,
    - evidence report regeneration.
- Added `tests/test_plan_split_mesh_humanoid_intake.py`.
  - Verifies only empty humanoid slots are selected.
  - Verifies generated command shapes for source smoke, workflow, and registration.
  - Verifies current manifest JSON output recommends H-002.
- Updated `docs/release-checklist.md`.
  - Added `Split-mesh humanoid intake plan` as a passing checklist item.
  - Added planner command to verification commands.
  - Known issues now separate the tooling readiness from the still-missing real asset.
- Committed and pushed:
  - `c6293e5 feat: plan split mesh humanoid intake`

## Current State

- Production trial gate: pass.
- Strict configured Animator gate: blocked for H-003, H-004, H-005, H-009, H-010.
- Strict real separate-mesh humanoid gate: blocked for H-003, H-004, H-005, H-006, H-009, H-010.
- Split-mesh intake is now scriptable, but no actual real split-mesh humanoid asset is present locally.
- Worktree is clean after push.

## Important Files / Artifacts

- `scripts/plan_split_mesh_humanoid_intake.py`: split-mesh humanoid intake planner.
- `tests/test_plan_split_mesh_humanoid_intake.py`: planner regression tests.
- `docs/release-checklist.md`: documents planner gate and command.
- `docs/asset-evidence-progress.md`: still shows strict split-mesh humanoid gate blocked.

## Verification

- Command: `python3 -m pytest tests/test_plan_split_mesh_humanoid_intake.py tests/test_generate_asset_evidence_report.py -q`
  - Result: passed, `21 passed`.
- Command: `scripts/plan_split_mesh_humanoid_intake.py --manifest samples/manifest.json --json`
  - Result: passed; output status `needs_asset`, open slots H-002/H-007/H-008, recommended H-002.
- Command: `scripts/plan_split_mesh_humanoid_intake.py --manifest samples/manifest.json --asset local_assets/H-002/example-split-character.glb --source-name 'Example Split Character' --source-url https://example.invalid/split-character --license CC0 --json`
  - Result: passed; emitted source smoke, workflow, registration, and report commands.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `235 passed`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; manifest validation, Python tests, quick performance benchmark, compileall, package build, and package content check completed.
- Command: `git diff --check`
  - Result: passed.

## Open Questions / Risks

- Need a true licensed real separate-mesh humanoid asset with hair, clothing, accessories, or other split mesh parts.
- Unity configured Animator migration remains blocked by Unity Licensing Client/bootstrap timeout until Unity Hub or Unity Editor is opened once on the Mac.
- Unreal real import validation remains blocked by missing local `UnrealEditor`.
- Blender 4.2 target compatibility remains unproven locally; only Blender 4.5.10 LTS is discovered.
- GitHub Actions remains blocked by private-repo billing/spending-limit state.
- Advanced deformation quality remains out of scope for the deterministic capsule weighting layer: fingers, face, twist bones, corrective shapes, cloth/coat behavior, and accessory-specific weighting need dedicated product work.

## Next Steps

1. Place a licensed split-mesh humanoid asset under `local_assets/H-002/` or provide its local path.
2. Run `scripts/plan_split_mesh_humanoid_intake.py --manifest samples/manifest.json --asset <asset> --source-name <name> --source-url <url> --license <license> --json`.
3. Execute the emitted source import smoke and workflow commands.
4. Create `evidence/H-002/notes.md`, then run the emitted registration command with a real deformation score.
5. Regenerate `docs/asset-evidence-progress.md`; the strict gate should pass only if source and rig mesh counts are both > 1 and H-002 reaches score >= 3.

## Resume Prompt

Continue from this checkpoint. First inspect `git status -sb`, `scripts/plan_split_mesh_humanoid_intake.py`, `docs/asset-evidence-progress.md`, and `docs/release-checklist.md`. The immediate next product-level task is obtaining or creating a true licensed split-mesh humanoid asset and running the planner-emitted intake commands for H-002.
