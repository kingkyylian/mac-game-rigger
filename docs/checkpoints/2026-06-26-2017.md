# Project Checkpoint - 2026-06-26 20:17 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, tracking `origin/main`; working tree clean.
- GitHub: `https://github.com/kingkyylian/mac-game-rigger`, private.
- Context: continuing product hardening toward a product-level Mac-first game rigging tool.

## Done This Session

- Added `scripts/verify_split_mesh_humanoid_evidence.py`.
  - Verifies split-mesh humanoid evidence slot-by-slot or across the manifest.
  - Pass condition: real humanoid, score >= 3, source import mesh count > 1, and rig workflow mesh count > 1.
  - Current manifest correctly returns blocked because all real humanoid evidence is single mesh.
- Added `tests/test_verify_split_mesh_humanoid_evidence.py`.
  - Passes when source and rig mesh counts are both > 1.
  - Blocks when source import is single mesh even if workflow mesh count is > 1.
  - Confirms the current repo remains blocked until real split-mesh evidence exists.
- Updated `docs/release-checklist.md`.
  - Added `Split-mesh humanoid verifier` checklist row.
  - Added verifier command to verification commands.
- Committed and pushed:
  - `4637fae test: verify split mesh humanoid evidence`

## Current State

- Production trial gate: pass.
- Strict configured Animator gate: blocked for H-003, H-004, H-005, H-009, H-010.
- Strict real separate-mesh humanoid gate: blocked for H-003, H-004, H-005, H-006, H-009, H-010.
- Split-mesh intake is scriptable and slot-level verification is now scriptable.
- No actual real split-mesh humanoid asset is present locally.
- Worktree is clean after push.

## Important Files / Artifacts

- `scripts/plan_split_mesh_humanoid_intake.py`: emits intake commands for H-002/H-007/H-008.
- `scripts/verify_split_mesh_humanoid_evidence.py`: verifies source+rig split mesh evidence.
- `tests/test_verify_split_mesh_humanoid_evidence.py`: verifier regression tests.
- `docs/release-checklist.md`: documents verifier gate and command.
- `docs/asset-evidence-progress.md`: still shows strict split-mesh humanoid gate blocked.

## Verification

- Command: `python3 -m pytest tests/test_verify_split_mesh_humanoid_evidence.py tests/test_plan_split_mesh_humanoid_intake.py tests/test_generate_asset_evidence_report.py -q`
  - Result: passed, `24 passed`.
- Command: `scripts/verify_split_mesh_humanoid_evidence.py --manifest samples/manifest.json --evidence-root . --json`
  - Result: expected blocked, no passing slots; H-003/H-004/H-005/H-006/H-009/H-010 all report source mesh count 1 and rig mesh count 1.
- Command: `scripts/verify_split_mesh_humanoid_evidence.py --manifest samples/manifest.json --evidence-root . --slot H-003`
  - Result: expected blocked; source and rig mesh counts must be > 1.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `238 passed`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; manifest validation, Python tests, quick performance benchmark, compileall, package build, and package content check completed.
- Command: `git diff --check`
  - Result: passed.

## Open Questions / Risks

- Need a true licensed real separate-mesh humanoid asset with hair, clothing, accessories, or other split mesh parts.
- Unity configured Animator migration remains blocked by Unity Licensing Client/bootstrap timeout until Unity Hub or Unity Editor is opened once on the Mac.
- Unreal real import validation remains blocked by missing local `UnrealEditor`.
- Blender 4.2 target compatibility remains unproven locally; only Blender 4.5.10 LTS is discovered.
- GitHub Actions remains blocked by private-repo billing/spending-limit state.
- Advanced deformation quality remains out of scope for the deterministic capsule weighting layer: fingers, face, twist bones, corrective shapes, cloth/coat behavior, and accessory-specific weighting need dedicated product work.

## Next Steps

1. Place a licensed split-mesh humanoid asset under `local_assets/H-002/` or provide its local path.
2. Run `scripts/plan_split_mesh_humanoid_intake.py --manifest samples/manifest.json --asset <asset> --source-name <name> --source-url <url> --license <license> --json`.
3. Execute the emitted source import smoke and workflow commands.
4. Create `evidence/H-002/notes.md`, then run the emitted registration command with a real deformation score.
5. Run `scripts/verify_split_mesh_humanoid_evidence.py --manifest samples/manifest.json --evidence-root . --slot H-002 --json` and regenerate `docs/asset-evidence-progress.md`.

## Resume Prompt

Continue from this checkpoint. First inspect `git status -sb`, `scripts/plan_split_mesh_humanoid_intake.py`, `scripts/verify_split_mesh_humanoid_evidence.py`, and `docs/asset-evidence-progress.md`. The immediate next product-level task is obtaining or creating a true licensed split-mesh humanoid asset and running the planner plus verifier for H-002.
