# Project Checkpoint - 2026-06-26 20:23 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, tracking `origin/main`; working tree clean.
- GitHub: `https://github.com/kingkyylian/mac-game-rigger`, private.
- Context: continuing product hardening toward a product-level Mac-first game rigging tool.

## Done This Session

- Added `samples/split_mesh_humanoid_candidates.json`.
  - Primary candidate: KayKit Adventurers.
  - License metadata: CC0.
  - Format notes: FBX, GLTF, OBJ, BLEND.
  - Verification requirement remains strict: local source import mesh count > 1 and rig workflow mesh count > 1.
- Extended `scripts/plan_split_mesh_humanoid_intake.py`.
  - New `--candidate` option.
  - Default candidate registry path: `samples/split_mesh_humanoid_candidates.json`.
  - `--candidate kaykit-adventurers` prefills source name, source URL, license, and verification notes.
- Added candidate registry tests in `tests/test_plan_split_mesh_humanoid_intake.py`.
- Updated `docs/release-checklist.md`.
  - Added `Split-mesh source candidate registry` checklist row.
  - Added candidate-prefilled planner command to verification commands.
- Committed and pushed:
  - `c2adb12 feat: add split mesh humanoid source candidates`

## Current State

- Production trial gate: pass.
- Strict configured Animator gate: blocked for H-003, H-004, H-005, H-009, H-010.
- Strict real separate-mesh humanoid gate: blocked for H-003, H-004, H-005, H-006, H-009, H-010.
- Split-mesh intake and verification are scriptable.
- KayKit Adventurers is now the primary recorded CC0 source candidate for H-002.
- No actual split-mesh humanoid asset file has been downloaded or placed locally yet.
- Worktree is clean after push.

## Important Files / Artifacts

- `samples/split_mesh_humanoid_candidates.json`: candidate source registry.
- `scripts/plan_split_mesh_humanoid_intake.py`: now supports `--candidate kaykit-adventurers`.
- `scripts/verify_split_mesh_humanoid_evidence.py`: verifies source+rig split mesh evidence.
- `docs/release-checklist.md`: records source candidate registry and commands.
- `docs/asset-evidence-progress.md`: strict split-mesh humanoid gate remains blocked.

## Verification

- Command: `python3 -m pytest tests/test_plan_split_mesh_humanoid_intake.py tests/test_verify_split_mesh_humanoid_evidence.py -q`
  - Result: passed, `8 passed`.
- Command: `scripts/plan_split_mesh_humanoid_intake.py --manifest samples/manifest.json --candidate kaykit-adventurers --asset local_assets/H-002/kaykit-adventurer.glb --json`
  - Result: passed; emitted H-002 intake commands with KayKit Adventurers source metadata.
- Command: `scripts/plan_split_mesh_humanoid_intake.py --manifest samples/manifest.json --candidate kaykit-adventurers --json`
  - Result: passed; output status `needs_asset`, open slots H-002/H-007/H-008, candidate metadata included.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `240 passed`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; manifest validation, Python tests, quick performance benchmark, compileall, package build, and package content check completed.
- Command: `git diff --check`
  - Result: passed.

## Open Questions / Risks

- Need the actual KayKit Adventurers or another licensed split-mesh humanoid asset file locally.
- Candidate metadata does not prove mesh separation; only source import smoke can do that.
- Unity configured Animator migration remains blocked by Unity Licensing Client/bootstrap timeout until Unity Hub or Unity Editor is opened once on the Mac.
- Unreal real import validation remains blocked by missing local `UnrealEditor`.
- Blender 4.2 target compatibility remains unproven locally; only Blender 4.5.10 LTS is discovered.
- GitHub Actions remains blocked by private-repo billing/spending-limit state.
- Advanced deformation quality remains out of scope for the deterministic capsule weighting layer: fingers, face, twist bones, corrective shapes, cloth/coat behavior, and accessory-specific weighting need dedicated product work.

## Next Steps

1. Download or place KayKit Adventurers asset under `local_assets/H-002/`.
2. Run `scripts/plan_split_mesh_humanoid_intake.py --manifest samples/manifest.json --candidate kaykit-adventurers --asset <asset> --json`.
3. Execute the emitted source import smoke and workflow commands.
4. Create `evidence/H-002/notes.md`, then run the emitted registration command with a real deformation score.
5. Run `scripts/verify_split_mesh_humanoid_evidence.py --manifest samples/manifest.json --evidence-root . --slot H-002 --json` and regenerate `docs/asset-evidence-progress.md`.

## Resume Prompt

Continue from this checkpoint. First inspect `git status -sb`, `samples/split_mesh_humanoid_candidates.json`, `scripts/plan_split_mesh_humanoid_intake.py`, and `scripts/verify_split_mesh_humanoid_evidence.py`. The immediate next product-level task is downloading/placing the KayKit Adventurers or another licensed split-mesh humanoid asset, then running the planner and verifier for H-002.
