# Project Checkpoint - 2026-06-27 03:07 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, tracking `origin/main`; working tree clean after implementation push.
- GitHub: `https://github.com/kingkyylian/mac-game-rigger`, private.
- Context: continuing product hardening toward a product-level Mac-first game rigging tool.

## Done This Session

- Added `scripts/preflight_split_mesh_candidate.py`.
  - Validates a candidate source import smoke JSON before a split-mesh humanoid is accepted for workflow intake.
  - Requires source metadata: `sourceName`, `sourceUrl`, and `license`.
  - Requires source smoke `status=pass`.
  - Requires `metrics.meshCount > 1`.
  - Requires `metrics.suggestedCategory == humanoid`.
  - Exits `0` for pass, `1` for blocked candidate evidence, and `2` for an unknown candidate id.
- Extended `scripts/plan_split_mesh_humanoid_intake.py`.
  - Candidate-backed asset plans now emit a `candidatePreflight` command before workflow/registration commands.
- Added tests in `tests/test_preflight_split_mesh_candidate.py`.
  - Passing split-mesh humanoid source smoke.
  - Blocking single-mesh source smoke.
  - Blocking missing source smoke through CLI JSON.
  - Reporting an unknown candidate without Python traceback.
- Updated `tests/test_plan_split_mesh_humanoid_intake.py`.
  - Verifies planner output includes the new preflight command for `kaykit-adventurers`.
- Updated `docs/release-checklist.md`.
  - Added `Split-mesh candidate preflight` row.
  - Added explicit expected blocking check note for H-003 single-mesh source smoke.
- Committed and pushed:
  - `42ba0af feat: preflight split mesh candidates`

## Current State

- Production trial gate: pass.
- Python unit test suite: pass.
- Blender-skipped alpha smoke gate: pass.
- Strict configured Animator gate: blocked for H-003, H-004, H-005, H-009, H-010.
- Strict real separate-mesh humanoid gate: blocked for H-003, H-004, H-005, H-006, H-009, H-010.
- Split-mesh source candidate intake is now more guarded:
  - Planner records candidate metadata.
  - Preflight blocks single-mesh source evidence before spending time on full rig workflow.
  - Verifier still blocks final gate until a real source+rig split-mesh humanoid is registered.

## Important Files / Artifacts

- `scripts/preflight_split_mesh_candidate.py`: new source-smoke preflight gate.
- `tests/test_preflight_split_mesh_candidate.py`: behavior coverage for pass/block/CLI error cases.
- `scripts/plan_split_mesh_humanoid_intake.py`: emits `candidatePreflight` in candidate-backed plans.
- `samples/split_mesh_humanoid_candidates.json`: includes KayKit Adventurers as the primary CC0 H-002 candidate.
- `docs/release-checklist.md`: records the preflight gate and expected negative check.
- `docs/asset-evidence-progress.md`: strict split-mesh humanoid gate remains blocked until real evidence exists.

## Verification

- Command: `python3 -m pytest tests/test_preflight_split_mesh_candidate.py tests/test_plan_split_mesh_humanoid_intake.py tests/test_verify_split_mesh_humanoid_evidence.py -q`
  - Result: passed, `12 passed`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `244 passed`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; manifest validation, Python tests, quick performance benchmark, compileall, package build, and package content check completed.
- Command: `git diff --check`
  - Result: passed.
- Command: `scripts/preflight_split_mesh_candidate.py --candidate kaykit-adventurers --source-smoke evidence/H-003/asset-import-smoke.json --json`
  - Result: expected blocked, exit `1`; H-003 reports source mesh count `1`, so the preflight rejects it.

## Open Questions / Risks

- Need the actual KayKit Adventurers or another licensed split-mesh humanoid asset file locally.
- Candidate registry metadata still does not prove mesh separation; the new preflight only proves it once a real source smoke JSON exists.
- Unity configured Animator migration remains blocked by Unity Licensing Client/bootstrap timeout until Unity Hub or Unity Editor is opened once on the Mac.
- Unreal real import validation remains blocked by missing local `UnrealEditor`.
- Blender 4.2 target compatibility remains unproven locally; only Blender 4.5.10 LTS is discovered.
- GitHub Actions remains blocked by private-repo billing/spending-limit state.
- Advanced deformation quality remains out of scope for the current deterministic capsule weighting layer: fingers, face, twist bones, corrective shapes, cloth/coat behavior, and accessory-specific weighting need dedicated product work.

## Next Steps

1. Download or place KayKit Adventurers under `local_assets/H-002/`.
2. Run `scripts/plan_split_mesh_humanoid_intake.py --manifest samples/manifest.json --candidate kaykit-adventurers --asset <asset> --json`.
3. Execute the emitted source import smoke, then `candidatePreflight`, then Blender workflow.
4. Register H-002 only if source and rig workflow both report mesh count > 1 and visual/deformation review is acceptable.
5. Rerun `scripts/verify_split_mesh_humanoid_evidence.py --manifest samples/manifest.json --evidence-root . --slot H-002 --json` and regenerate `docs/asset-evidence-progress.md`.

## Resume Prompt

Continue from this checkpoint. First inspect `git status -sb`, `scripts/preflight_split_mesh_candidate.py`, `scripts/plan_split_mesh_humanoid_intake.py`, `samples/split_mesh_humanoid_candidates.json`, and `docs/asset-evidence-progress.md`. The immediate next product-level task is sourcing the KayKit Adventurers or another licensed split-mesh humanoid asset, then running source import smoke, candidate preflight, workflow, registration, and verifier for H-002.
