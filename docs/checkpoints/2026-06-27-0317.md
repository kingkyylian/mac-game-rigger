# Project Checkpoint - 2026-06-27 03:17 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, tracking `origin/main`; working tree clean after implementation push.
- GitHub: `https://github.com/kingkyylian/mac-game-rigger`, private.
- Context: continuing product hardening toward a product-level Mac-first game rigging tool.

## Done This Session

- Added `scripts/run_split_mesh_humanoid_intake.py`.
  - Builds and optionally executes the guarded H-002 split-mesh intake sequence.
  - Sequence order is fixed as source import smoke -> candidate preflight -> Blender workflow.
  - Requires a candidate id, so the source metadata and preflight gate are not skipped.
  - Blocks non-empty manifest slots such as H-003 to avoid overwriting existing evidence.
  - In execute mode, stops at `needs_registration_review` after workflow instead of auto-registering evidence without manual deformation score and visual review.
- Added `tests/test_run_split_mesh_humanoid_intake.py`.
  - Covers phase ordering.
  - Covers non-empty slot blocking.
  - Covers execute-phase stop before registration.
  - Covers dry-run JSON output.
  - Covers traceback-free CLI error for non-empty slots.
- Updated `docs/release-checklist.md`.
  - Added `Split-mesh intake runner` checklist row.
  - Added the runner dry-run command to verification commands.
- Committed and pushed:
  - `9e834ae feat: add split mesh intake runner`

## Current State

- Production trial gate: pass.
- Python unit test suite: pass.
- Blender-skipped alpha smoke gate: pass.
- Strict configured Animator gate: blocked for H-003, H-004, H-005, H-009, H-010.
- Strict real separate-mesh humanoid gate: blocked for H-003, H-004, H-005, H-006, H-009, H-010.
- H-002 is still the recommended open humanoid slot for the first real split-mesh candidate.
- Intake is now safer once the actual KayKit or equivalent asset exists:
  - dry-run shows all generated commands;
  - execute mode runs source smoke, preflight, and workflow;
  - registration still requires human quality judgment.

## Important Files / Artifacts

- `scripts/run_split_mesh_humanoid_intake.py`: guarded intake runner.
- `tests/test_run_split_mesh_humanoid_intake.py`: runner behavior coverage.
- `scripts/preflight_split_mesh_candidate.py`: candidate/source-smoke preflight gate.
- `scripts/plan_split_mesh_humanoid_intake.py`: lower-level command planner used by the runner.
- `samples/split_mesh_humanoid_candidates.json`: includes KayKit Adventurers as the primary CC0 H-002 candidate.
- `docs/release-checklist.md`: records the runner command and remaining product gates.

## Verification

- Command: `python3 -m pytest tests/test_run_split_mesh_humanoid_intake.py -q`
  - Result: passed, `5 passed`.
- Command: `python3 -m pytest tests/test_run_split_mesh_humanoid_intake.py tests/test_plan_split_mesh_humanoid_intake.py tests/test_preflight_split_mesh_candidate.py tests/test_verify_split_mesh_humanoid_evidence.py -q`
  - Result: passed, `17 passed`.
- Command: `scripts/run_split_mesh_humanoid_intake.py --manifest samples/manifest.json --candidate kaykit-adventurers --asset local_assets/H-002/kaykit-adventurer.glb --dry-run --json`
  - Result: passed; emitted H-002 source smoke, candidate preflight, workflow, manual registration, report generation, and strict verifier commands.
- Command: `scripts/run_split_mesh_humanoid_intake.py --manifest samples/manifest.json --candidate kaykit-adventurers --slot H-003 --asset local_assets/H-003/kaykit.glb --dry-run --json`
  - Result: expected blocked, exit `2`; stderr `Slot H-003 is not empty`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `249 passed`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; manifest validation, Python tests, quick performance benchmark, compileall, package build, and package content check completed.
- Command: `git diff --check`
  - Result: passed.

## Open Questions / Risks

- Need the actual KayKit Adventurers or another licensed split-mesh humanoid asset file locally.
- The new runner does not prove mesh separation by itself; it orchestrates the checks once a real asset is present.
- Unity configured Animator migration remains blocked by Unity Licensing Client/bootstrap timeout until Unity Hub or Unity Editor is opened once on the Mac.
- Unreal real import validation remains blocked by missing local `UnrealEditor`.
- Blender 4.2 target compatibility remains unproven locally; only Blender 4.5.10 LTS is discovered.
- GitHub Actions remains blocked by private-repo billing/spending-limit state.
- Advanced deformation quality remains out of scope for the current deterministic capsule weighting layer: fingers, face, twist bones, corrective shapes, cloth/coat behavior, and accessory-specific weighting need dedicated product work.

## Next Steps

1. Download or place KayKit Adventurers under `local_assets/H-002/`.
2. Run `scripts/run_split_mesh_humanoid_intake.py --manifest samples/manifest.json --candidate kaykit-adventurers --asset <asset> --json`.
3. Review generated H-002 previews and notes, then run the emitted registration command only with a real deformation score and visual review.
4. Run the emitted strict verifier command and regenerate `docs/asset-evidence-progress.md`.
5. If H-002 passes, commit the manifest/evidence docs; if it blocks, use source smoke/workflow JSON to decide whether to choose another candidate.

## Resume Prompt

Continue from this checkpoint. First inspect `git status -sb`, `scripts/run_split_mesh_humanoid_intake.py`, `scripts/preflight_split_mesh_candidate.py`, `samples/split_mesh_humanoid_candidates.json`, and `docs/asset-evidence-progress.md`. The immediate next product-level task is sourcing a real split-mesh humanoid asset, then running the guarded intake runner for H-002 and only registering it after manual deformation/visual review.
