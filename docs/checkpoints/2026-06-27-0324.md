# Project Checkpoint - 2026-06-27 03:24 +0300

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, tracking `origin/main`; working tree clean after implementation push.
- GitHub: `https://github.com/kingkyylian/mac-game-rigger`, private.
- Context: continuing product hardening toward a product-level Mac-first game rigging tool.

## Done This Session

- Strengthened `scripts/run_split_mesh_humanoid_intake.py`.
  - Runner plans now include `workflowSummary`.
  - Execute mode now reads `workflow-summary.json` after the Blender workflow phase.
  - If rig workflow mesh count is missing or `<= 1`, the runner returns `blocked` at `workflowSplitMeshCheck`.
  - This prevents the runner from asking for manual registration review when the workflow collapsed a split source into single-mesh evidence.
- Updated `tests/test_run_split_mesh_humanoid_intake.py`.
  - Added a passing workflow summary case with `meshCount=2`.
  - Added a blocking workflow summary case with `meshCount=1`.
  - Added dry-run assertion for the emitted `workflowSummary` path.
- Updated `docs/release-checklist.md`.
  - `Split-mesh intake runner` now explicitly records the post-workflow rig mesh count guard.
- Committed and pushed:
  - `885b98f test: guard split mesh workflow intake`

## Current State

- Production trial gate: pass.
- Python unit test suite: pass.
- Blender-skipped alpha smoke gate: pass.
- Strict configured Animator gate: blocked for H-003, H-004, H-005, H-009, H-010.
- Strict real separate-mesh humanoid gate: blocked for H-003, H-004, H-005, H-006, H-009, H-010.
- H-002 remains the recommended open humanoid slot for the first real split-mesh candidate.
- The guarded runner now protects both halves of the split-mesh requirement:
  - source import preflight requires source mesh count > 1;
  - post-workflow check requires rig workflow mesh count > 1.

## Important Files / Artifacts

- `scripts/run_split_mesh_humanoid_intake.py`: guarded intake runner with post-workflow split-mesh validation.
- `tests/test_run_split_mesh_humanoid_intake.py`: runner behavior coverage, including collapsed-mesh block.
- `scripts/preflight_split_mesh_candidate.py`: source import smoke preflight gate.
- `scripts/verify_split_mesh_humanoid_evidence.py`: final strict split-mesh verifier after registration.
- `docs/release-checklist.md`: records the runner's workflow summary guard.
- `docs/asset-evidence-progress.md`: strict split-mesh humanoid gate still blocked until real H-002 evidence exists.

## Verification

- Command: `python3 -m pytest tests/test_run_split_mesh_humanoid_intake.py -q`
  - Result: passed, `6 passed`.
- Command: `python3 -m pytest tests/test_run_split_mesh_humanoid_intake.py tests/test_plan_split_mesh_humanoid_intake.py tests/test_preflight_split_mesh_candidate.py tests/test_verify_split_mesh_humanoid_evidence.py -q`
  - Result: passed, `18 passed`.
- Command: `python3 -m pytest tests -q`
  - Result: passed, `250 passed`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; manifest validation, Python tests, quick performance benchmark, compileall, package build, and package content check completed.
- Command: `scripts/run_split_mesh_humanoid_intake.py --manifest samples/manifest.json --candidate kaykit-adventurers --asset local_assets/H-002/kaykit-adventurer.glb --dry-run --json`
  - Result: passed; output includes `workflowSummary: evidence/H-002/workflow-summary.json`.
- Command: `git diff --check`
  - Result: passed.

## Open Questions / Risks

- Need the actual KayKit Adventurers or another licensed split-mesh humanoid asset file locally.
- The runner still cannot prove mesh separation without a real asset; it now enforces the correct checks once the asset exists.
- Unity configured Animator migration remains blocked by Unity Licensing Client/bootstrap timeout until Unity Hub or Unity Editor is opened once on the Mac.
- Unreal real import validation remains blocked by missing local `UnrealEditor`.
- Blender 4.2 target compatibility remains unproven locally; only Blender 4.5.10 LTS is discovered.
- GitHub Actions remains blocked by private-repo billing/spending-limit state.
- Advanced deformation quality remains out of scope for the current deterministic capsule weighting layer: fingers, face, twist bones, corrective shapes, cloth/coat behavior, and accessory-specific weighting need dedicated product work.

## Next Steps

1. Download or place KayKit Adventurers under `local_assets/H-002/`.
2. Run `scripts/run_split_mesh_humanoid_intake.py --manifest samples/manifest.json --candidate kaykit-adventurers --asset <asset> --json`.
3. If source preflight or workflow split-mesh check blocks, inspect `evidence/H-002/asset-import-smoke.json` and `evidence/H-002/workflow-summary.json` before choosing another candidate.
4. If runner reaches `needs_registration_review`, inspect previews and notes, then register H-002 only with a real deformation score and visual review.
5. Run the emitted strict verifier command and regenerate `docs/asset-evidence-progress.md`.

## Resume Prompt

Continue from this checkpoint. First inspect `git status -sb`, `scripts/run_split_mesh_humanoid_intake.py`, `scripts/preflight_split_mesh_candidate.py`, `scripts/verify_split_mesh_humanoid_evidence.py`, and `docs/asset-evidence-progress.md`. The immediate next product-level task is sourcing a real split-mesh humanoid asset and running the guarded H-002 intake runner end to end.
