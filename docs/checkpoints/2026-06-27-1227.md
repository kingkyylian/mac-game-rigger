# Project Checkpoint - 2026-06-27 12:27

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, clean after push
- Latest commit: `88f2869 feat: register H-002 split mesh humanoid evidence`
- Context: Product-readiness work continued by closing the first real split-mesh humanoid evidence gate on macOS.

## Done This Session

- Downloaded KayKit Adventurers Free 2.0 from itch.io through the browser-equivalent direct download flow.
- Kept the downloaded source ZIP and extracted source files under ignored `local_assets/H-002/`; no source binary ZIP was committed.
- Selected `Ranger.glb` as the H-002 real split-mesh humanoid candidate.
- Ran source import smoke:
  - Source mesh count: `9`
  - Armature count: `1`
  - Suggested category: `humanoid`
- Ran guarded H-002 split-mesh intake runner with external permission because sandboxed Python-spawned Blender crashed in Metal backend init.
- Generated H-002 evidence artifacts:
  - `evidence/H-002/asset-import-smoke.json`
  - `evidence/H-002/workflow-summary.json`
  - `evidence/H-002/qa-report.json`
  - `evidence/H-002/export-unity.fbx`
  - `evidence/H-002/export-unity.qa.json`
  - `evidence/H-002/preview-neutral.png`
  - `evidence/H-002/preview-neutral-side.png`
  - `evidence/H-002/preview-pose.png`
  - `evidence/H-002/preview-pose-side.png`
  - `evidence/H-002/notes.md`
- Registered H-002 in `samples/manifest.json` as `KayKit Adventurers - Ranger.glb`.
- Set H-002 deformation score to conservative `3`, visual review `pass`, Unity status `not tested`, Unreal status `blocked`.
- Updated `docs/asset-evidence-progress.md`.
- Updated split-mesh runner review packet command to use `--force`, so existing placeholder notes do not block evidence generation.
- Updated review packet generation to fall back to top-level workflow `status` when `qa.status` is absent.
- Updated tests that depended on H-002 being the first empty humanoid slot; H-007 is now the next recommended open humanoid slot.
- Pushed commit `88f2869` to `origin/main`.

## Current State

- Production trial gate: `pass`.
- Real assets registered: `13`.
- Complete evidence entries: `13`.
- Score >= 3 count: `12`.
- Score >= 3 ratio: `0.923`.
- Strict `realSeparateMeshHumanoidEvidence`: `pass`, with H-002 as passing slot.
- Strict `configuredAnimatorSmokeForHumanoidScore3`: still `blocked`.
- Worktree is clean.
- Remote GitHub CI for `88f2869` failed before running any steps because of account billing/spending-limit status, not code:
  - Run: `https://github.com/kingkyylian/mac-game-rigger/actions/runs/28285160288`
  - Job steps: `0`
  - Annotation: recent account payments failed or spending limit must be increased.

## Important Files / Artifacts

- `samples/manifest.json`: H-002 registered as the first real split-mesh humanoid evidence slot.
- `docs/asset-evidence-progress.md`: summary now shows `realSeparateMeshHumanoidEvidence` pass.
- `evidence/H-002/notes.md`: manual review details and conservative limitations for H-002.
- `evidence/H-002/workflow-summary.json`: workflow output with source/workflow mesh count `9`, pose deformation pass, and diagnostics.
- `evidence/H-002/qa-report.json`: QA warning still reports `42` unweighted vertices.
- `scripts/run_split_mesh_humanoid_intake.py`: review packet phase now includes `--force`.
- `scripts/create_split_mesh_review_packet.py`: QA status fallback now uses top-level workflow status.
- `tests/test_*split_mesh*` and `tests/test_plan_split_mesh_humanoid_intake.py`: updated for H-002 registered / H-007 next-open-slot state.

## Verification

- Command: `scripts/run_split_mesh_humanoid_intake.py --manifest samples/manifest.json --candidate kaykit-adventurers --asset local_assets/H-002/KayKit_Adventurers_2.0_FREE/Characters/gltf/Ranger.glb --json`
  - Result: passed with external permission; status `needs_registration_review`, source mesh count `9`, rig mesh count `9`.
- Command: `scripts/verify_split_mesh_humanoid_evidence.py --manifest samples/manifest.json --evidence-root . --slot H-002 --json`
  - Result: passed; H-002 source mesh count `9`, rig mesh count `9`, deformation score `3`.
- Command: `scripts/verify_split_mesh_humanoid_evidence.py --manifest samples/manifest.json --evidence-root . --json`
  - Result: passed; passing slots: `H-002`.
- Command: `python3 -m pytest -q`
  - Result: `255 passed`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial`
  - Result: passed; production trial status `pass`.
- Command: `git diff --check`
  - Result: passed.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; alpha smoke gate completed.

## Open Questions / Risks

- H-002 is a conservative pass, not final animator-quality output.
- H-002 QA/export still report `42` unweighted vertices.
- H-002 previews are silhouette-only; textured/material visual QA is not yet covered.
- Weight diagnostics show suspicious distal/core fallback assignments on some accessory and limb vertices.
- Unity configured Animator smoke is not recorded for H-002 and still blocks the stricter Animator gate across H-003/H-004/H-005/H-009/H-010.
- Local Blender 4.5.10 can segfault in Metal backend init when spawned from Python under the sandboxed runner; running the runner with external permission passed.
- Remote GitHub CI cannot provide signal until billing/spending-limit is fixed.

## Next Steps

1. Add or improve diagnostics/tests around H-002 unweighted vertices and suspicious fallback assignments; target reducing the `42` unweighted vertices before raising score above `3`.
2. Run Unity configured Animator smoke for H-002 and migrate/record configured Animator smoke for H-003/H-004/H-005/H-009/H-010 after local Unity licensing/bootstrap is fixed.
3. Decide whether to add a robust Blender-spawn strategy or retry/diagnostic handling for Python-spawned Blender Metal init crashes on macOS.

## Resume Prompt

Continue from this checkpoint. First read this file and the project `AGENTS.md`, then inspect `samples/manifest.json`, `docs/asset-evidence-progress.md`, `evidence/H-002/notes.md`, `evidence/H-002/workflow-summary.json`, and `scripts/run_split_mesh_humanoid_intake.py` before making changes. The immediate next product-level task is improving H-002 binding quality and Unity configured Animator evidence.
