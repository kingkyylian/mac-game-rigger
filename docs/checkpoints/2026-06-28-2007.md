# Project Checkpoint - 2026-06-28 20:07

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`; this checkpoint was written before committing this session's verified changes
- Context: Continued product hardening for the Mac-native game rigging pipeline, focused on H-002 split-mesh humanoid evidence quality.

## Done This Session

- Investigated H-002's previous `42` unweighted vertex warning.
- Found the true root cause:
  - KayKit Ranger GLB imports an `Icosphere` helper mesh in Blender's `glTF_not_exported` collection.
  - That helper is not part of the exportable character and should not be counted, rigged, QA'd, or exported as production evidence.
- Added import pruning in `tools/blender_asset_import_smoke.py` and `tools/blender_asset_workflow.py`.
- Added workflow helpers so headless evidence binding/cleanup uses explicit mesh collections instead of Blender selection context.
- Added selection hardening for exportable meshes that are hidden from selection but still valid production meshes.
- Updated asset category heuristic so a rigged, wide split-mesh humanoid like KayKit Ranger still classifies as `humanoid` after pruning non-exportable GLTF helper meshes.
- Regenerated H-002 evidence:
  - Source smoke now reports `8` exportable meshes, `7734` vertices, `Icosphere` pruned.
  - Workflow now reports `8` rig meshes, `7734` weighted vertices.
  - QA and Unity export QA now report `0` unweighted vertices and `0` over-limit vertices.
  - Unity FBX re-import check showed `8` meshes, `7734` vertices, `0` unweighted vertices.
- Regenerated `docs/asset-evidence-progress.md`.
- Updated H-002 manifest and review notes to explain the `glTF_not_exported` prune and remaining limits.
- Hardened flaky Unity timeout test by increasing the fake Unity timeout window from 1s to 2s so the log is reliably written before kill.

## Current State

- H-002 remains the only passing strict real split-mesh humanoid slot.
- H-002 is cleaner than before:
  - Before: source/rig counted 9 meshes including non-exportable `Icosphere`; QA/export warned about unweighted vertices.
  - Now: source/rig/export count 8 exportable meshes; no unweighted vertex warning.
- Production trial gate remains pass.
- Strict `configuredAnimatorSmokeForHumanoidScore3` remains blocked by H-003/H-004/H-005/H-009/H-010.
- H-002 Unity status is still `not tested`.

## Important Files / Artifacts

- `tools/blender_asset_import_smoke.py`: prunes meshes in `glTF_not_exported` before source metrics.
- `tools/blender_asset_workflow.py`: prunes non-exportable import helpers, binds/cleans explicit mesh collections, records `importPrune` and `capsuleBind`.
- `addon/mac_game_rigger/core/asset_analysis.py`: recognizes rigged wide split-mesh humanoids.
- `tests/test_blender_asset_import_smoke.py`: coverage for source-smoke pruning.
- `tests/test_blender_asset_workflow.py`: coverage for pruning, explicit mesh bind/cleanup, and export selection hardening.
- `tests/test_asset_analysis.py`: coverage for KayKit-like rigged split-mesh humanoid heuristic.
- `evidence/H-002/asset-import-smoke.json`: source evidence now `8` exportable meshes, `Icosphere` pruned.
- `evidence/H-002/workflow-summary.json`: workflow evidence now `8` rig meshes, `7734` weighted vertices.
- `evidence/H-002/qa-report.json`: `0` unweighted vertices.
- `evidence/H-002/export-unity.qa.json`: `0` unweighted vertices.
- `evidence/H-002/export-unity.fbx`: regenerated Unity FBX artifact.
- `docs/asset-evidence-progress.md`: H-002 row now `8` source / `8` rig meshes.

## Verification

- Command: `scripts/preflight_split_mesh_candidate.py --candidate kaykit-adventurers --source-smoke evidence/H-002/asset-import-smoke.json --json`
  - Result: passed; source mesh count `8`, suggested category `humanoid`.
- Command: `scripts/verify_split_mesh_humanoid_evidence.py --manifest samples/manifest.json --evidence-root . --slot H-002 --json`
  - Result: passed; H-002 source mesh count `8`, rig mesh count `8`, deformation score `3`.
- Command: Blender FBX re-import of `evidence/H-002/export-unity.fbx`
  - Result: passed; `8` meshes, `7734` vertices, `0` unweighted vertices.
- Command: `python3 -m pytest -q`
  - Result: passed; `261 passed`.
- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial`
  - Result: passed; production trial gate `pass`, score >= 3 ratio `0.923`.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; alpha smoke gate completed.
- Command: `git diff --check`
  - Result: passed.

## Open Questions / Risks

- H-002 still has no Unity configured Animator smoke.
- H-002 previews are still silhouette-based; textured/material visual QA is not covered.
- Unreal validation remains blocked by missing UnrealEditor.
- GitHub Actions has been failing externally due billing/spending-limit, not code execution.
- Blender can still segfault under sandboxed Python-spawned Metal init; direct/external Blender runs worked.

## Next Steps

1. Run and record Unity configured Animator smoke for H-002, then migrate the same evidence for H-003/H-004/H-005/H-009/H-010.
2. Add textured/material visual QA for H-002 so the evidence is not silhouette-only.
3. Harden Blender launch wrapper around sandbox/Metal startup crashes or document the required execution mode.

## Resume Prompt

Continue from this checkpoint. First read this file and the project `AGENTS.md`, then inspect `tools/blender_asset_workflow.py`, `tools/blender_asset_import_smoke.py`, `addon/mac_game_rigger/core/asset_analysis.py`, `evidence/H-002/workflow-summary.json`, `evidence/H-002/export-unity.qa.json`, and `docs/asset-evidence-progress.md` before making changes. The immediate next product-level task is Unity configured Animator evidence for H-002 and the other score-3 humanoids.
