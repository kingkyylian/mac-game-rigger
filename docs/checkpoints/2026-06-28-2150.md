# Project Checkpoint - 2026-06-28 21:50

## Project

- Root: `/Users/kyylian/mac-game-rigger`
- Git: `main`, latest commit before this checkpoint: `c431ff9 fix: prune non-exportable gltf helper meshes`
- Context: Mac-first game rigger productization; this pass closed the strict Unity configured Animator evidence gate for current score >= 3 humanoids.

## Done This Session

- Confirmed local Unity batchmode health after clearing a stale Unity Licensing Client process.
- Recorded real Unity import evidence for H-002:
  - `evidence/H-002/unity-import.json`
  - 8 renderers, 8 skinned mesh renderers, 1 Animator
  - `boneTransformSmoke.passed: true`
  - `animationClipSmoke.passed: true`
  - `configuredAnimatorSmoke.passed: true`
- Ran the configured Animator migration batch for the remaining humanoid score >= 3 Unity-pass gaps:
  - H-003
  - H-004
  - H-005
  - H-009
  - H-010
- Updated `samples/manifest.json` so H-002 Unity import status is `pass`.
- Regenerated `docs/asset-evidence-progress.md`.
- Updated release and Unity verification docs to reflect the new strict gate state.
- Updated planner tests because the current manifest now correctly reports no configured Animator migration gaps.

## Current State

- Production trial gate: pass.
- Strict `configuredAnimatorSmokeForHumanoidScore3`: pass.
- Strict `realSeparateMeshHumanoidEvidence`: pass, currently proven by H-002.
- Current evidence report:
  - 25 slots
  - 13 real assets registered
  - 13 complete evidence entries
  - 12 score >= 3 entries
  - score >= 3 ratio `0.923`
- H-002 is the strongest split-mesh humanoid evidence:
  - source mesh count 8
  - rig mesh count 8
  - 7734 weighted vertices
  - 0 unweighted vertices
  - 0 over-limit vertices
  - Unity configured Animator smoke pass

## Important Files / Artifacts

- `evidence/H-002/unity-import.json`: first H-002 Unity configured Animator evidence.
- `evidence/H-003/unity-import.json`: regenerated with configured Animator smoke.
- `evidence/H-004/unity-import.json`: regenerated with configured Animator smoke.
- `evidence/H-005/unity-import.json`: regenerated with configured Animator smoke.
- `evidence/H-009/unity-import.json`: regenerated with configured Animator smoke.
- `evidence/H-010/unity-import.json`: regenerated with configured Animator smoke.
- `docs/asset-evidence-progress.md`: strict Animator and split-mesh gates now pass.
- `docs/release-checklist.md`: release gate status updated for current evidence and latest checked CI run.
- `docs/unity-import-verification.md`: configured Animator migration docs updated from future blocker to current strict gate.
- `tests/test_plan_unity_animator_smoke_migration.py`: current-manifest expectations now assert no remaining gaps.

## Verification

- Command: `scripts/validate_asset_evidence.py --manifest samples/manifest.json --evidence-root . --check-evidence-files --require-production-trial --require-configured-animator-smoke --quiet`
  - Result: passed.
- Command: `scripts/plan_unity_animator_smoke_migration.py --manifest samples/manifest.json --evidence-root . --json`
  - Result: passed; output `{"gaps": []}`.
- Command: `scripts/verify_split_mesh_humanoid_evidence.py --manifest samples/manifest.json --evidence-root . --json`
  - Result: passed; `passingSlots: ["H-002"]`.
- Command: `python3 -m pytest tests/test_plan_unity_animator_smoke_migration.py -q`
  - Result: passed; 3 tests.
- Command: `python3 -m pytest -q`
  - Result: passed; 261 tests.
- Command: `scripts/run_full_alpha_smoke.sh --skip-blender`
  - Result: passed; 261 tests, performance smoke, compileall, package build.
- Command: `git diff --check`
  - Result: passed.
- Command: `gh api repos/kingkyylian/mac-game-rigger/actions/runs --jq '.workflow_runs[0:5][] | [.id,.head_sha,.status,.conclusion,.html_url] | @tsv'`
  - Result: latest checked remote run `28329808032` for `c431ff9` completed with `success`.

## Open Questions / Risks

- Unity evidence still uses Generic Animator/controller sampling, not Humanoid Avatar retarget validation.
- Unreal import validation remains blocked until Unreal Editor is installed/discoverable and a real import pass is captured.
- Textured/material visual QA is still missing; current previews are mostly silhouette and stress-pose based.
- H-002 proves one real split-mesh humanoid; broader production confidence needs more real split-mesh humanoids in H-007/H-008 or similar slots.
- Blender 4.2 target compatibility remains unproven locally because only Blender 4.5.10 LTS is discovered.

## Next Steps

1. Add a Unity Humanoid Avatar/retarget-specific smoke check separate from Generic Animator sampling.
2. Start textured/material QA and cleanup scoring for H-002 plus the strongest humanoids.
3. Capture a real Unreal Editor import pass or document the exact installation/configuration blocker with a repeatable verifier.

## Resume Prompt

Continue from this checkpoint. First read this file and the project `AGENTS.md`, then inspect `docs/asset-evidence-progress.md`, `docs/release-checklist.md`, `docs/unity-import-verification.md`, `evidence/H-002/unity-import.json`, and `tests/test_plan_unity_animator_smoke_migration.py` before making changes. The immediate next product-level task is moving beyond Generic Animator smoke into Unity Humanoid Avatar/retarget validation, then textured/material QA.
